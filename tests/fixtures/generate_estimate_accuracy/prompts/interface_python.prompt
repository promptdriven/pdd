<pdd-interface>
module: inventory
function: parse_inventory_csv(text: str) -> list[dict[str, str]]
function: summarize_inventory(rows: list[dict[str, str]]) -> dict[str, int]
</pdd-interface>

Implement a Python inventory parser. It should read CSV text, normalize column
names, skip blank rows, and return a useful summary by category.
