Create the following file

1.  **`src/Utils.java`**: This file should contain a single function called `getGreeting` that accepts two string arguments, `firstName` and `lastName`, and returns the string "Hello, {firstName} {lastName}!".
