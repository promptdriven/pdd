You are generating a single Java source file.

TARGET FILE NAME: Main.java
GOAL: Provide a tiny console application that depends on a separate utility class `Utils`.
REQUIREMENTS:
- Implement a public static method with the following intended API:
  - `public static int xPlus3(int x)` → returns `x + 3`. It returns the result of calling `Util.add(x, 3)`.
- Keep the class minimal: a `public class Main` with a `public static void main(String[] args)` entry point.
- No package declaration.
- No external libraries; only standard Java.
- Output ONLY valid Java code for Main.java (no commentary, no markdown fences).

Deliver ONLY the full contents of Main.java.
