%You are an expert Fish engineer. Your goal is to create pdd_completion.fish for the PDD CLI.

% The PDD CLI's features, including most commands and options, are defined in its README file:
    <readme_pdd>
        <include select="section:Usage,section:Commands,section:Options">README.md</include>
    </readme_pdd>

% Dependencies
<pdd.cli>
    <include select="class:cli,def:checkup,def:install_completion">context/change/15/initial_cli.py</include>
</pdd.cli>

% IMPORTANT ADDITION: Beyond the options detailed in the README.md, the PDD CLI also includes the following global option:
%   - `--time`: Controls LLM reasoning effort (0.0-1.0).
% This new global option must be supported in the completion script.

% The generated pdd_completion.fish script must provide comprehensive auto-completion support for:
%   - All commands of the PDD CLI (as per the README).
%   - All command-specific options (as per the README).
%   - The `checkup` command and its recovery/PR/review-loop options: `--start-step`, `--pr`, `--issue`, `--review-loop`, `--review-only`, `--reviewers`, `--reviewer`, `--fixer`, `--reviewer-fallback`, review round/cost/minute limits, reviewer-status compatibility flags (`--continue-on-reviewer-limit`, `--fallback-reviewer-on-failure`), `--blocking-severities`, `--clean-reviewer-states`, and the issue #1092 deterministic-gate flags `--no-gates`, `--gate-timeout`, and the repeatable `--gate-allow`. Emit only one `# checkup command` completion block — do not append a second checkup section for additional options, fish does not merge them and duplicates produce stale suggestions.
%   - All global options, including those found in the README and the newly specified `--time` option.
%   - Filenames, where appropriate for command arguments.
