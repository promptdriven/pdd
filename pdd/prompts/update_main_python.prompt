<include>context/python_preamble.prompt</include>
<include>context/ctx_obj_params.prompt</include>

<pdd-reason>CLI wrapper for updating prompts from modified code with repo-wide scan, agentic routing, and optional metadata sync.</pdd-reason>

<pdd-interface>
{
  "type": "module",
  "module": {
    "functions": [
      {
        "name": "update_main",
        "signature": "(ctx, input_prompt_file, modified_code_file, input_code_file, output, use_git, repo, extensions, directory, strength, temperature, simple=False, base_branch='main', budget=None, dry_run=False, sync_metadata=False)",
        "returns": "Optional[Tuple[str, float, str]]"
      }
    ]
  }
}
</pdd-interface>

<pdd-dependency>auto_include_python.prompt</pdd-dependency>
<pdd-dependency>metadata_sync_python.prompt</pdd-dependency>

% Goal
Write `pdd/update_main.py` — the CLI wrapper for updating prompts based on modified code, featuring repository-wide scanning, agentic routing, and post-update synchronization.

% Role & Scope
Supports three modes: true update, regeneration, and repository-wide updates. Routes to agentic or legacy update based on `simple` parameter and agent availability. In repo mode, detects changed files via fingerprint comparison (with git fallback) and performs post-update architecture/PRD sync.

% Requirements
1. Function: `update_main(ctx, input_prompt_file: Optional[str], modified_code_file: Optional[str], input_code_file: Optional[str], output: Optional[str], use_git, repo, extensions, directory, strength: Optional[float], temperature: Optional[float], simple: bool = False, base_branch: str = "main", budget: Optional[float] = None, dry_run: bool = False, sync_metadata: bool = False) -> Optional[Tuple[str, float, str]]`
2. Return (prompt, cost, model) on success, None on error.
3. Agentic routing (when `simple=False` and agents available): try agentic first, fall back to legacy on failure — applies to all three modes.
4. Read updated prompt file content after successful agentic update (agentic writes directly to file).
5. Retrieve verbose/quiet/time from ctx.obj; pass to update functions.
6. Resolve strength/temperature: prefer explicit params over ctx.obj values; update ctx.obj with resolved values.
7. Validate prompt is non-empty before writing (true update mode).
8. Without --output, overwrite input_prompt_file in place.
9. use_git and input_code_file are mutually exclusive (raise ValueError).
10. Before any legacy path writes generated/updated `.prompt` content, call `sanitize_prompt_output(content, destination_path)` so invalid `<include>` tags or selected includes are cleaned before a later `pdd sync` preprocess pass.
11. **Metadata sync orchestration**:
    - **Single-file & regeneration modes — default path (`sync_metadata=False`, #988)**: After the prompt has been written successfully, call the internal helper `_run_single_file_metadata_sync(prompt_path, modified_code_file)` (which wraps `run_metadata_sync(..., dry_run=False)` from `pdd.metadata_sync`) so the post-update fingerprint is finalized through the shared API rather than left stale. The helper prints one Rich error line per failed stage on a module-level **stderr** `Console` (constructed with `stderr=True` so the output is not interleaved with stdout success messages) when any stage fails or the orchestrator raises — `rich.print` / a default `Console()` writes to stdout, so the helper must NOT use those. On the default path, treat that stderr warning as the sole escalation: **do NOT raise `click.exceptions.Exit(1)`** and **do NOT swallow the helper's stderr output** — `update_main` must still return the normal success tuple so the CLI exits 0 and the rest of the flow (single-file `pdd update <code>` reported by users) completes normally. This closes the silent stale-metadata path described in #988 (`.pdd/meta/<module>.json` reflects the post-update prompt/code state on success; a finalization failure is reported on stderr rather than silently leaving partial metadata). The helper must not be invoked when the update itself failed or the writer returned None.
    - **Single-file & regeneration modes — opt-in (`sync_metadata=True`, #871)**: Same `_run_single_file_metadata_sync` call, but stricter exit semantics. When the orchestrator returns a `MetadataSyncResult` whose `.ok` is False (or it raises), `update_main` MUST raise `click.exceptions.Exit(1)` — `return None` is insufficient because the Click command wrapper (`pdd/commands/modify.py:update`) treats a None return as a normal completion (`exit_code 0`). Raising `click.exceptions.Exit(1)` is the only way to make the CLI exit non-zero, which is what the preflight auto-heal subprocess keys off (`agentic_change_orchestrator.py:1009` checks `returncode == 0`). The Click command wrapper must include `click.exceptions.Exit` in its re-raise list so a bare `except Exception` does not swallow the signal back into exit 0 (#871 acceptance criterion).
    - **Repo mode**: Replace the existing post-update per-pair fingerprint + per-pair architecture-sync sequence with a single `run_metadata_sync` call per successfully updated pair. The orchestrator handles fingerprint finalization (last), architecture sync, and run-report cleanup. **PRD sync remains in `update_main`** (called once after the per-pair loop, keyed off the count of pairs whose `MetadataSyncResult.stages['architecture']` reports updates) — the orchestrator deliberately does NOT call PRD sync itself, keeping its blast radius scoped to per-prompt metadata. One pair's failure must not abort the batch — accumulate per-pair `MetadataSyncResult` and render them in the summary table. **After the summary table is printed, if any per-pair `MetadataSyncResult.ok` is False, `update_main` MUST raise `click.exceptions.Exit(1)`** — same contract as single-file mode (the success tuple is reserved for "every pair finalized cleanly"). Without this, repo-mode silently returns the success tuple and Click exits 0, which is the exact false-green automation hazard #871 was wired up to prevent.
    - **Summary table**: Add a `metadata` column showing one of `synced`, `partial:<stage>`, `failed:<stage>`, `skipped`, or `dry-run`. When `sync_metadata=False`, render `skipped` so the column is present but informative.
    - **Failure reporting**: When any stage in `MetadataSyncResult` is not `ok`, print a Rich error line per failed stage with the stage name and reason. The `metadata` column makes it obvious which layer is incomplete.
    - **Backward compatibility**: When `sync_metadata=False` (default), repo mode keeps its current post-update fingerprint and architecture-sync calls (unchanged). Single-file/regeneration modes now also finalize fingerprints via `_run_single_file_metadata_sync` (#988), but the helper's failure is downgraded to a stderr warning rather than a non-zero exit, so existing successful flows still observe a normal (exit 0) success tuple.

% Modes
1. **True update**: `input_prompt_file` + `modified_code_file` + (use_git OR `input_code_file`).
2. **Regeneration**: Only `modified_code_file` provided. Derive prompt path via `resolve_prompt_code_pair`.
3. **Repo mode** (repo=True): Scan repo, detect changes via `is_code_changed`, update changed pairs, and run post-update sync.

% Helper Functions
- `_extract_template_vars(concrete_path, template) -> Optional[Dict]`: Reverse-match a path against a template to extract variables like `{category}` and `{name}`. Repeated variables use regex backreferences.
- `_resolve_prompt_from_pddrc(code_file_path, repo_root, language) -> Optional[str]`: Loads `.pddrc` (searching from code file parent, then repo_root), finds matching context, and expands `outputs.prompt.path` template using extracted variables via `expand_template`.
- `_resolve_existing_prompt_path_case_insensitive(prompt_path) -> Path`: If a case-insensitive filename match already exists in the prompt directory, return the actual on-disk path so mixed-case prompt suffixes are preserved on case-sensitive filesystems.
- `resolve_prompt_code_pair(code_file_path, quiet, output_dir) -> (prompt_path, code_path)`: Derives prompt file path. Uses `_find_nearest_pddrc_for_file` to determine effective repo root. Tries `.pddrc` template resolution first (when no output_dir). Falls back to `prompts_dir` logic with context-aware config. Preserves subdirectory structure relative to repo root (stripping `generate_output_path` prefix). Before creating or returning a prompt path, preserves the on-disk casing of any existing case-insensitive prompt filename match. Creates missing directories and empty prompt files.
- `_load_pddignore(scan_root) -> (patterns, pddignore_dir)`: Walk upward from scan_root to find `.pddignore` file (stopping at git root). Returns parsed patterns and the directory containing the file.
- `_is_pddignored(filepath, pddignore_root, patterns) -> bool`: Match file against `.pddignore` patterns — supports relative-path glob, basename glob, and directory-prefix patterns (ending with `/`).
- `_has_skip_suffix(filepath) -> bool`: Check if file stem ends with a skip suffix (.test, .spec, .stories, .story, .config, .setup, .e2e.test, .e2e.spec, .d).
- `_has_meaningful_code(filepath) -> bool`: Return True if file has at least one non-blank, non-comment line.
- `find_and_resolve_all_pairs(repo_root, quiet, extensions, output_dir) -> List[Tuple]`: Walks repo (using `git ls-files` or `os.walk` fallback), filters code files through multi-layer exclusion, resolves prompt pairs.
- `get_git_changed_files(repo_root, base_branch) -> Set[str]`: Combines merge-base diff, uncommitted changes, and untracked files into a set of absolute paths.
- `derive_basename_and_language(code_file_path, repo_root) -> (basename, language)`: Uses relative path from repo_root (without extension) as basename to avoid fingerprint collisions for same-named files in different directories.
- `_check_include_deps_changed(fingerprint) -> (bool, reason)`: Check if any stored include dependencies have changed on disk by comparing current SHA256 hashes against `fingerprint.include_deps`.
- `is_code_changed(code_file_path, repo_root, git_changed_files, prompt_file_path=None) -> (bool, reason)`: Prefers `infer_module_identity` from prompt_file_path for fingerprint lookup; falls back to `derive_basename_and_language`. Check fingerprint code_hash first; if code hash matches, check include dependency hashes; fall back to git changed set if no fingerprint.
- `update_file_pair(prompt_file, code_file, ctx, repo, simple) -> Dict`: Wrapper for single pair update. Tries agentic first, then legacy (untracked/empty -> update_prompt with generation sentinel, else -> git_update). Returns dict with prompt_file, status, cost, model, error.
- `_find_prd_file(project_root) -> Optional[Path]`: Find PRD file by convention: PRD.md, prd.md, *_prd.md, *_PRD.md.

% Repo Mode Behavior
- If `directory` provided, scan from that path; otherwise scan from repo root.
- Multi-layer file exclusion in `find_and_resolve_all_pairs`:
  - `git ls-files` (respects .gitignore); fallback `os.walk` ignoring dirs: .git, .idea, .vscode, __pycache__, node_modules, .venv, venv, dist, build, .next, .nuxt, .output, .cache, .turbo, .parcel-cache, coverage, .pdd.
  - Skip extensions via `_SKIP_EXTENSIONS` set: .json, .jsonl, .yaml, .yml, .md, .toml, .ini, .css, .html, .lock, .svg, .png, .jpg, .gif, .ico, .woff, .woff2, .ttf, .eot, .map, .csv, .txt.
  - Skip specific filenames via `_SKIP_FILENAMES` set (e.g., package-lock.json, jest.config.js, tailwind.config.js, mockServiceWorker.js, next-env.d.ts, etc.).
  - Skip basename suffixes via `_SKIP_BASENAME_SUFFIXES` (.config, .setup, .stories, .story, .test, .spec, .e2e.test, .e2e.spec, .d).
  - Exclude *.prompt, test_* prefix, *_example suffix.
  - `.pddignore` patterns (loaded via `_load_pddignore`, matched via `_is_pddignored`).
  - Skip files without meaningful code (`_has_meaningful_code`).
  - Only process files where `get_language()` returns non-None.
  - Filter by extensions if provided.
- After resolving pairs, call `ensure_pddrc_for_scan` from `pddrc_initializer` to auto-create `.pddrc` if needed.
- **Change detection**: Use `get_git_changed_files` + `is_code_changed` to filter pairs. Also include pairs with empty (0-byte) prompt files regardless of code changes. Include dependency hashes from fingerprints are checked so that changes to shared include files (preambles, examples) trigger updates.
- Progress bar: Rich Progress with spinner, bar, percentage, time remaining, and running total cost.
- **Post-update fingerprinting** (legacy, `sync_metadata=False` only): After each successful pair update, `save_fingerprint` via `infer_module_identity`. When `sync_metadata=True`, the per-pair `run_metadata_sync` call replaces this — do NOT also call `save_fingerprint` here, or the fingerprint will be written before the orchestrator's gates run and a stale fingerprint can record "in sync" even when an earlier metadata stage failed.
- **Post-update architecture sync** (legacy, `sync_metadata=False` only): Use `find_architecture_for_project` to locate architecture files; for each successfully updated prompt, call `update_architecture_from_prompt` if architecture file exists. When `sync_metadata=True`, this is handled inside `run_metadata_sync` (which also gates the write on prior-stage success), so do NOT call it again from `update_main`.
- **Post-update PRD sync** (both branches): If architecture entries were updated, find PRD file and use `run_agentic_task` to review/update PRD content (parse `<updated-prd>` tags from LLM output). PRD sync stays in `update_main` even under `sync_metadata=True` — the orchestrator deliberately does NOT call it (see Requirement 11). Key off the count of pairs whose `MetadataSyncResult.stages['architecture']` reported `updated`/`ok` (or, in the legacy branch, off the count returned by the inline arch loop).
- Summary table: `prompt file`, `status`, `cost`, `model`, `error` columns. When `sync_metadata=True`, append a `metadata` column (one of `synced`, `partial:<stage>`, `failed:<stage>`, `skipped`, `dry-run`) so partial heals surface visibly. Show architecture/PRD status if relevant.
- **Non-zero exit on per-pair metadata failure** (`sync_metadata=True` only): After the summary table prints, if any per-pair `MetadataSyncResult.ok` is False, raise `click.exceptions.Exit(1)` — the success tuple is reserved for "every pair finalized cleanly". Without this, repo-mode silently returns success and Click exits 0, which is the false-green automation hazard #871 was wired up to prevent.
- Return values:
  - `("Repository update complete.", total_cost, "models")` on a completed batch — i.e. at least one pair was actually updated (and, under `sync_metadata=True`, every per-pair `MetadataSyncResult.ok` is True). `total_cost` is summed across the batch; `"models"` is a comma-joined sorted list of models used.
  - `None` on early no-op exits BEFORE any pair is updated: not-in-a-git-repo, no scannable code files in the scan dir (the "No scannable code files found" branch), no changed code files (the "Everything is in sync" branch), and other input-error paths (ValueError, InvalidGitRepositoryError, broad Exception). `None` here means "nothing to do or nothing changed" and is the documented signal the CLI layer treats as exit_code 0.
  - `click.exceptions.Exit(1)` raised when `sync_metadata=True` and any per-pair `MetadataSyncResult.ok` is False (after the summary table prints). Reserved for "we ran the batch and at least one pair finalized with a `failed` stage" — distinct from the `None` no-op above.

% Error Handling
- Print Rich error (unless quiet) and return None on errors (ValueError, InvalidGitRepositoryError, Exception).
- Re-raise click.Abort.
- Pass confirm_callback, force, and context from ctx.obj to construct_paths.
- Module-level Rich Console with custom theme (info=cyan, warning=yellow, error=bold red, success=green, path=dim blue).

% Dependencies
<module_dependencies>
  <construct_paths_example><include>context/construct_paths_example.py</include></construct_paths_example>
  <update_prompt_example><include>context/update_prompt_example.py</include></update_prompt_example>
  <git_update_example><include>context/git_update_example.py</include></git_update_example>
  <agentic_common_example><include select="def:example_run_agentic_task,def:example_provider_preference">context/agentic_common_example.py</include></agentic_common_example>
  <sync_determine_operation_example><include select="def:example_determine_operation">context/sync_determine_operation_example.py</include></sync_determine_operation_example>
  <architecture_sync_example><include select="def:example_update_single_prompt,def:example_get_entry,def:example_sync_all,def:example_find_architecture_for_project,def:example_update_architecture_from_prompt">context/architecture_sync_example.py</include></architecture_sync_example>
  <operation_log_example><include select="def:example_save_fingerprint,def:example_infer_module_identity">context/operation_log_example.py</include></operation_log_example>
  <template_expander_example><include>context/template_expander_example.py</include></template_expander_example>
  <validate_prompt_includes><include select="def:sanitize_prompt_output">pdd/validate_prompt_includes.py</include></validate_prompt_includes>
  <pdd.agentic_update><include select="def:example_no_agents_available,def:example_successful_update,def:example_agent_runs_but_no_change,def:example_missing_input_files">context/agentic_update_example.py</include></pdd.agentic_update>
<pdd.api_key_scanner>
    <include>context/api_key_scanner_example.py</include>
</pdd.api_key_scanner>
<pdd.auto_include>
    <include select="def:main">context/auto_include_example.py</include>
</pdd.auto_include>
<pdd.metadata_sync>
    <include optional>context/metadata_sync_example.py</include>
</pdd.metadata_sync>
</module_dependencies>

% Deliverables
- Code: `pdd/update_main.py`
