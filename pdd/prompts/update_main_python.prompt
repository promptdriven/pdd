<include>context/python_preamble.prompt</include>
<include>context/ctx_obj_params.prompt</include>
<pdd-dependency>auto_include_python.prompt</pdd-dependency>

% Goal
Write `pdd/update_main.py` — the CLI wrapper for updating prompts based on modified code, featuring repository-wide scanning, agentic routing, and post-update synchronization.

% Role & Scope
Supports three modes: true update, regeneration, and repository-wide updates. Routes to agentic or legacy update based on `simple` parameter and agent availability. In repo mode, detects changed files via fingerprint comparison (with git fallback) and performs post-update architecture/PRD sync.

% Requirements
1. Function: `update_main(ctx, input_prompt_file: Optional[str], modified_code_file: Optional[str], input_code_file: Optional[str], output: Optional[str], use_git, repo, extensions, directory, strength: Optional[float], temperature: Optional[float], simple: bool = False, base_branch: str = "main") -> Optional[Tuple[str, float, str]]`
2. Return (prompt, cost, model) on success, None on error.
3. Agentic routing (when `simple=False` and agents available): try agentic first, fall back to legacy on failure — applies to all three modes.
4. Read updated prompt file content after successful agentic update (agentic writes directly to file).
5. Retrieve verbose/quiet/time from ctx.obj; pass to update functions.
6. Resolve strength/temperature: prefer explicit params over ctx.obj values; update ctx.obj with resolved values.
7. Validate prompt is non-empty before writing (true update mode).
8. Without --output, overwrite input_prompt_file in place.
9. use_git and input_code_file are mutually exclusive (raise ValueError).

% Modes
1. **True update**: `input_prompt_file` + `modified_code_file` + (use_git OR `input_code_file`).
2. **Regeneration**: Only `modified_code_file` provided. Derive prompt path via `resolve_prompt_code_pair`.
3. **Repo mode** (repo=True): Scan repo, detect changes via `is_code_changed`, update changed pairs, and run post-update sync.

% Helper Functions
- `_extract_template_vars(concrete_path, template) -> Optional[Dict]`: Reverse-match a path against a template to extract variables like `{category}` and `{name}`. Repeated variables use regex backreferences.
- `_resolve_prompt_from_pddrc(code_file_path, repo_root, language) -> Optional[str]`: Loads `.pddrc` (searching from code file parent, then repo_root), finds matching context, and expands `outputs.prompt.path` template using extracted variables via `expand_template`.
- `_resolve_existing_prompt_path_case_insensitive(prompt_path) -> Path`: If a case-insensitive filename match already exists in the prompt directory, return the actual on-disk path so mixed-case prompt suffixes are preserved on case-sensitive filesystems.
- `resolve_prompt_code_pair(code_file_path, quiet, output_dir) -> (prompt_path, code_path)`: Derives prompt file path. Uses `_find_nearest_pddrc_for_file` to determine effective repo root. Tries `.pddrc` template resolution first (when no output_dir). Falls back to `prompts_dir` logic with context-aware config. Preserves subdirectory structure relative to repo root (stripping `generate_output_path` prefix). Before creating or returning a prompt path, preserves the on-disk casing of any existing case-insensitive prompt filename match. Creates missing directories and empty prompt files.
- `_load_pddignore(scan_root) -> (patterns, pddignore_dir)`: Walk upward from scan_root to find `.pddignore` file (stopping at git root). Returns parsed patterns and the directory containing the file.
- `_is_pddignored(filepath, pddignore_root, patterns) -> bool`: Match file against `.pddignore` patterns — supports relative-path glob, basename glob, and directory-prefix patterns (ending with `/`).
- `_has_skip_suffix(filepath) -> bool`: Check if file stem ends with a skip suffix (.test, .spec, .stories, .story, .config, .setup, .e2e.test, .e2e.spec, .d).
- `_has_meaningful_code(filepath) -> bool`: Return True if file has at least one non-blank, non-comment line.
- `find_and_resolve_all_pairs(repo_root, quiet, extensions, output_dir) -> List[Tuple]`: Walks repo (using `git ls-files` or `os.walk` fallback), filters code files through multi-layer exclusion, resolves prompt pairs.
- `get_git_changed_files(repo_root, base_branch) -> Set[str]`: Combines merge-base diff, uncommitted changes, and untracked files into a set of absolute paths.
- `derive_basename_and_language(code_file_path, repo_root) -> (basename, language)`: Uses relative path from repo_root (without extension) as basename to avoid fingerprint collisions for same-named files in different directories.
- `_check_include_deps_changed(fingerprint) -> (bool, reason)`: Check if any stored include dependencies have changed on disk by comparing current SHA256 hashes against `fingerprint.include_deps`.
- `is_code_changed(code_file_path, repo_root, git_changed_files, prompt_file_path=None) -> (bool, reason)`: Prefers `infer_module_identity` from prompt_file_path for fingerprint lookup; falls back to `derive_basename_and_language`. Check fingerprint code_hash first; if code hash matches, check include dependency hashes; fall back to git changed set if no fingerprint.
- `update_file_pair(prompt_file, code_file, ctx, repo, simple) -> Dict`: Wrapper for single pair update. Tries agentic first, then legacy (untracked/empty -> update_prompt with generation sentinel, else -> git_update). Returns dict with prompt_file, status, cost, model, error.
- `_find_prd_file(project_root) -> Optional[Path]`: Find PRD file by convention: PRD.md, prd.md, *_prd.md, *_PRD.md.

% Repo Mode Behavior
- If `directory` provided, scan from that path; otherwise scan from repo root.
- Multi-layer file exclusion in `find_and_resolve_all_pairs`:
  - `git ls-files` (respects .gitignore); fallback `os.walk` ignoring dirs: .git, .idea, .vscode, __pycache__, node_modules, .venv, venv, dist, build, .next, .nuxt, .output, .cache, .turbo, .parcel-cache, coverage, .pdd.
  - Skip extensions via `_SKIP_EXTENSIONS` set: .json, .jsonl, .yaml, .yml, .md, .toml, .ini, .css, .html, .lock, .svg, .png, .jpg, .gif, .ico, .woff, .woff2, .ttf, .eot, .map, .csv, .txt.
  - Skip specific filenames via `_SKIP_FILENAMES` set (e.g., package-lock.json, jest.config.js, tailwind.config.js, mockServiceWorker.js, next-env.d.ts, etc.).
  - Skip basename suffixes via `_SKIP_BASENAME_SUFFIXES` (.config, .setup, .stories, .story, .test, .spec, .e2e.test, .e2e.spec, .d).
  - Exclude *.prompt, test_* prefix, *_example suffix.
  - `.pddignore` patterns (loaded via `_load_pddignore`, matched via `_is_pddignored`).
  - Skip files without meaningful code (`_has_meaningful_code`).
  - Only process files where `get_language()` returns non-None.
  - Filter by extensions if provided.
- After resolving pairs, call `ensure_pddrc_for_scan` from `pddrc_initializer` to auto-create `.pddrc` if needed.
- **Change detection**: Use `get_git_changed_files` + `is_code_changed` to filter pairs. Also include pairs with empty (0-byte) prompt files regardless of code changes. Include dependency hashes from fingerprints are checked so that changes to shared include files (preambles, examples) trigger updates.
- Progress bar: Rich Progress with spinner, bar, percentage, time remaining, and running total cost.
- **Post-update fingerprinting**: After successful update, `save_fingerprint` for the pair via `infer_module_identity`.
- **Post-update architecture sync**: Use `find_architecture_for_project` to locate architecture files. For each successfully updated prompt, call `update_architecture_from_prompt` if architecture file exists.
- **Post-update PRD sync**: If architecture entries were updated, find PRD file and use `run_agentic_task` to review/update PRD content (parse `<updated-prd>` tags from LLM output).
- Summary table: prompt file, status, cost, model, error columns. Show architecture/PRD status if relevant.
- Return: ("Repository update complete.", total_cost, "models") or None if no files/no changes.

% Error Handling
- Print Rich error (unless quiet) and return None on errors (ValueError, InvalidGitRepositoryError, Exception).
- Re-raise click.Abort.
- Pass confirm_callback, force, and context from ctx.obj to construct_paths.
- Module-level Rich Console with custom theme (info=cyan, warning=yellow, error=bold red, success=green, path=dim blue).

% Dependencies
<module_dependencies>
  <construct_paths_example><include>context/construct_paths_example.py</include></construct_paths_example>
  <update_prompt_example><include>context/update_prompt_example.py</include></update_prompt_example>
  <git_update_example><include>context/git_update_example.py</include></git_update_example>
  <agentic_common_example><include select="def:example_run_agentic_task,def:example_provider_preference">context/agentic_common_example.py</include></agentic_common_example>
  <sync_determine_operation_example><include select="def:example_determine_operation">context/sync_determine_operation_example.py</include></sync_determine_operation_example>
  <architecture_sync_example><include select="def:example_update_single_prompt,def:example_get_entry,def:example_sync_all,def:example_find_architecture_for_project,def:example_update_architecture_from_prompt">context/architecture_sync_example.py</include></architecture_sync_example>
  <operation_log_example><include select="def:example_save_fingerprint,def:example_infer_module_identity">context/operation_log_example.py</include></operation_log_example>
  <template_expander_example><include>context/template_expander_example.py</include></template_expander_example>
  <pdd.agentic_update><include select="def:example_no_agents_available,def:example_successful_update,def:example_agent_runs_but_no_change,def:example_missing_input_files">context/agentic_update_example.py</include></pdd.agentic_update>
<pdd.api_key_scanner>
    <include>context/api_key_scanner_example.py</include>
</pdd.api_key_scanner>
<pdd.auto_include>
    <include select="def:main">context/auto_include_example.py</include>
</pdd.auto_include>
</module_dependencies>

% Deliverables
- Code: `pdd/update_main.py`
