<pdd-reason>CLI commands for the Generate Suite: generate, test, and example, with GitHub URL detection for agentic modes.</pdd-reason>
<pdd-interface>
{
  "type": "cli",
  "cli": {
    "commands": [
      {"name": "generate", "description": "Generate code from prompt or trigger agentic architecture from GitHub issue"},
      {"name": "test", "description": "Generate or enhance unit tests (agentic or manual mode)"},
      {"name": "example", "description": "Generate context examples from prompt and code"}
    ]
  }
}
</pdd-interface>
<pdd-dependency>agentic_architecture_python.prompt</pdd-dependency>
<pdd-dependency>agentic_test_python.prompt</pdd-dependency>
<pdd-dependency>cmd_test_main_python.prompt</pdd-dependency>
<pdd-dependency>code_generator_main_python.prompt</pdd-dependency>
<pdd-dependency>context_generator_main_python.prompt</pdd-dependency>
<pdd-dependency>operation_log_python.prompt</pdd-dependency>
<pdd-dependency>template_registry_python.prompt</pdd-dependency>
<pdd-dependency>track_cost_python.prompt</pdd-dependency>
% You are an expert Python engineer. Your goal is to write `pdd/commands/generate.py`.

% Role & Scope
  Implement the "Generate Suite" CLI commands using `click` and `rich`. These commands interface with LLM-driven generation workflows for code, tests, and examples.

<include>context/python_preamble.prompt</include>

% Requirements
  1. **generate**:
     - **Interface**: `prompt_file` (arg, `click.UNPROCESSED`), `--template`, `-e/--env` (multiple), `--output`, `--original-prompt`, `--incremental` (flag), `--experimental-prd` (flag), `--dry-run` (flag), `--unit-test`, `--exclude-tests` (flag), `--skip-prompts` (flag), `--output-dir`, `--force-single` (flag), `--no-github-state`, `--project-root` (path, optional).
     - **`--project-root`**: Optional explicit project root override for agentic and incremental PRD modes. When supplied, this path is used as the resolved project root instead of walking up from cwd to find a marker (`.pddrc`/`.pdd/`, then `sources/` + PRD/spec markdown, then `.git`). Useful when the cwd is a self-contained pdd project nested inside an unrelated outer git repo. Forwarded as the `project_root` keyword argument to both `run_agentic_architecture` and `run_incremental_architecture`. Standard prompt-file mode does not support `--project-root`; passing it there must raise `click.UsageError` so users learn the flag is mode-specific instead of silently no-opping.
     - **Experimental Incremental PRD Mode**: Only if both `--incremental` and `--experimental-prd` are set and `prompt_file` is a GitHub issue URL, or is a PRD-like file (`.md`, `.markdown`, `.txt`, `.rst`, `.adoc`) without code-generation-only options (`--output`, `--original-prompt`, `--env`, `--template`, `--unit-test`, `--exclude-tests`), call `run_incremental_architecture(prd_source=prompt_file, dry_run=dry_run, target_dir=output_dir, use_github_state=not no_github_state, strength=<global strength>, temperature=<global temperature>, time=<global time>, project_root=project_root, ...)`.
     - **Explicit PRD opt-in**: If `--incremental` is set with a GitHub issue URL or PRD-like file that would otherwise route to PRD propagation, but `--experimental-prd` is absent, raise a `click.UsageError` telling the user to use `pdd generate --incremental --experimental-prd <prd-file-or-issue-url>`. Do not silently dispatch based on suffix alone.
     - **Dry run guard**: `--dry-run` is only valid with `--incremental --experimental-prd` PRD/GitHub issue mode.
     - In Incremental PRD dry-run output, label returned paths as `Would change files:` instead of `Output files:` because dry-run must not imply files were written.
     - **Agentic Mode**: If `prompt_file` matches a GitHub issue URL without `--incremental`, call `run_agentic_architecture(issue_url=prompt_file, skip_prompts=skip_prompts, target_dir=output_dir, use_github_state=not no_github_state, project_root=project_root, ...)`.
     - **Standard Mode**: Validate `prompt_file` vs `--template` exclusivity. Resolve templates via `load_template`. Parse `env` as `KEY=VALUE` or `KEY` (from `os.environ`).
     - **Guardrail**: Reject `--dry-run` outside Incremental PRD Mode.
     - **Logic**: Call `code_generator_main` with parsed options.
     - **Decorators**: `@log_operation(operation="generate", clears_run_report=True, updates_fingerprint=True)`, `@track_cost`.

  2. **example**:
     - **Interface**: `prompt_file`, `code_file` (args), `--output`, `--format` (choice: code, md).
     - **Logic**: Call `context_generator_main`.
     - **Decorators**: `@log_operation(operation="example", updates_fingerprint=True)`, `@track_cost`.

  3. **test**: Supports Agentic (default) and Manual (`--manual`) modes.
     - **Interface**: `args` (nargs=-1), `--manual`, `--timeout-adder`, `--no-github-state`, `--output`, `--language`, `--coverage-report`, `--existing-tests` (multiple), `--target-coverage`, `--merge`.
     - **Agentic Mode**: Triggered if first arg is a URL and not `--manual`. Exactly 1 arg required. Call `run_agentic_test(issue_url=args[0], timeout_adder=timeout_adder, use_github_state=not no_github_state, ...)`.
     - **Manual Mode**: Triggered by `--manual`. Exactly 2 args required (PROMPT, CODE). Call `cmd_test_main` with all relevant options.
     - **Decorators**: `@log_operation(operation="test", updates_run_report=True)`, `@track_cost`.

% Implementation Details
  - Use `GenerateCommand(click.Command)` to handle help text for conditionally required arguments.
  - Defer heavy imports (like `code_generator_main`) to avoid slow module load.
  - Use `handle_error(e, command_name, quiet)` for general exceptions.
  - **CRITICAL**: Re-raise `click.Abort` and `click.UsageError` type exceptions; do not swallow them in `handle_error`.
  - Use `rich.console.Console` for stylized output in agentic modes.

% Dependencies
  <include>context/command_logging.prompt</include>
  <include select="def:code_generator_main">context/code_generator_main_example.py</include>
  <include>context/agentic_architecture_example.py</include>
  <include>context/context_generator_main_example.py</include>
  <include select="def:cmd_test_main">context/cmd_test_main_example.py</include>
  <include>context/agentic_test_example.py</include>
  <include>context/track_cost_example.py</include>
  <utils.auth_service>
      <include select="def:verify_auth">context/auth_service_example.py</include>
  </utils.auth_service>

% Deliverables
  - Code: `pdd/commands/generate.py`
