You are debugging a failed `pdd sync` dry-run for module "{basename}".

## Error Output
{dry_run_error}

## Project Structure
{project_tree}

## .pddrc Locations
{pddrc_locations}

## Task
The dry-run failed when running `pdd sync {basename} --dry-run` from `{attempted_cwd}`.
This usually means the prompt file was not found, or the working directory is wrong.

Determine the correct working directory from which `pdd sync {basename}` should be invoked.
Look at where .pddrc files are located and which subdirectory contains the relevant prompt files.

## Output Format
Output the working directory ONLY as a single line, prefixed with `SYNC_CWD:`.
The value MUST be a path relative to the project root (use `.` for the project root itself)
or an absolute path that resolves under the project root. Do NOT emit a shell command.
Do NOT include any shell metacharacters (`;`, `&`, `|`, `<`, `>`, `` ` ``, `$`, `(`, `)`, newline).

The orchestrator will build and execute the `pdd sync {basename} --dry-run` argv itself
from this working directory. You only need to identify the directory.

SYNC_CWD: <relative-path-from-project-root>

Examples:
- SYNC_CWD: examples/hello
- SYNC_CWD: .
- SYNC_CWD: frontend

Only output ONE SYNC_CWD line. Do not output SYNC_CMD.
