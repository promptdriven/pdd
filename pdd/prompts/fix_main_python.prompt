<pdd-reason>Runs bounded unit-test repair across local, cloud, and compressed-context modes.</pdd-reason>

<pdd-interface>
{
  "type": "cli",
  "cli": {
    "commands": [
      {"name": "fix_main", "signature": "(ctx: click.Context, prompt_file: str, code_file: str, unit_test_file: str, error_file: str, output_test: Optional[str], output_code: Optional[str], output_results: Optional[str], loop: bool, verification_program: Optional[str], max_attempts: int, budget: float, auto_submit: bool, agentic_fallback: bool = True, strength: Optional[float] = None, temperature: Optional[float] = None, protect_tests: bool = False, test_files: list[str] | None = None, failure_aware_retries: bool = True, compress_test_context: bool | None = None, context_compression: str | None = None, compression_fallback: str | None = None, compressed_context: Mapping[str, Any] | None = None, agentic_fallback_events: list[dict[str, Any]] | None = None) -> Tuple[bool, str, str, int, float, str]", "returns": "Tuple[bool, str, str, int, float, str]"}
    ]
  }
}
</pdd-interface>

<pdd-dependency>fix_error_loop_python.prompt</pdd-dependency>
<pdd-dependency>fix_focus_python.prompt</pdd-dependency>
<pdd-dependency>agentic_langtest_python.prompt</pdd-dependency>
<pdd-dependency>auth_service_python.prompt</pdd-dependency>
<pdd-dependency>core/cloud_python.prompt</pdd-dependency>
<pdd-dependency>compressed_sync_context_python.prompt</pdd-dependency>

% You are an expert Python engineer. Write `fix_main`, the CLI wrapper for the PDD `fix` command. It orchestrates single-pass fixes via `fix_errors_from_unit_tests`, iterative fixes via `fix_error_loop`, optional cloud execution, and optional auto-submit of successful fixes.

<include>context/python_preamble.prompt</include>
<include>context/ctx_obj_params.prompt</include>

% Dependencies
<pdd.core.fix_error_loop>
    <include select="class:IterationResult,def:fix_error_loop,def:extract_test_results">context/change/16/fix_error_loop.py</include>
</pdd.core.fix_error_loop>
<pdd.core.fix_focus>
    <include optional mode="interface">pdd/fix_focus.py</include>
</pdd.core.fix_focus>
<pdd.core.fix_unit_tests>
    <include select="def:fix_errors_from_unit_tests">context/change/10/initial_fix_errors_from_unit_tests.py</include>
</pdd.core.fix_unit_tests>
<pdd.core.auth>
    <include select="def:get_cached_jwt,def:get_refresh_token,def:verify_auth,def:get_auth_status">context/auth_service_example.py</include>
</pdd.core.auth>
<pdd.utils.languages>
    <include select="def:default_verify_cmd_for,path:missing_tool_hints">context/agentic_langtest_example.py</include>
</pdd.utils.languages>
<pdd.cli>
    <include select="def:get_paths_for_shell,class:cli,def:log_cost">context/change/15/initial_cli.py</include>
</pdd.cli>

% Inputs
- `ctx` (`click.Context`)
- `prompt_file`, `code_file`, `unit_test_file`, `error_file` (`str`)
- `output_test`, `output_code`, `output_results` (`Optional[str]`)
- `loop` (`bool`)
- `verification_program` (`Optional[str]`)
- `max_attempts` (`int`)
- `budget` (`float`)
- `auto_submit` (`bool`)
- `agentic_fallback` (`bool`, default `True`)
- `strength`, `temperature` (`Optional[float]`)
- `protect_tests` (`bool`, default `False`)
- `test_files` (`list[str] | None`)
- `failure_aware_retries` (`bool`, default `True`)
- `compress_test_context` (`bool | None`, default `None`)
- `context_compression` (`str | None`, default `None`)
- `compression_fallback` (`str | None`, default `None`)
- `compressed_context` (`Mapping[str, Any] | None`, default `None`): optional sync-provided repair context containing failing tests/errors, relevant contract rules, prompt interface, and selected prior repair evidence.
- `agentic_fallback_events` (`list[dict[str, Any]] | None`, default `None`): optional mutable list from sync that records per-phase agentic fallback telemetry (`phase`, `attempted`, `used`, `detail`). Forward to `fix_error_loop` in loop mode when supplied.

% Output
Return `Tuple[bool, str, str, int, float, str]`:
- success
- fixed unit test content
- fixed code content
- attempt count
- total cost
- model name, or `f"Error: {e}"` on failure

% Requirements
1. Validate inputs before the main file-processing logic:
   - `loop=True` requires `verification_program`, otherwise raise `click.UsageError`.
   - In non-loop mode, verify `error_file` exists before calling `construct_paths`; raise `FileNotFoundError` if missing.
   - Initialize `analysis_results = None` before the main `try`.

2. Build paths through `construct_paths`:
   - Always pass prompt/code/test in `input_file_paths`.
   - Include `error_file` only in non-loop mode.
   - Resolve the command as `fix` and feed through the requested output path overrides.
   - Pass `force`, `quiet`, `context_override=ctx.obj.get("context")`, `confirm_callback=ctx.obj.get("confirm_callback")`, and `create_error_file=loop`.

3. Resolve runtime options from direct arguments first, then `ctx.obj`:
   - `strength` falls back to `ctx.obj.get("strength", DEFAULT_STRENGTH)`.
   - `temperature` falls back to `ctx.obj.get("temperature", 0)`.
   - Also read `time`, `verbose`, and `local` behavior from `ctx.obj`.

4. Execution strategy:
   - Loop mode always calls `fix_error_loop` with the original required file-path arguments positionally, then uses keyword arguments for every optional loop parameter so signature drift cannot misroute values: `prompt=input_strings["prompt_file"]`, `verification_program=verification_program`, `strength=strength`, `temperature=temperature`, `time=time`, `max_attempts=max_attempts`, `budget=budget`, `error_log_file=output_file_paths.get("output_results")`, `verbose=verbose`, `agentic_fallback=agentic_fallback`, `protect_tests=protect_tests`, `use_cloud=use_cloud`, `test_files=test_files`, `failure_aware_retries=failure_aware_retries`, `no_local_fallback=cloud_only`, `compress_test_context=compress_test_context`, `context_compression=context_compression`, `compression_fallback=compression_fallback`, `compressed_context=compressed_context`, and `agentic_fallback_events=agentic_fallback_events` when sync supplies it.
   - Loop mode is hybrid by default: local test execution, but `use_cloud=True` for LLM calls unless local execution is explicitly forced. `PDD_CLOUD_ONLY` / `PDD_NO_LOCAL_FALLBACK` still keep cloud LLM calls enabled in loop mode.
   - Single-pass mode is cloud-first unless local execution is forced or `CloudConfig.is_running_in_cloud()` says the process is already running inside the cloud worker environment.
   - Respect `PDD_CLOUD_ONLY` / `PDD_NO_LOCAL_FALLBACK` so cloud failures do not silently fall back to local execution when fallback is disabled. Pass `no_local_fallback=cloud_only` to `fix_error_loop`; it stops the loop and logs an error instead of attempting a local LLM call when fallback is forbidden.

5. Single-pass cloud mode:
   - Authenticate with `CloudConfig.get_jwt_token(verbose=verbose)`.
   - POST to `CloudConfig.get_endpoint_url("fixCode")` with timeout `get_cloud_timeout()`.
   - Payload includes `unitTest`, `code`, `prompt`, `errors`, `language`, `strength`, `temperature`, `time` (defaulting to `0.25` when unset), `verbose`, and `protectTests`.
   - When `compressed_context` is provided, include its rendered repair context in the single-pass cloud payload and local LLM prompt as supplemental context while preserving concrete failing test output and error logs.
   - Focused-repair path (large dev units): before building the payload, if `is_large(code_content, unit_test_content)`, call `prepare_focused_inputs` to obtain sliced failing-test and code subsets. Send those slices as `unitTest` / `code`, set `protectTests` to `protect_tests or bool(focused_inputs)`, and store `focused_inputs.slices` for reconstruction.
   - Parse `success`, `fixedUnitTest`, `fixedCode`, `analysis`, `updateUnitTest`, `updateCode`, `totalCost`, and `modelName`.
   - If focused repair was used and the cloud result includes updated code, reconstruct the full code with `reconstruct_code(original_code, fixedCode, focused_inputs.slices)` before validation, saving, or returning.
   - If the cloud result contains candidate fixes, validate them locally with `run_pytest_on_file` before reporting success. In focused-repair mode, validate against the full original test file, never against returned sliced test content.
   - Fall back to local fixing on recoverable auth/network/timeout/5xx/invalid-JSON/no-fixed-code cases.
   - Raise `click.UsageError` instead of falling back for non-recoverable cases: 400, 401, 402, 403, or any cloud-only failure.

6. Single-pass local mode:
   - Call `fix_errors_from_unit_tests` with processed `input_strings` for `unit_test`, `code`, `prompt`, and `error`.
   - Pass `error_file=output_file_paths.get("output_results")`, `strength`, `temperature`, `time`, `verbose`, and `protect_tests or bool(focused_inputs)` when focused repair is active.
   - Set `attempts = 1`.
   - Do not trust `updateUnitTest` / `updateCode` alone; if either file changed, validate with `run_pytest_on_file` using temp files. If nothing changed, `success=False`.
   - Focused-repair path (large dev units): if `is_large(code_content, unit_test_content)`, call `prepare_focused_inputs` to obtain sliced failing-test and code subsets. Pass the slices as `unit_test` / `code` to the LLM call. After the call, reconstruct the full code with `reconstruct_code(code_content, fixed_code, focused_inputs.slices)`. Never use the returned sliced test content as the final test file; validate the fixed code against the full original test file with `run_pytest_on_file`. Always pass `protect_tests=True` (or equivalent `effective_protect_tests = protect_tests or bool(focused_inputs)`) to the LLM when focused slices are in use, so the model cannot return a test-only fix that would be silently discarded.

7. Saving and reporting:
   - Write only non-empty outputs.
   - Never write the fixed test file when `protect_tests=True`; optionally log that skip in verbose mode.
   - In focused-repair mode, the LLM only sees a test slice, so the returned test content is always a partial subset; never write it back as the final test file — always use the original full test content for the saved test output.
   - Create parent directories before writing.
   - When not quiet, print success/failure, attempts, cost, model, and saved paths with Rich.
   - Only print the test/code output path when that file was actually produced.
   - In verbose mode, print only a short analysis preview and escape Rich markup safely when preview text is invalid.

8. Auto-submit:
   - Run only when `success` is true, `auto_submit` is true, `PDD_FORCE_LOCAL` is not enabled, and `CloudConfig.is_running_in_cloud()` is False. When skipped for the cloud reason, emit a yellow `Skipping example submission: cloud executor has no interactive auth path.` (subject to `quiet`).
   - Before calling the lower-level async `get_jwt_token(...)`, call `CloudConfig.ensure_default_env()` so `PDD_CLOUD_URL` infers staging/prod/local before cached JWT audience validation. This prevents a prod cached JWT from being reused against a staging `submitExample` URL (#859 follow-up).
   - Get a JWT with `asyncio.run(asyncio.wait_for(get_jwt_token(...), timeout=auth_timeout_s))` where `auth_timeout_s` is `float(os.environ.get('PDD_AUTO_SUBMIT_AUTH_TIMEOUT_S', '300'))` parsed defensively (`try/except (TypeError, ValueError)` falls back to `300.0`). On `asyncio.TimeoutError` / `TimeoutError`, emit a yellow warning (subject to `quiet`) and skip submission; do not propagate as a fix failure.
   - Preprocess the prompt with `recursive=False` and `double_curly_brackets=True`.
   - Submit to `CloudConfig.get_endpoint_url("submitExample")` (imported from `pdd.core.cloud`) so `PDD_CLOUD_URL` is honored for staging/prod/local — never hardcode the production `submitExample` URL (#859, Firebase audience mismatch under staging canary). The `requests.post` call MUST pass `timeout=get_cloud_request_timeout()` (imported from `pdd.core.cloud`) — every other cloud POST in this package uses the same helper, and omitting `timeout=` is a sibling copy-paste variant of the `pdd sync` hang documented in #1272.
   - Include original prompt/code/test, optional error/example inputs, fixed code/test outputs, and analysis from `output_results` if readable, otherwise from in-memory `analysis_results`.
   - Metadata should include a title/description, language from `get_language(os.path.splitext(code_file)[1])`, empty framework, tags `["auto-fix", "example"]`, `isPublic=True`, and `price=0.0`.
   - Submission failures should be reported but must not change the main fix result.

8a. Compression and fallback metadata:
   - Expose whether agentic fallback was invoked or used so sync/evidence can record it.
   - Preserve `protect_tests`, focused repair, cloud-only fallback, and direct CLI behavior when no compressed context is supplied.

9. Error handling:
   - Re-raise `click.Abort` and `click.UsageError`.
   - For all other exceptions, print Rich-safe error messages; handle `MarkupError` explicitly by escaping its message.
   - Return `(False, "", "", 0, 0.0, f"Error: {e}")` instead of exiting the process.
