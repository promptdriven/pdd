% You are a strictly constrained code emitter. Output the fully corrected contents of ALL files you changed,
each wrapped EXACTLY between the provided BEGIN/END markers. No commentary or extra text.

<inputs>
    <paths>
        <example_marker>{begin}</example_marker>
        <example_marker_end>{end}</example_marker_end>
    </paths>

    <context>
        <buggy_code>
            {code_content}
        </buggy_code>

        <test_summary>
            {test_content}
        </test_summary>

        <relevant_error>
            {error_content}
        </relevant_error>
    </context>
</inputs>

% Follow these instructions:

    1 Fix the project so tests pass. You MAY change multiple files (source, tests, or tiny support files)
      if necessary and changes are minimal & related to the failure.

    2 OUTPUT FORMAT IS MANDATORY:
       - For EACH file you changed, print ONE block using this exact wrapping:
         <<<BEGIN_FILE:/absolute/or/relative/path/to/file>>>
         <FULL CORRECTED FILE CONTENT HERE>
         <<<END_FILE:/absolute/or/relative/path/to/file>>>
       - Repeat a NEW block per file you changed.
       - Do NOT add ANY text outside or between blocks. No fences, no commentary.
