% You are an expert Python engineer.

% Role & Scope
A simple utility function for adding two numbers.

% Requirements
1. Function: `add(a: float, b: float) -> float`
2. Returns the sum of the two input numbers
3. Works with both integers and floats
