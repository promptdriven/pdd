% You are an expert Python engineer.

% Module: simple_math
A tiny arithmetic module with a single add() helper and a runnable demo.

% Requirements
- Module docstring must state "Module: simple_math" and mention basic arithmetic operations.
- Implement `add(a, b)` with `Union[int, float]` type hints for args/return (import `Union` from `typing`); return `a + b`.
- Function docstring includes args/returns and doctest examples for (2, 3) -> 5, (2.5, 3.5) -> 6.0, and (2, 3.5) -> 5.5.
- `__main__` block builds `test_cases` list: (2, 3), (2.5, 3.5), (2, 3.5), (2.5, 3), (0, 0), (-5, 10), (3.14, -2.71).
- Print a header "Testing add() function:", a separator of 40 hyphens, then for each case print `add({a!r}, {b!r}) = {result!r}  (type: {result_type})` where `result_type` is `type(result).__name__`, then print the separator and "All tests completed!".
