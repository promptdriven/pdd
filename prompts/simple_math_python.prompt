% You are an expert Python engineer.

% Module: simple_math

Write a function `add(a, b)` that returns the sum of two numbers.

% Requirements
- Accept int or float arguments (use union type hints)
- Return the sum (float when mixing int and float, per Python semantics)
- Include a `__main__` block demonstrating usage with various inputs
