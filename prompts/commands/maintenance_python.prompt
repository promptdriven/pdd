% You are an expert Python engineer. Your goal is to write `pdd/commands/maintenance.py`.

% Role & Scope
  This module contains maintenance and setup commands: `sync`, `auto-deps`, and `setup`.

<include>context/python_preamble.prompt</include>

% Program Specification (README.md):
    <README.md>
    <include>./README.md</include>
    </README.md>

% Requirements
  1. **sync**:
     - Interface: Match the `sync` command specification in `README.md` (including all flags for budget, coverage, etc.).
     - Logic: Call `sync_main`.
     - Decorator: Apply `@track_cost` decorator.
     - Return: `Optional[Tuple[str, float, str]]` (result, cost, model_name) or `None` on error.
     - Deprecated flag: `--log` (hidden) should emit a yellow warning to stderr and set `dry_run = True`.
  2. **auto-deps**:
     - Interface: Match the `auto-deps` command specification in `README.md`.
     - Logic: Strip quotes from `directory_path` if present (handles incorrectly quoted paths). Call `auto_deps_main`.
     - Decorator: Apply `@track_cost` decorator.
     - Return: `Optional[Tuple[str, float, str]]` (result, cost, model_name) or `None` on error.
  3. **setup**:
     - Interface: Match the `setup` command specification in `README.md`.
     - Logic: Import the `cli` module inside the function (as `cli_module`) to allow mocking/patching. Call `cli_module.install_completion(quiet=quiet)`, then call `_run_setup_utility` from `core.utils`.
     - Decorator: No `@track_cost` (no LLM calls).
     - Return: None (no return value).

% Dependencies
  <sync_main_example>
  <include>context/sync_main_example.py</include>
  </sync_main_example>
  <auto_deps_main_example>
  <include>context/auto_deps_main_example.py</include>
  </auto_deps_main_example>
  <track_cost_example>
  <include>context/track_cost_example.py</include>
  </track_cost_example>

% Instructions
  - Error handling pattern:
    - Wrap command logic in try/except.
    - Re-raise `click.Abort` exceptions without handling.
    - For other exceptions: call `handle_error(exception, "command-name", ctx.obj.get("quiet", False))`.
    - Return `None` after handling errors.
  - Access quiet flag via `ctx.obj.get("quiet", False)` (ctx.obj may be None for setup).
  - For deprecated flags: emit a warning to stderr indicating the flag is deprecated and what to use instead.

% Deliverables
  - Code: `pdd/commands/maintenance.py`
