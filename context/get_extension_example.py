# Here's a concise example of how to use the `get_extension` function from the provided module. 

# ### Example Usage

# ```python
# Import the get_extension function from the module
from get_extension import get_extension

# Example calls to the function
print(get_extension("Python"))        # Output: .py
print(get_extension("Makefile"))      # Output: Makefile
print(get_extension("JavaScript"))    # Output: .js
# ```

# ### Explanation
# 1. **Importing**: The function is imported from the specified module path.
# 2. **Function Calls**: The function is called with different programming language names, demonstrating its case insensitivity and handling of unknown languages. 

# Make sure the module is correctly placed in the specified directory for the import to work.