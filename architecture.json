[
  {
    "reason": "Provides shared agentic CLI execution across Claude, Gemini/Antigravity, Codex, and OpenCode, with mid-run steering and state persistence.",
    "description": "Common functions for running agentic tasks: provider preference, availability detection, subprocess orchestration, JSON output parsing, cost tracking, GitHub state persistence, progress tracking, and OpenCode model resolution.",
    "dependencies": [
      "llm_invoke_python.prompt",
      "auth_service_python.prompt",
      "_keyring_timeout_python.prompt",
      "git_porcelain_python.prompt",
      "routing_policy_python.prompt"
    ],
    "priority": 5,
    "filename": "agentic_common_python.prompt",
    "filepath": "pdd/agentic_common.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "dataclasses": [
          {
            "name": "SteerEntry",
            "fields": [
              {
                "name": "comment_id",
                "type": "str"
              },
              {
                "name": "author",
                "type": "str"
              },
              {
                "name": "body",
                "type": "str"
              }
            ]
          }
        ],
        "functions": [
          {
            "name": "get_agent_provider_preference",
            "signature": "() -> List[str]",
            "returns": "List[str]"
          },
          {
            "name": "get_available_agents",
            "signature": "() -> List[str]",
            "returns": "List[str]"
          },
          {
            "name": "get_agentic_capabilities",
            "signature": "() -> Dict[str, Any]",
            "returns": "Dict[str, Any]"
          },
          {
            "name": "validate_claude_policy",
            "signature": "(policy: Any, *, interactive: bool = False) -> ClaudePolicy",
            "returns": "ClaudePolicy"
          },
          {
            "name": "build_agentic_task_instruction",
            "signature": "(instruction: str, *, user_feedback: Optional[str] = None, steers: Optional[List[SteerEntry]] = None) -> str",
            "returns": "str"
          },
          {
            "name": "run_agentic_task",
            "signature": "(instruction, cwd, *, verbose=False, quiet=False, label=\"\", timeout=None, stall_timeout=None, max_retries=1, retry_delay=DEFAULT_RETRY_DELAY, deadline=None, use_playwright=False, reasoning_time=None, steers=None, claude_policy=None, routing_policy=None, task_class=None, before_attempt=None, single_provider_attempt=False) -> AgenticTaskResult",
            "returns": "AgenticTaskResult"
          },
          {
            "name": "select_harness_for_task",
            "signature": "(task_class: str, candidates: List[str]) -> List[str]",
            "returns": "List[str]"
          },
          {
            "name": "detect_control_token",
            "signature": "(output, token, tail_lines=_SEMANTIC_TAIL_LINES) -> Optional[TokenMatch]",
            "returns": "Optional[TokenMatch]"
          },
          {
            "name": "classify_step_output",
            "signature": "(output, expected_tokens, model=\"gemini/gemini-3-flash\") -> Optional[TokenMatch]",
            "returns": "Optional[TokenMatch]"
          },
          {
            "name": "substitute_template_variables",
            "signature": "(template, context, *, strict_unresolved: bool = False) -> str",
            "returns": "str"
          },
          {
            "name": "set_agentic_progress",
            "signature": "(workflow, current_step, total_steps, step_name, completed_steps) -> None",
            "returns": "None"
          },
          {
            "name": "clear_agentic_progress",
            "signature": "() -> None",
            "returns": "None"
          },
          {
            "name": "get_and_clear_agentic_interrupt_context",
            "signature": "() -> Optional[Dict]",
            "returns": "Optional[Dict]"
          },
          {
            "name": "get_job_deadline",
            "signature": "() -> Optional[float]",
            "returns": "Optional[float]"
          },
          {
            "name": "post_step_comment",
            "signature": "(repo_owner, repo_name, issue_number, step_num, total_steps, description, output, cwd, body=None, failure_mode: Optional[str] = None, failure_detail: Optional[str] = None) -> bool",
            "returns": "bool"
          },
          {
            "name": "_extract_step_report",
            "signature": "(text: Optional[str]) -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "extract_step_report",
            "signature": "(text: Optional[str]) -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "normalize_step_comments_state",
            "signature": "(raw: Any) -> Set[int]",
            "returns": "Set[int]"
          },
          {
            "name": "post_step_comment_once",
            "signature": "(*, repo_owner: str, repo_name: str, issue_number: int, step_num: int, body: str, posted_steps: Set[int], cwd: Path) -> bool",
            "returns": "bool"
          },
          {
            "name": "_sanitize_comment_body",
            "signature": "(body: Optional[str], max_chars: int = 25_000) -> str",
            "returns": "str"
          },
          {
            "name": "post_pr_comment",
            "signature": "(repo_owner, repo_name, pr_number, body, cwd) -> bool",
            "returns": "bool"
          },
          {
            "name": "post_final_comment",
            "signature": "(repo_owner, repo_name, issue_number, reason, total_cost, steps_completed, total_steps, cwd) -> bool",
            "returns": "bool"
          },
          {
            "name": "validate_cached_state",
            "signature": "(last_completed_step, step_outputs, step_order=None, quiet=False) -> Union[int, float]",
            "returns": "Union[int, float]"
          },
          {
            "name": "github_save_state",
            "signature": "(repo_owner: str, repo_name: str, issue_number: int, workflow_type: str, state: Dict, cwd: Path, comment_id: Optional[int] = None, *, dedupe: bool = False) -> Optional[int]",
            "returns": "Optional[int]"
          },
          {
            "name": "github_load_state",
            "signature": "(repo_owner: str, repo_name: str, issue_number: int, workflow_type: str, cwd: Path) -> Tuple[Optional[Dict], Optional[int]]",
            "returns": "Tuple[Optional[Dict], Optional[int]]"
          },
          {
            "name": "github_clear_state",
            "signature": "(repo_owner: str, repo_name: str, issue_number: int, workflow_type: str, cwd: Path) -> bool",
            "returns": "bool"
          },
          {
            "name": "drain_issue_steers",
            "signature": "(repo_owner: str, repo_name: str, issue_number: int, state: Dict, *, cwd: Path) -> List[SteerEntry]",
            "returns": "List[SteerEntry]"
          },
          {
            "name": "load_workflow_state",
            "signature": "(cwd: Path, issue_number: int, workflow_type: str, state_dir: Path, repo_owner: str, repo_name: str, use_github_state: bool = True) -> Tuple[Optional[Dict], Optional[int]]",
            "returns": "Tuple[Optional[Dict], Optional[int]]"
          },
          {
            "name": "save_workflow_state",
            "signature": "(cwd: Path, issue_number: int, workflow_type: str, state: Dict, state_dir: Path, repo_owner: str, repo_name: str, use_github_state: bool = True, github_comment_id: Optional[int] = None, *, dedupe: bool = False) -> Optional[int]",
            "returns": "Optional[int]"
          },
          {
            "name": "clear_workflow_state",
            "signature": "(cwd: Path, issue_number: int, workflow_type: str, state_dir: Path, repo_owner: str, repo_name: str, use_github_state: bool = True) -> bool",
            "returns": "bool"
          }
        ],
        "enums": [
          {
            "name": "EffortCapability",
            "values": [
              "FULL",
              "CLAMPED",
              "CONFIG_ONLY",
              "INTERACTIVE_ONLY",
              "UNSUPPORTED"
            ]
          }
        ],
        "typeAliases": [
          {
            "name": "TaskClass",
            "definition": "Literal['single_file', 'multi_file', 'repo_scale', 'high_isolation']"
          }
        ]
      }
    },
    "position": {
      "x": 14400,
      "y": 800
    }
  },
  {
    "reason": "Static task-class routing table and bounded escalation ladder for agentic CLI config selection.",
    "description": "Provides a static YAML-configurable lookup table keyed by coarse task class and a bounded four-step escalation ladder. Consumed by run_agentic_task() via optional routing_policy argument. Records policy version, task class, selected config, escalation step, fallback reason, cost, latency, and verifier result.",
    "dependencies": [],
    "priority": 5,
    "filename": "routing_policy_python.prompt",
    "filepath": "pdd/routing_policy.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "dataclasses": [
          {
            "name": "RoutingConfig",
            "fields": [
              {
                "name": "harness",
                "type": "str"
              },
              {
                "name": "model_tier",
                "type": "int"
              },
              {
                "name": "thinking_effort",
                "type": "EffortLevel"
              },
              {
                "name": "repeat_runs",
                "type": "int"
              }
            ]
          },
          {
            "name": "EscalationStep",
            "fields": [
              {
                "name": "axis",
                "type": "str"
              },
              {
                "name": "value",
                "type": "Any"
              },
              {
                "name": "reason",
                "type": "str"
              }
            ]
          },
          {
            "name": "RoutingPolicyRow",
            "fields": [
              {
                "name": "initial_config",
                "type": "Optional[RoutingConfig]"
              },
              {
                "name": "escalation_steps",
                "type": "List[EscalationStep]"
              }
            ]
          },
          {
            "name": "RoutingPolicy",
            "fields": [
              {
                "name": "version",
                "type": "str"
              },
              {
                "name": "rows",
                "type": "Dict[str, RoutingPolicyRow]"
              },
              {
                "name": "path",
                "type": "Optional[Path]"
              }
            ]
          },
          {
            "name": "RoutingRecord",
            "fields": [
              {
                "name": "timestamp_utc",
                "type": "str"
              },
              {
                "name": "job_id",
                "type": "Optional[str]"
              },
              {
                "name": "policy_version",
                "type": "str"
              },
              {
                "name": "task_class",
                "type": "str"
              },
              {
                "name": "selected_config",
                "type": "Optional[RoutingConfig]"
              },
              {
                "name": "escalation_step",
                "type": "int"
              },
              {
                "name": "fallback_reason",
                "type": "Optional[str]"
              },
              {
                "name": "cost_usd",
                "type": "float"
              },
              {
                "name": "latency_seconds",
                "type": "float"
              },
              {
                "name": "verifier_result",
                "type": "Optional[str]"
              }
            ]
          }
        ],
        "functions": [
          {
            "name": "default_policy",
            "signature": "() -> RoutingPolicy",
            "returns": "RoutingPolicy"
          },
          {
            "name": "load_policy",
            "signature": "(path: Optional[Path]) -> RoutingPolicy",
            "returns": "RoutingPolicy"
          },
          {
            "name": "select_config",
            "signature": "(policy: RoutingPolicy, task_class: Optional[str], budget_remaining: Optional[float], deadline: Optional[float]) -> tuple[RoutingConfig | None, RoutingRecord]",
            "returns": "tuple[RoutingConfig | None, RoutingRecord]"
          },
          {
            "name": "escalate",
            "signature": "(policy: RoutingPolicy, record: RoutingRecord, verifier_result: str, budget_remaining: Optional[float], deadline: Optional[float]) -> tuple[RoutingConfig | None, RoutingRecord]",
            "returns": "tuple[RoutingConfig | None, RoutingRecord]"
          },
          {
            "name": "resolve_model_for_tier",
            "signature": "(tier: int) -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "emit_routing_record",
            "signature": "(record: RoutingRecord, log_dir: Path) -> None",
            "returns": "None"
          }
        ]
      }
    },
    "position": {
      "x": 14400,
      "y": 1200
    }
  },
  {
    "reason": "Detects programming language from prompt or filename.",
    "description": "Identifies the target language for code generation. Uses filename patterns and prompt content analysis.",
    "dependencies": [],
    "priority": 6,
    "filename": "get_language_python.prompt",
    "filepath": "pdd/get_language.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_language",
            "signature": "(extension: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 9560,
      "y": 620
    }
  },
  {
    "reason": "Provides path resolution utilities for includes and templates.",
    "description": "Resolves relative paths for include directives, locates prompt templates, finds data files, and discovers project roots.",
    "dependencies": [],
    "priority": 7,
    "filename": "path_resolution_python.prompt",
    "filepath": "pdd/path_resolution.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_default_resolver",
            "signature": "() -> PathResolver",
            "returns": "PathResolver",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "resolve_include",
            "signature": "(self, rel: str, profile: IncludeProfile = 'cwd_then_package_then_repo') -> Path",
            "returns": "Path",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "resolve_prompt_template",
            "signature": "(self, name: str, profile: PromptProfile = 'pdd_path_then_repo_then_cwd') -> Optional[Path]",
            "returns": "Optional[Path]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "resolve_data_file",
            "signature": "(self, rel: str, profile: DataProfile = 'pdd_path_only') -> Path",
            "returns": "Path",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "resolve_project_root",
            "signature": "(self, profile: ProjectRootProfile = 'pdd_path_then_marker_then_cwd', max_levels: int = 5) -> Path",
            "returns": "Path",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10800,
      "y": 0
    }
  },
  {
    "reason": "Extracts clean code from raw LLM responses.",
    "description": "Parses LLM output to extract code blocks, removes markdown fencing, validates syntax, and handles multi-block outputs. Uses LLM for complex extraction when needed.",
    "dependencies": [],
    "priority": 8,
    "filename": "postprocess_python.prompt",
    "filepath": "pdd/postprocess.py",
    "tags": [
      "module",
      "processing",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "postprocess_0",
            "signature": "(text: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "postprocess",
            "signature": "(llm_output: str, language: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0, time: float = DEFAULT_TIME, verbose: bool = False) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 8080,
      "y": 800
    }
  },
  {
    "reason": "Detects whether LLM output is complete or needs continuation.",
    "description": "Analyzes LLM responses to determine if code is complete. Checks for truncation markers, unclosed blocks, and incomplete functions.",
    "dependencies": [],
    "priority": 9,
    "filename": "unfinished_prompt_python.prompt",
    "filepath": "pdd/unfinished_prompt.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "unfinished_prompt",
            "signature": "(prompt_text: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0, time: float = DEFAULT_TIME, language: Optional[str] = None, verbose: bool = False) -> Tuple[str, bool, float, str]",
            "returns": "Tuple[str, bool, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 9000,
      "y": 800
    }
  },
  {
    "reason": "Generates and verifies language-specific tests.",
    "description": "Creates tests appropriate for the target language. Handles language-specific testing patterns.",
    "dependencies": [],
    "priority": 10,
    "filename": "agentic_langtest_python.prompt",
    "filepath": "pdd/agentic_langtest.py",
    "tags": [
      "agentic",
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "default_verify_cmd_for",
            "signature": "(lang: str, unit_test_file: str) -> str | None",
            "returns": "str | None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "missing_tool_hints",
            "signature": "(lang: str, verify_cmd: str | None, project_root: Path) -> str | None",
            "returns": "str | None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 7200,
      "y": 0
    }
  },
  {
    "reason": "Continues incomplete LLM outputs that were truncated.",
    "description": "Detects when LLM output is incomplete and continues generation from where it stopped. Handles token limit issues gracefully.",
    "dependencies": [],
    "priority": 11,
    "filename": "continue_generation_python.prompt",
    "filepath": "pdd/continue_generation.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "continue_generation",
            "signature": "(formatted_input_prompt: str, llm_output: str, strength: float, temperature: float, time: float = DEFAULT_TIME, language: Optional[str] = None, verbose: bool = False) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 7800,
      "y": 1200
    }
  },
  {
    "reason": "Handles authentication for PDD cloud services.",
    "description": "Obtains and caches JWT tokens for cloud API access. Supports Firebase authentication and environment-aware JWT cache validation so staging/prod cached tokens are not reused against the wrong PDD_CLOUD_URL target.",
    "dependencies": [
      "_keyring_timeout_python.prompt"
    ],
    "priority": 12,
    "filename": "get_jwt_token_python.prompt",
    "filepath": "pdd/get_jwt_token.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "__init__",
            "signature": "(self, firebase_api_key: str, app_name: str, client_id: str)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, firebase_api_key: str, app_name: str)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "verify_firebase_token",
            "signature": "def verify_firebase_token(id_token: str) -> dict",
            "returns": "dict with uid and claims",
            "errors": [
              "AuthError"
            ]
          },
          {
            "name": "verify_github_oauth",
            "signature": "async def verify_github_oauth(code: str) -> GitHubProfile",
            "returns": "GitHubProfile",
            "errors": [
              "AuthError"
            ]
          },
          {
            "name": "create_signed_jwt",
            "signature": "def create_signed_jwt(payload: dict, expiry_hours: int = 72) -> str",
            "returns": "str (JWT token)"
          },
          {
            "name": "verify_signed_jwt",
            "signature": "def verify_signed_jwt(token: str) -> dict",
            "returns": "dict (decoded payload)",
            "errors": [
              "AuthError"
            ]
          },
          {
            "name": "extract_bearer_token",
            "signature": "def extract_bearer_token(request) -> str",
            "returns": "str (token)",
            "errors": [
              "AuthError"
            ]
          }
        ]
      }
    }
  },
  {
    "reason": "Daily Rising Stars resurface check — Cloud Scheduler function scanning contribution thresholds to identify high-potential nurtured candidates and detect churned candidates",
    "description": "Cloud Function entry point recruiting_resurface_check triggered by Cloud Scheduler (daily). Scans all recruiting/candidates with crmFunnelStage in ('nurture', 'contributing'). For each candidate, evaluates four resurface criteria from config thresholds: (1) merged_prs >= threshold (e.g., 3 merged PRs), (2) hackathon_score >= threshold (e.g., top 25%), (3) platform_usage_days >= threshold (power user), (4) marketplace_examples >= threshold (marketplace contributor). Candidates meeting any criterion are marked resurfaced=true with appropriate ResurfaceReason, CrmFunnelStage updated to 'resurfaced', and CRM GitHub Issue label updated from recruiting-nurture to recruiting-resurfaced. Also detects churned candidates: if last_activity_at > 60 days ago and no contributions, sets crmFunnelStage='churned' and updates CRM label to recruiting-churned. Verifies merged PR counts via GitHub API to prevent gaming. Logs all resurface and churn transitions. Returns summary counts.",
    "dependencies": [],
    "priority": 18,
    "filename": "resurface_check_Python.prompt",
    "filepath": "prompts/resurface_check_Python.prompt",
    "tags": [
      "backend",
      "api",
      "scheduling",
      "nurture"
    ],
    "context_urls": [
      {
        "url": "https://cloud.google.com/scheduler/docs/creating",
        "purpose": "Cloud Scheduler daily job configuration"
      },
      {
        "url": "https://firebase.google.com/docs/firestore/query-data/queries",
        "purpose": "Firestore compound queries for contribution threshold scanning"
      },
      {
        "url": "https://docs.github.com/en/rest/pulls/pulls#list-pull-requests",
        "purpose": "GitHub API for merged PR count verification"
      },
      {
        "url": "https://cloud.google.com/functions/docs/writing/write-http-functions",
        "purpose": "Cloud Functions HTTP entry point for scheduler triggers"
      }
    ],
    "interface": {
      "type": "api",
      "api": {
        "endpoints": [
          {
            "method": "POST",
            "path": "/recruiting_resurface_check",
            "auth": "cloud-scheduler",
            "responseSchema": "{ scanned: int, resurfaced: int, churned: int, reasons: dict }"
          }
        ]
      }
    }
  },
  {
    "reason": "Enhances test plan with contract/schema validation and accessibility test cases.",
    "description": "Step 5b of the 18-step agentic test workflow. Enhances the test plan with OpenAPI contract validation and axe-core accessibility test cases.",
    "dependencies": [],
    "priority": 210,
    "filename": "agentic_test_step5b_enhance_plan_LLM.prompt",
    "filepath": "prompts/agentic_test_step5b_enhance_plan_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19440,
      "y": 0
    }
  },
  {
    "reason": "Assesses automated test coverage gaps requiring manual/exploratory testing.",
    "description": "Step 6 of the 18-step agentic test workflow. Compares requirements against test plan to identify gaps needing manual browser testing.",
    "dependencies": [],
    "priority": 211,
    "filename": "agentic_test_step6_coverage_LLM.prompt",
    "filepath": "prompts/agentic_test_step6_coverage_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19720,
      "y": 220
    }
  },
  {
    "reason": "Creates manual testing checklist for browser-based exploratory testing via playwright-cli.",
    "description": "Step 7 of the 18-step agentic test workflow. Creates checklist with page-by-page, user-story, and accessibility spot-check strategies.",
    "dependencies": [],
    "priority": 212,
    "filename": "agentic_test_step7_checklist_LLM.prompt",
    "filepath": "prompts/agentic_test_step7_checklist_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 20000,
      "y": 0
    }
  },
  {
    "reason": "Executes manual/exploratory browser tests against a web application using playwright-cli.",
    "description": "Step 8 of the 18-step agentic test workflow. Executes checklist items via playwright-cli commands and reports PASS/FAIL with evidence.",
    "dependencies": [],
    "priority": 213,
    "filename": "agentic_test_step8_manual_test_LLM.prompt",
    "filepath": "prompts/agentic_test_step8_manual_test_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 20280,
      "y": 220
    }
  },
  {
    "reason": "Creates automated regression tests that reproduce bugs found during manual testing.",
    "description": "Step 9 of the 18-step agentic test workflow. Generates Playwright/pytest tests that reproduce bugs found in Step 8.",
    "dependencies": [],
    "priority": 214,
    "filename": "agentic_test_step9_regression_LLM.prompt",
    "filepath": "prompts/agentic_test_step9_regression_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 20560,
      "y": 0
    }
  },
  {
    "reason": "Validates regression tests correctly detect bugs by confirming they fail against current code.",
    "description": "Step 10 of the 18-step agentic test workflow. Runs regression tests and confirms they fail as expected.",
    "dependencies": [],
    "priority": 215,
    "filename": "agentic_test_step10_validate_LLM.prompt",
    "filepath": "prompts/agentic_test_step10_validate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 20840,
      "y": 220
    }
  },
  {
    "reason": "Checks manual testing checklist completion and determines if another loop iteration is needed.",
    "description": "Step 11 of the 18-step agentic test workflow. Compares checklist against results and triggers loop back to Step 8 if items remain.",
    "dependencies": [],
    "priority": 216,
    "filename": "agentic_test_step11_loop_LLM.prompt",
    "filepath": "prompts/agentic_test_step11_loop_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 21120,
      "y": 0
    }
  },
  {
    "reason": "Validates test plan traceability by cross-referencing planned cases against generated tests.",
    "description": "Step 15 of the 18-step agentic test workflow. Cross-references enhanced test plan against generated test files and generates missing tests.",
    "dependencies": [],
    "priority": 217,
    "filename": "agentic_test_step15_plan_validation_LLM.prompt",
    "filepath": "prompts/agentic_test_step15_plan_validation_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 21400,
      "y": 220
    }
  },
  {
    "reason": "Orchestrates agentic bug fixing workflow.",
    "description": "Main entry point for the agentic bug fixing pipeline.",
    "dependencies": [],
    "priority": 20,
    "filename": "agentic_bug_python.prompt",
    "filepath": "pdd/agentic_bug.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_bug",
            "signature": "(issue_url: str, *, verbose: bool = False, quiet: bool = False, timeout_adder: float = 0.0, use_github_state: bool = True, reasoning_time: Optional[float] = None, clean_restart: bool = False, manual_args: Optional[Tuple[str, str, str, str, str]] = None) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple[bool, str, float, str, List[str]]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 13800,
      "y": 1600
    }
  },
  {
    "reason": "Repairs verification issues by identifying prompt-intent mismatches and proposing code fixes.",
    "description": "Corrects code that fails verification checks. Addresses logic errors and specification mismatches.",
    "dependencies": [],
    "priority": 20,
    "filename": "fix_verification_errors_python.prompt",
    "filepath": "pdd/fix_verification_errors.py",
    "tags": [
      "fix",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "fix_verification_errors",
            "signature": "(program: str, prompt: str, code: str, output: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, verbose: bool = False, time: float = DEFAULT_TIME, compressed_context: Optional[Mapping[str, Any]] = None) -> Dict[str, Any]",
            "returns": "Dict[str, Any]"
          }
        ]
      }
    },
    "position": {
      "x": 10760,
      "y": 1020
    }
  },
  {
    "reason": "Updates prompts to reflect code modifications.",
    "description": "Back-propagates code changes into the source prompt. Keeps prompts synchronized with manually edited code.",
    "dependencies": [],
    "priority": 21,
    "filename": "update_prompt_python.prompt",
    "filepath": "pdd/update_prompt.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "update_prompt",
            "signature": "(input_prompt: str, input_code: str, modified_code: str, strength: float, temperature: float, verbose: bool = False, time: float = DEFAULT_TIME) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 11400,
      "y": 1240
    }
  },
  {
    "reason": "Fixes code based on failing unit test output.",
    "description": "Analyzes test failures and generates code or test corrections. Determines whether to fix the code or the test based on error patterns.",
    "dependencies": [],
    "priority": 22,
    "filename": "fix_errors_from_unit_tests_python.prompt",
    "filepath": "pdd/fix_errors_from_unit_tests.py",
    "tags": [
      "fix",
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "validate_inputs",
            "signature": "(strength: float, temperature: float) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "write_to_error_file",
            "signature": "(file_path: str, content: str) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "fix_errors_from_unit_tests",
            "signature": "(unit_test: str, code: str, prompt: str, error: str, error_file: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, verbose: bool = False, time: float = DEFAULT_TIME) -> Tuple[bool, bool, str, str, str, float, str]",
            "returns": "Tuple[bool, bool, str, str, str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 7480,
      "y": 800
    }
  },
  {
    "reason": "Generates example code demonstrating module usage.",
    "description": "Creates minimal runnable examples that show how to use generated modules. Examples serve as interface documentation and grounding context.",
    "dependencies": [],
    "priority": 23,
    "filename": "context_generator_python.prompt",
    "filepath": "pdd/context_generator.py",
    "tags": [
      "generation",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "context_generator",
            "signature": "(code_module: str, prompt: str, language: str = 'python', strength: float = 0.5, temperature: float = 0, time: Optional[float] = DEFAULT_TIME, verbose: bool = False, source_file_path: str = None, example_file_path: str = None, module_name: str = None) -> tuple",
            "returns": "tuple",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 9600,
      "y": 1600
    }
  },
  {
    "reason": "Identifies which prompts need updates based on changes.",
    "description": "Analyzes change descriptions to determine affected prompts. Supports batch change detection.",
    "dependencies": [],
    "priority": 24,
    "filename": "detect_change_python.prompt",
    "filepath": "pdd/detect_change.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "detect_change",
            "signature": "(prompt_files: List[str], change_description: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: Optional[float] = DEFAULT_TIME, verbose: bool = False) -> Tuple[List[Dict[str, str]], float, str]",
            "returns": "Tuple[List[Dict[str, str]], float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 9880,
      "y": 1820
    }
  },
  {
    "reason": "LLM prompt template for agentic crash explore operations.",
    "description": "Defines the prompt structure for agentic crash explore. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 25,
    "filename": "agentic_crash_explore_LLM.prompt",
    "filepath": "prompts/agentic_crash_explore_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 7480,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for agentic fix explore operations.",
    "description": "Defines the prompt structure for agentic fix explore. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 26,
    "filename": "agentic_fix_explore_LLM.prompt",
    "filepath": "prompts/agentic_fix_explore_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 7760,
      "y": 440
    }
  },
  {
    "reason": "Autonomously updates prompts based on code changes.",
    "description": "Analyzes code modifications and generates corresponding prompt updates. Ensures prompt-code synchronization.",
    "dependencies": [],
    "priority": 27,
    "filename": "agentic_update_LLM.prompt",
    "filepath": "prompts/agentic_update_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 8080,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for agentic verify explore operations.",
    "description": "Defines the prompt structure for agentic verify explore. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 28,
    "filename": "agentic_verify_explore_LLM.prompt",
    "filepath": "prompts/agentic_verify_explore_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 4760,
      "y": 220
    }
  },
  {
    "reason": "Main CLI entry point and command dispatcher.",
    "description": "Defines all pdd commands using Click. Routes to appropriate subcommands and handles global options.",
    "dependencies": [],
    "priority": 29,
    "filename": "cli_python.prompt",
    "filepath": "pdd/cli.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module"
    },
    "position": {
      "x": 8400,
      "y": 660
    }
  },
  {
    "reason": "Determines output file paths for generated code.",
    "description": "Calculates where generated files should be written based on project structure and conventions.",
    "dependencies": [
      "auto_deps_main_python.prompt"
    ],
    "priority": 30,
    "filename": "generate_output_paths_python.prompt",
    "filepath": "pdd/generate_output_paths.py",
    "tags": [
      "generation",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "generate_output_paths",
            "signature": "(command: str, output_locations: Dict[str, Optional[str]], basename: str, language: str, file_extension: str, context_config: Optional[Dict[str, str]] = None, input_file_dir: Optional[str] = None, input_file_dirs: Optional[Dict[str, str]] = None, config_base_dir: Optional[str] = None, path_resolution_mode: PathResolutionMode = 'config_base') -> Dict[str, str]",
            "returns": "Dict[str, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 12880,
      "y": 1100
    }
  },
  {
    "reason": "Displays animated PDD logo in terminal.",
    "description": "Renders animated ASCII art logo. Used for CLI branding.",
    "dependencies": [],
    "priority": 31,
    "filename": "logo_animation_python.prompt",
    "filepath": "pdd/logo_animation.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "start_logo_animation",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "stop_logo_animation",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "run_logo_animation_inline",
            "signature": "(console: Console, stop_event: threading.Event)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "update_progress",
            "signature": "(self, progress: float)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "set_new_transition",
            "signature": "(self, new_target_x: float, new_target_y: float)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 14960,
      "y": 0
    }
  },
  {
    "reason": "Centralizes concise user-facing CLI branding strings for orientation surfaces.",
    "description": "Defines shared tagline and positioning constants used by root help, setup onboarding, and connect startup output.",
    "dependencies": [],
    "priority": 31,
    "filename": "cli_branding_python.prompt",
    "filepath": "pdd/cli_branding.py",
    "tags": [
      "module",
      "python",
      "cli",
      "branding"
    ],
    "interface": {
      "type": "module",
      "module": {
        "constants": [
          {
            "name": "PDD_TAGLINE",
            "type": "str"
          },
          {
            "name": "PDD_FULL_TAGLINE",
            "type": "str"
          },
          {
            "name": "PDD_POSITIONING",
            "type": "str"
          }
        ]
      }
    },
    "position": {
      "x": 14960,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for sync analysis operations.",
    "description": "Defines the prompt structure for sync analysis. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 32,
    "filename": "sync_analysis_LLM.prompt",
    "filepath": "prompts/sync_analysis_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "sync",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 14400,
      "y": 0
    }
  },
  {
    "reason": "Click decorator that tracks per-command LLM cost, model, and attempted-models history to a CSV file.",
    "description": "Records token usage and calculates costs. Aggregates costs across workflow steps. Captures chronological attempted-models history (including failed cloud attempts that fell back to local) via ctx.obj['attempted_models'].",
    "dependencies": [
      "llm_invoke_python.prompt"
    ],
    "priority": 33,
    "filename": "track_cost_python.prompt",
    "filepath": "pdd/track_cost.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "track_cost",
            "signature": "(func)",
            "returns": "Callable"
          },
          {
            "name": "extract_cost_and_model",
            "signature": "(result: Any) -> Tuple[Any, str]",
            "returns": "Tuple[Any, str]"
          },
          {
            "name": "collect_files",
            "signature": "(args, kwargs)",
            "returns": "Tuple[List[str], List[str]]"
          },
          {
            "name": "looks_like_file",
            "signature": "(path_str)",
            "returns": "bool"
          }
        ]
      }
    },
    "position": {
      "x": 10480,
      "y": 2420
    }
  },
  {
    "reason": "Processes batch changes from CSV files.",
    "description": "Reads change specifications from CSV and applies them to multiple prompts. Supports bulk updates.",
    "dependencies": [],
    "priority": 34,
    "filename": "process_csv_change_python.prompt",
    "filepath": "pdd/process_csv_change.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "resolve_prompt_path",
            "signature": "(prompt_name: str, csv_file: str, code_directory: str) -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "derive_code_file_stem",
            "signature": "(prompt_filename: str, default_language: str) -> Tuple[str, str, bool]",
            "returns": "Tuple[str, str, bool]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "resolve_code_path",
            "signature": "(prompt_name: str, resolved_prompt_path: str, csv_file: str, code_directory: str, input_code_filename: str) -> str",
            "returns": "str",
            "sideEffects": [
              "Reads filesystem metadata for candidate code paths"
            ]
          },
          {
            "name": "process_csv_change",
            "signature": "(csv_file: str, strength: float, temperature: float, code_directory: str, language: str, extension: str, budget: float, time: float = DEFAULT_TIME) -> Tuple[bool, List[Dict[str, str]], float, Optional[str]]",
            "returns": "Tuple[bool, List[Dict[str, str]], float, Optional[str]]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "mock_change",
            "signature": "(input_prompt, input_code, change_prompt, strength, temperature)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "mock_get_extension",
            "signature": "(language_name)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 11080,
      "y": 1240
    }
  },
  {
    "reason": "CLI entry point for auto-deps with doc discovery, dedup, and parallelization support.",
    "description": "Command-line interface for auto deps. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [
      "construct_paths_python.prompt",
      "insert_includes_python.prompt",
      "validate_prompt_includes_python.prompt",
      "operation_log_python.prompt",
      "auto_deps_architecture_python.prompt"
    ],
    "priority": 35,
    "filename": "auto_deps_main_python.prompt",
    "filepath": "pdd/auto_deps_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "auto_deps_main",
            "signature": "(ctx: click.Context, prompt_file: str, directory_path: str, auto_deps_csv_path: Optional[str], output: Optional[str], force_scan: Optional[bool] = False, progress_callback: Optional[Callable[[int, int], None]] = None, include_docs: bool = False, no_dedup: bool = False, concurrency: int = 1, _skip_finalization: bool = False)",
            "returns": "Tuple[str, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 12600,
      "y": 2000
    }
  },
  {
    "reason": "CLI entry point for the conflicts command.",
    "description": "Command-line interface for conflicts. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 36,
    "filename": "conflicts_main_python.prompt",
    "filepath": "pdd/conflicts_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "conflicts_main",
            "signature": "(ctx: click.Context, prompt1: str, prompt2: str, output: Optional[str], verbose: bool = False, language: Optional[str] = None) -> Tuple[List[Dict], float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 9600,
      "y": 2080
    }
  },
  {
    "reason": "CLI entry point for the context generator command.",
    "description": "Command-line interface for context generator. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 37,
    "filename": "context_generator_main_python.prompt",
    "filepath": "pdd/context_generator_main.py",
    "tags": [
      "cli",
      "generation",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "context_generator_main",
            "signature": "(ctx: click.Context, prompt_file: str, code_file: str, output: Optional[str]) -> Tuple[str, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 12000,
      "y": 2220
    }
  },
  {
    "reason": "Resolves test runner command and working directory for a given test file across monorepos.",
    "description": "Provides the command to run tests in a given language with cwd detection for monorepos. Defaults to pytest for Python.",
    "dependencies": [],
    "priority": 38,
    "filename": "get_test_command_python.prompt",
    "filepath": "pdd/get_test_command.py",
    "tags": [
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_test_command_for_file",
            "signature": "(test_file: str, language: Optional[str] = None)",
            "returns": "Optional[TestCommand]"
          },
          {
            "name": "_detect_ts_test_runner",
            "signature": "(test_path: Path)",
            "returns": "Optional[Tuple[str, Path]]"
          }
        ]
      }
    },
    "position": {
      "x": 6000,
      "y": 800
    }
  },
  {
    "reason": "Recursively discovers and summarizes files in a directory using LLM with caching.",
    "description": "Generates overview of files in a directory. Used for context in LLM prompts.",
    "dependencies": [],
    "priority": 39,
    "filename": "summarize_directory_python.prompt",
    "filepath": "pdd/summarize_directory.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "summarize_directory",
            "signature": "(directory_path: str, strength: float, temperature: float, time: float = DEFAULT_TIME, verbose: bool = False, csv_file: Optional[str] = None, progress_callback: Optional[Callable[[int, int], None]] = None, include_docs: bool = False, max_workers: int = 1, csv_path: Optional[str] = None)",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 11360,
      "y": 1020
    }
  },
  {
    "reason": "CLI entry point for the trace command.",
    "description": "Command-line interface for trace. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 40,
    "filename": "trace_main_python.prompt",
    "filepath": "pdd/trace_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "trace_main",
            "signature": "(ctx: click.Context, prompt_file: str, code_file: str, code_line: int, output: Optional[str]) -> Tuple[str, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 10200,
      "y": 2740
    }
  },
  {
    "reason": "Adds XML structure tags to prompt content.",
    "description": "Wraps content sections in XML tags for better LLM parsing. Improves prompt structure clarity.",
    "dependencies": [],
    "priority": 41,
    "filename": "xml_tagger_python.prompt",
    "filepath": "pdd/xml_tagger.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "xml_tagger",
            "signature": "(raw_prompt: str, strength: float, temperature: float, verbose: bool = False, time: float = DEFAULT_TIME) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "main",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 11680,
      "y": 1020
    }
  },
  {
    "reason": "Orchestrates the 12-step agentic bug workflow with E2E gate, persistent state, and orchestrator-owned GitHub step comments.",
    "description": "Coordinates duplicate checking, documentation review, triage, API research, reproduction, root cause analysis, prompt classification, test planning, test generation, verification, E2E testing (gated by E2E_NEEDED classification), and PR creation. Posts per-step progress comments to the issue and persists workflow state for crash-safe resumes.",
    "dependencies": [],
    "priority": 42,
    "filename": "agentic_bug_orchestrator_python.prompt",
    "filepath": "pdd/agentic_bug_orchestrator.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_bug_orchestrator",
            "signature": "(issue_url: str, issue_content: str, repo_owner: str, repo_name: str, issue_number: int, issue_author: str, issue_title: str, *, cwd: Path, verbose: bool = False, quiet: bool = False, timeout_adder: float = 0.0, use_github_state: bool = True, reasoning_time: Optional[float] = None, clean_restart: bool = False) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple[bool, str, float, str, List[str]]",
            "sideEffects": [
              "Posts GitHub issue comments via post_step_comment",
              "Persists workflow state via save_workflow_state (local + optional GitHub state comment)",
              "Creates/removes git worktrees and branches under .pdd/worktrees/fix-issue-{N}/",
              "Spawns external agentic CLI subprocesses (claude/agy/gemini/codex/opencode)"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 13760,
      "y": 1860
    }
  },
  {
    "reason": "Coordinates agentic prompt updates through Claude, Gemini/Antigravity, Codex, or OpenCode CLIs.",
    "description": "Coordinates repository-aware prompt update runs through external agentic CLIs and reports prompt/test changes.",
    "dependencies": [
      "agentic_update_LLM.prompt",
      "sync_order_python.prompt"
    ],
    "priority": 43,
    "filename": "agentic_update_python.prompt",
    "filepath": "pdd/agentic_update.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_update",
            "signature": "(prompt_file: str, code_file: str, test_files: Optional[List[Path]] = None, *, tests_dir: Optional[Path] = None, verbose: bool = False, quiet: bool = False) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          }
        ]
      }
    },
    "position": {
      "x": 11400,
      "y": 1640
    }
  },
  {
    "reason": "Autonomously verifies code against specifications.",
    "description": "Runs generated code, checks output against expectations, and identifies discrepancies. Supports exploration for context.",
    "dependencies": [],
    "priority": 44,
    "filename": "agentic_verify_python.prompt",
    "filepath": "pdd/agentic_verify.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_verify",
            "signature": "(prompt_file: Path, code_file: Path, program_file: Path, verification_log_file: Path) -> tuple[bool, str, float, str, list[str]]",
            "returns": "tuple[bool, str, float, str, list[str]]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 9880,
      "y": 2740
    }
  },
  {
    "reason": "Automatically selects relevant code and doc includes for prompts using two-stage retrieval.",
    "description": "Analyzes prompt content to determine which context files should be included. Reduces manual dependency management.",
    "dependencies": [],
    "priority": 45,
    "filename": "auto_include_python.prompt",
    "filepath": "pdd/auto_include.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "auto_include",
            "signature": "(input_prompt: str, directory_path: str, csv_file: Optional[str] = None, prompt_filename: Optional[str] = None, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: float = DEFAULT_TIME, verbose: bool = False, progress_callback: Optional[Callable[[int, int], None]] = None, include_docs: bool = False, max_workers: int = 1, csv_path: Optional[str] = None)",
            "returns": "Tuple[str, str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 12000,
      "y": 1200
    }
  },
  {
    "reason": "Modifies prompts based on change instructions.",
    "description": "Applies specified modifications to prompt content. Preserves structure while incorporating requested changes.",
    "dependencies": [],
    "priority": 46,
    "filename": "change_python.prompt",
    "filepath": "pdd/change.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "change",
            "signature": "(input_prompt: str, input_code: str, change_prompt: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: float = DEFAULT_TIME, budget: float = 5.0, verbose: bool = False) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "main",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 11080,
      "y": 800
    }
  },
  {
    "reason": "CLI entry point for the crash command.",
    "description": "Command-line interface for crash. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 47,
    "filename": "crash_main_python.prompt",
    "filepath": "pdd/crash_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "crash_main",
            "signature": "(ctx: click.Context, prompt_file: str, code_file: str, program_file: str, error_file: str, output: Optional[str] = None, output_program: Optional[str] = None, loop: bool = False, max_attempts: Optional[int] = None, budget: Optional[float] = None, agentic_fallback: bool = True, strength: Optional[float] = None, temperature: Optional[float] = None) -> Tuple[bool, str, str, int, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 11400,
      "y": 2000
    }
  },
  {
    "reason": "CLI wrapper for functional verification and verification-fix loops with compressed context.",
    "description": "Command-line interface for fix verification. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 48,
    "filename": "fix_verification_main_python.prompt",
    "filepath": "pdd/fix_verification_main.py",
    "tags": [
      "cli",
      "fix",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "run_program",
            "signature": "(program_path: str, args: List[str] = []) -> Tuple[bool, str, str]",
            "returns": "Tuple[bool, str, str]"
          },
          {
            "name": "fix_verification_main",
            "signature": "(ctx: click.Context, prompt_file: str, code_file: str, program_file: str, output_results: Optional[str], output_code: Optional[str], output_program: Optional[str], loop: bool, verification_program: Optional[str], max_attempts: int = DEFAULT_MAX_ATTEMPTS, budget: float = DEFAULT_BUDGET, agentic_fallback: bool = True, strength: Optional[float] = None, temperature: Optional[float] = None, compressed_context: Optional[Mapping[str, Any]] = None, agentic_fallback_events: Optional[list[dict[str, Any]]] = None) -> Tuple[bool, str, str, int, float, str]",
            "returns": "Tuple[bool, str, str, int, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 11680,
      "y": 2220
    }
  },
  {
    "reason": "Integrates with git for change tracking.",
    "description": "Detects git changes, stages files, and supports commit operations. Tracks prompt modifications.",
    "dependencies": [],
    "priority": 49,
    "filename": "git_update_python.prompt",
    "filepath": "pdd/git_update.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "git_update",
            "signature": "(input_prompt: str, modified_code_file: str, strength: float, temperature: float, verbose: bool = False, time: float = DEFAULT_TIME, simple: bool = False, quiet: bool = False, prompt_file: Optional[str] = None) -> Tuple[Optional[str], float, str]",
            "returns": "Tuple[Optional[str], float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 12600,
      "y": 2260
    }
  },
  {
    "reason": "Expands test coverage for existing test suites.",
    "description": "Analyzes existing tests and generates additional test cases to improve coverage. Focuses on edge cases and error conditions.",
    "dependencies": [],
    "priority": 50,
    "filename": "increase_tests_python.prompt",
    "filepath": "pdd/increase_tests.py",
    "tags": [
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "increase_tests",
            "signature": "(existing_unit_tests: str, coverage_report: str, code: str, prompt_that_generated_code: str, language: str = 'python', strength: float = 0.5, temperature: float = 0.0, time: Optional[float] = DEFAULT_TIME, verbose: bool = False) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10800,
      "y": 2300
    }
  },
  {
    "reason": "CLI wrapper for preprocessing prompts, XML tagging, PDD tag injection, and context snapshots.",
    "description": "Command-line interface for preprocess. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [
      "preprocess_python.prompt",
      "xml_tagger_python.prompt",
      "architecture_sync_python.prompt",
      "context_snapshot_python.prompt"
    ],
    "priority": 51,
    "filename": "preprocess_main_python.prompt",
    "filepath": "pdd/preprocess_main.py",
    "tags": [
      "cli",
      "processing",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "preprocess_main",
            "signature": "(ctx: click.Context, prompt_file: str, output: Optional[str], xml: bool, recursive: bool, double: bool, exclude: list, pdd_tags: bool = False, snapshot: bool = False) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 12280,
      "y": 1640
    }
  },
  {
    "reason": "Splits functionality from one prompt into a new sub-prompt.",
    "description": "Extracts a subset of functionality into a separate prompt file. Creates proper dependency relationships.",
    "dependencies": [],
    "priority": 52,
    "filename": "split_python.prompt",
    "filepath": "pdd/split.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "split",
            "signature": "(input_prompt: str, input_code: str, example_code: str, strength: float = DEFAULT_STRENGTH, temperature: float = DEFAULT_TEMPERATURE, time: Optional[float] = DEFAULT_TIME, verbose: bool = False) -> Tuple[Tuple[str, str], float, str]",
            "returns": "Tuple[Tuple[str, str], float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 11960,
      "y": 800
    }
  },
  {
    "reason": "Maps generated code lines back to source prompt lines.",
    "description": "Traces code locations to their originating prompt sections. Useful for debugging and understanding generation.",
    "dependencies": [],
    "priority": 53,
    "filename": "trace_python.prompt",
    "filepath": "pdd/trace.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "trace",
            "signature": "(code_file: str, code_line: int, prompt_file: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0, verbose: bool = False, time: float = DEFAULT_TIME) -> Tuple[Optional[int], float, str]",
            "returns": "Tuple[Optional[int], float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 12560,
      "y": 1020
    }
  },
  {
    "reason": "Autonomously investigates and fixes runtime crashes.",
    "description": "Multi-step workflow that explores code, identifies crash causes, and generates fixes. Uses LLM for root cause analysis.",
    "dependencies": [],
    "priority": 54,
    "filename": "agentic_crash_python.prompt",
    "filepath": "pdd/agentic_crash.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_crash",
            "signature": "(prompt_file: str | Path, code_file: str | Path, program_file: str | Path, crash_log_file: str | Path) -> tuple[bool, str, float, str | None, list[str]]",
            "returns": "tuple[bool, str, float, str | None, list[str]]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10160,
      "y": 2520
    }
  },
  {
    "reason": "CLI entry point for the bug command.",
    "description": "Command-line interface for bug. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 55,
    "filename": "bug_main_python.prompt",
    "filepath": "pdd/bug_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "bug_main",
            "signature": "(ctx: click.Context, prompt_file: str, code_file: str, program_file: str, current_output: str, desired_output: str, output: Optional[str] = None, language: Optional[str] = 'Python') -> Tuple[str, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 11960,
      "y": 2000
    }
  },
  {
    "reason": "CLI wrapper for generating or enhancing unit tests with optional compressed sync context.",
    "description": "Command-line interface for cmd test. Parses arguments, orchestrates the workflow, and formats output. Before overwriting an existing non-empty test file in either native or agentic generation, applies the shared TestChurnError gate from code_generator_main; high-churn agentic rewrites restore the pre-existing file before raising so broad regression coverage is not discarded.",
    "dependencies": [],
    "priority": 56,
    "filename": "cmd_test_main_python.prompt",
    "filepath": "pdd/cmd_test_main.py",
    "tags": [
      "cli",
      "python",
      "testing"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "cmd_test_main",
            "signature": "(ctx: click.Context, prompt_file: str, code_file: str, output: str | None = None, language: str | None = None, coverage_report: str | None = None, existing_tests: list[str] | None = None, target_coverage: float | None = None, merge: bool = False, strength: float | None = None, temperature: float | None = None, manual: bool = False, *, repair_directive: str | None = None, compressed_context: Optional[Mapping[str, Any]] = None) -> TestResult",
            "returns": "TestResult"
          }
        ]
      }
    },
    "position": {
      "x": 13160,
      "y": 2000
    }
  },
  {
    "reason": "Primary orchestration engine for transitioning .prompt files to source code, handling path resolution, incremental generation, and architectural conformance.",
    "description": "Command-line interface for code generator. Parses arguments, orchestrates the workflow, and formats output. Raises structured ArchitectureConformanceError failures with output/expected/found/missing fields plus failed-attempt total_cost/model_name (constructor accepts an optional repair_directive override for the <pdd-interface> signature check whose source of truth is the prompt, not architecture.json), and injects a non-empty PDD_REPAIR_DIRECTIVE into the generation prompt inside an <architecture_repair_directive> block so sync retry attempts receive concrete missing-export instructions. Architecture conformance now also enforces the prompt's <pdd-interface> signature across module/cli/command interface types in three categories: (1) missing function/method \u2014 declared callables (including dotted methods like ContentSelector.select) absent from the generated code surface as bare names in missing_symbols; (2) missing parameter \u2014 declared parameter names absent from a matching function/method signature surface as dotted funcname.paramname entries; (3) signature drift \u2014 annotation drift is conservative (raises only when both sides specify and differ) while default drift is strict (raises when the prompt declares a default and the generated code drops or changes it, since callers omitting an optional kwarg would otherwise break with TypeError). Each category emits a distinct error sentence so the agentic_sync_runner repair loop can build a targeted directive (add missing function/method, add missing parameter, or update parameter to match the prompt's annotation/default). Issue #1012 adds two additional repair-loop gates: (a) PublicSurfaceRegressionError \u2014 for mature modules (pre-existing non-empty code file), snapshots top-level functions, classes, recursively-walked nested classes/methods (so a method on `Outer.Inner` is captured as `Outer.Inner.method`), and module-level public `ast.Assign` / bound `ast.AnnAssign` (only when `node.value is not None`; bare annotations like `PUBLIC_NAME: int` don't bind a runtime attribute and are ignored) targets plus `ast.Import`/`ast.ImportFrom` aliases (so removing a re-export like `import git` or a public constant like `PUBLIC_SETTING = ...` surfaces as a regression). When the module declares `__all__` as a clean list/tuple of string constants, that list is AUTHORITATIVE per Python semantics: a name is public iff it appears in `__all__`, even if underscore-prefixed; names NOT in `__all__` are NOT public. Classes listed in `__all__` ALSO contribute their recursively-walked members (methods, nested classes, methods on nested classes at every depth) regardless of underscore prefix \u2014 so `__all__ = [\"Service\"]` protects `Service.run`, `Service.Inner`, and `Service.Inner.method`. Computed `__all__` (`sorted(...)`, `X + Y`) or AugAssign extension (`__all__ += [...]`) falls back to the heuristic. Without `__all__`, underscore-prefixed names (including dunders) are excluded at every level; `from X import *` contributes no fixed name and is ignored. The same `__all__` precedence rule (including the class-member recursion) is applied to the parallel `_snapshot_public_signatures` so signature-drift detection sees the same public set. `_snapshot_public_signatures` entries carry a leading kind prefix (`[function]`/`[async_function]`/`[class]` at top level; `[instance]`/`[staticmethod]`/`[classmethod]`/`[property:<roles>]` inside classes; `[assignment]` for module-level rebindings; and `[import]`/`[import:<module>]`/`[import:from <module>]`/`[import:from <module>:<source>]` for re-exports) so a binding-kind flip (e.g. a re-exported `pathlib.Path` replaced by a same-named `def`) diffs even when the parameter list happens to match. For `@dataclass` classes without an explicit `__init__`, `_snapshot_public_signatures` synthesises the constructor signature from class-body `AnnAssign` fields in source order: `@dataclass(kw_only=True)` emits a leading `*` so every field is kw-only; an in-body `_: KW_ONLY` sentinel (`KW_ONLY` Name or `dataclasses.KW_ONLY` Attribute) splits positional vs kw-only fields and is recognised before the underscore-prefix skip; when both decorator and sentinel are present only ONE `*` is emitted (decorator wins); `InitVar[...]` fields ARE included (they are runtime constructor params even though not stored as instance attributes); `ClassVar[...]` fields and `field(init=False, ...)` defaults are excluded; an explicit `__init__` still wins over synthesis. Inherited dataclass fields are collected in REVERSE-MRO order (matching Python's `@dataclass`: `__dataclass_fields__` is populated by walking bases right-to-left so later bases overwrite earlier ones in dict-insertion order) \u2014 `class C(A, B)` synthesises `(b, a, c)`. A base decorated with `@dataclass(init=False)` STILL contributes its annotated fields to a derived `@dataclass`'s synth; `init=False` only suppresses the base's OWN `__init__` synthesis. Cross-module bases that resolve to imports rather than same-module `ClassDef`s are marked `[inherited_unresolved]`. When a derived class redeclares a base's field name, the derived annotation/default wins but the field's POSITION matches the base's original slot (insertion-order dict semantics). The gate raises when public symbols are removed/renamed or their callable signature changes unless the prompt body contains an anchored `BREAKING-CHANGE:` directive (regex `^\\s*BREAKING-CHANGE:\\s*` with `re.MULTILINE`) whose tail starts with a `remove`/`delete`/`drop`/`rename` action verb (followed by a comma-separated symbol list) for removals or a `change signature`/`change api`/`change contract` verb pair for signature drift. Buried mid-line marker mentions and bare `BREAKING-CHANGE:` markers do NOT disable the gate. First-time generation is exempt. (b) TestChurnError \u2014 when overwriting an existing non-empty test file through code_generator_main or cmd_test_main, computes the stdlib `difflib.unified_diff` churn ratio between pre and post test bodies and raises when the ratio exceeds `PDD_TEST_CHURN_THRESHOLD` (default 0.40, accepts both decimal `0.40` and percent `40%` strings; defensive fallback to default on unparseable values) without an anchored `BREAKING-CHANGE:` directive that pairs an opt-out verb (`rewrite`/`replace`/`regenerate`/`overwrite`/`churn`/`remove`/`drop`) with `tests` as the verb's direct object. PublicSurfaceRegressionError and TestChurnError share the existing PDD_REPAIR_DIRECTIVE plumbing so sync_main, sync_orchestration, and agentic_sync_runner route them through the same repair loop. Issue #1649 adds ProseOutputError before the architecture/public-surface/test-churn gates: when extracted generated content is empty or whitespace-only for a non-prompt target, code_generator_main raises a typed generation-output extraction failure containing provider/model, prompt, output path, extractor result, and raw-output excerpt, so sync retries once with an output-shape directive instead of misdiagnosing empty extraction as missing exports/churn. `PDD_ALLOW_EMPTY_GENERATION=1` is the explicit escape hatch for the rare intentional empty-output case.",
    "dependencies": [
      "construct_paths_python.prompt",
      "preprocess_python.prompt",
      "code_generator_python.prompt",
      "incremental_code_generator_python.prompt",
      "core/cloud_python.prompt",
      "auto_include_python.prompt",
      "agentic_langtest_python.prompt",
      "_keyring_timeout_python.prompt",
      "context/python_preamble.prompt",
      "context_snapshot_python.prompt",
      "compressed_sync_context_python.prompt"
    ],
    "priority": 57,
    "filename": "code_generator_main_python.prompt",
    "filepath": "pdd/code_generator_main.py",
    "tags": [
      "cli",
      "generation",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "is_git_repository",
            "signature": "(path: Optional[str] = None) -> bool",
            "returns": "bool"
          },
          {
            "name": "get_git_content_at_ref",
            "signature": "(file_path: str, git_ref: str = \"HEAD\") -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "get_file_git_status",
            "signature": "(file_path: str) -> str",
            "returns": "str"
          },
          {
            "name": "git_add_files",
            "signature": "(file_paths: List[str], verbose: bool = False) -> bool",
            "returns": "bool"
          },
          {
            "name": "code_generator_main",
            "signature": "(ctx: click.Context, prompt_file: str, output: Optional[str], original_prompt_file_path: Optional[str], force_incremental_flag: bool, env_vars: Optional[Dict[str, str]] = None, unit_test_file: Optional[str] = None, exclude_tests: bool = False, language: Optional[str] = None, output_from_config: bool = False, compress: bool = False, snapshot_context: bool = False, compressed_context: Optional[Mapping[str, Any]] = None) -> Tuple[str, bool, float, str]",
            "returns": "Tuple[str, bool, float, str]"
          },
          {
            "name": "ArchitectureConformanceError",
            "signature": "(prompt_name: str, output_path: str, architecture_entry: Dict[str, Any], expected_symbols: List[str], found_symbols: List[str], missing_symbols: List[str], message: Optional[str] = None, total_cost: float = 0.0, model_name: str = \"unknown\", repair_directive: Optional[str] = None)",
            "returns": "ArchitectureConformanceError"
          },
          {
            "name": "PublicSurfaceRegressionError",
            "signature": "(prompt_name: str, output_path: str, removed_symbols: List[str], pre_surface_size: int, post_surface_size: int, changed_signatures: Optional[List[str]] = None, total_cost: float = 0.0, model_name: str = \"unknown\", repair_directive: Optional[str] = None)",
            "returns": "PublicSurfaceRegressionError"
          },
          {
            "name": "TestChurnError",
            "signature": "(prompt_name: str, output_path: str, churn_ratio: float, threshold: float, pre_line_count: int, post_line_count: int, total_cost: float = 0.0, model_name: str = \"unknown\", repair_directive: Optional[str] = None)",
            "returns": "TestChurnError"
          },
          {
            "name": "ProseOutputError",
            "signature": "(prompt_name: str, output_path: str, language: str, model_name: str = \"unknown\", total_cost: float = 0.0, raw_output: Optional[str] = None, extractor_result: str = \"empty\")",
            "returns": "ProseOutputError"
          }
        ]
      }
    },
    "position": {
      "x": 12560,
      "y": 1420
    }
  },
  {
    "reason": "Iteratively fixes code crashes until execution succeeds.",
    "description": "Runs crash-fix cycles with retry limits. Executes example code to verify fixes.",
    "dependencies": [],
    "priority": 58,
    "filename": "fix_code_loop_python.prompt",
    "filepath": "pdd/fix_code_loop.py",
    "tags": [
      "fix",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "cloud_crash_fix",
            "signature": "(program: str, prompt: str, code: str, errors: str, strength: float, temperature: float, time: float, verbose: bool, program_path: str = '', code_path: str = '', language: str = 'python') -> Tuple[bool, bool, str, str, str, float, Optional[str]]",
            "returns": "Tuple[bool, bool, str, str, str, float, Optional[str]]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "run_process_with_output",
            "signature": "(cmd_args, timeout = 300)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "fix_code_loop",
            "signature": "(code_file: str, prompt: str, verification_program: str, strength: float, temperature: float, max_attempts: int, budget: float, error_log_file: str, verbose: bool = False, time: float = DEFAULT_TIME, prompt_file: str = '', agentic_fallback: bool = True, use_cloud: bool = False) -> Tuple[bool, str, str, int, float, Optional[str]]",
            "returns": "Tuple[bool, str, str, int, float, Optional[str]]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, returncode, stdout, stderr)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "stream_pipe",
            "signature": "(pipe, sink, capture_list)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "dummy_fix_code_module_errors",
            "signature": "(program, prompt, code, errors, strength, temperature, verbose)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "run_agentic_crash",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_language",
            "signature": "(ext)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "default_verify_cmd_for",
            "signature": "(lang, verification_program)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "rprint",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "fix_code_module_errors",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10160,
      "y": 3140
    }
  },
  {
    "reason": "Iteratively fixes verification failures with cloud hybrid mode and compressed context support.",
    "description": "Runs verification-fix cycles until code meets specifications or retry limit reached.",
    "dependencies": [],
    "priority": 59,
    "filename": "fix_verification_errors_loop_python.prompt",
    "filepath": "pdd/fix_verification_errors_loop.py",
    "tags": [
      "fix",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "cloud_verify_fix",
            "signature": "(program: str, prompt: str, code: str, output: str, strength: float, temperature: float, time_param: float, verbose: bool, language: str = \"python\") -> Dict[str, Any]",
            "returns": "Dict[str, Any]"
          },
          {
            "name": "fix_verification_errors_loop",
            "signature": "(program_file: str, code_file: str, prompt: str, prompt_file: str, verification_program: str, strength: float, temperature: float, max_attempts: int, budget: float, verification_log_file: str = \"verification.log\", output_code_path: Optional[str] = None, output_program_path: Optional[str] = None, verbose: bool = False, program_args: Optional[list[str]] = None, llm_time: float = DEFAULT_TIME, agentic_fallback: bool = True, use_cloud: bool = False, compressed_context: Optional[Mapping[str, Any]] = None, agentic_fallback_events: Optional[list[dict[str, Any]]] = None) -> Dict[str, Any]",
            "returns": "Dict[str, Any]"
          }
        ]
      }
    },
    "position": {
      "x": 7800,
      "y": 1600
    }
  },
  {
    "reason": "Determines needed dependencies via auto_include and inserts them into a prompt, handling both new and updated includes.",
    "description": "Adds <include> tags for dependencies. Supports auto-dependency detection.",
    "dependencies": [
      "llm_invoke_python.prompt",
      "load_prompt_template_python.prompt",
      "auto_include_python.prompt",
      "preprocess_python.prompt",
      "path_resolution_python.prompt"
    ],
    "priority": 60,
    "filename": "insert_includes_python.prompt",
    "filepath": "pdd/insert_includes.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "insert_includes",
            "signature": "(input_prompt: str, directory_path: str, csv_filename: str, prompt_filename: Optional[str] = None, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: float = DEFAULT_TIME, verbose: bool = False, progress_callback: Optional[Callable[[int, int], None]] = None, include_docs: bool = False, max_workers: int = 1, dedup: bool = True, compress: bool = False)",
            "returns": "Tuple[str, str, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 11680,
      "y": 2480
    }
  },
  {
    "reason": "Splits functionality from one prompt into a new sub-prompt.",
    "description": "Extracts a subset of functionality into a separate prompt file. Creates proper dependency relationships.",
    "dependencies": [
      "cli_python.prompt",
      "conflicts_main_python.prompt",
      "trace_main_python.prompt",
      "track_cost_python.prompt"
    ],
    "priority": 61,
    "filename": "split_LLM.prompt",
    "filepath": "prompts/split_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 9000,
      "y": 1820
    }
  },
  {
    "reason": "CLI wrapper for updating prompts from modified code with repo-wide scan, agentic routing, optional metadata sync, and fingerprint finalization.",
    "description": "Command-line interface for update. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [
      "auto_include_python.prompt",
      "metadata_sync_python.prompt",
      "construct_paths_python.prompt",
      "config_resolution_python.prompt",
      "get_language_python.prompt",
      "agentic_common_python.prompt",
      "agentic_update_python.prompt",
      "sync_determine_operation_python.prompt",
      "operation_log_python.prompt"
    ],
    "priority": 62,
    "filename": "update_main_python.prompt",
    "filepath": "pdd/update_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "resolve_prompt_code_pair",
            "signature": "(code_file_path: str, quiet: bool = False, output_dir: Optional[str] = None) -> Tuple[str, str]"
          },
          {
            "name": "find_and_resolve_all_pairs",
            "signature": "(repo_root: str, quiet: bool = False, extensions: Optional[str] = None, output_dir: Optional[str] = None) -> List[Tuple[str, str]]"
          },
          {
            "name": "update_file_pair",
            "signature": "(prompt_file: str, code_file: str, ctx: click.Context, repo: git.Repo, simple: bool = False, strength: Optional[float] = None, temperature: Optional[float] = None) -> Dict[str, Any]"
          },
          {
            "name": "update_main",
            "signature": "(ctx: click.Context, input_prompt_file: Optional[str], modified_code_file: Optional[str], input_code_file: Optional[str], output: Optional[str], use_git: bool = False, repo: bool = False, extensions: Optional[str] = None, directory: Optional[str] = None, strength: Optional[float] = None, temperature: Optional[float] = None, simple: bool = False, base_branch: str = 'main', budget: Optional[float] = None, dry_run: bool = False, sync_metadata: bool = False) -> Optional[Tuple[str, float, str]]"
          }
        ]
      }
    },
    "position": {
      "x": 13760,
      "y": 2220
    }
  },
  {
    "reason": "Converts bug reports into failing unit tests.",
    "description": "Transforms GitHub issues or bug descriptions into reproducible test cases. Ensures bugs are captured before fixing.",
    "dependencies": [],
    "priority": 63,
    "filename": "bug_to_unit_test_python.prompt",
    "filepath": "pdd/bug_to_unit_test.py",
    "tags": [
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "bug_to_unit_test",
            "signature": "(current_output: str, desired_output: str, prompt_used_to_generate_the_code: str, code_under_test: str, program_used_to_run_code_under_test: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: float = DEFAULT_TIME, language: str = 'python') -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "main",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 6000,
      "y": 1600
    }
  },
  {
    "reason": "Core module that transforms prompts into executable code.",
    "description": "Compiles natural language prompts into code. Orchestrates preprocessing, LLM invocation, continuation for incomplete outputs, and postprocessing. Returns generated code, cost, and model used.",
    "dependencies": [],
    "priority": 64,
    "filename": "code_generator_python.prompt",
    "filepath": "pdd/code_generator.py",
    "tags": [
      "generation",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "code_generator",
            "signature": "(prompt: str, language: str, strength: float, temperature: float = 0.0, time: Optional[float] = None, verbose: bool = False, preprocess_prompt: bool = True, output_schema: Optional[dict] = None) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 8400,
      "y": 1600
    }
  },
  {
    "reason": "Classify pytest / verification output for `fix_error_loop` and `agentic_multishot` by failure type: syntax_import, assertion_logic, timeout_flaky, budget_truncated, infrastructure_error.",
    "description": "Heuristic classifier (regex-based, no LLM) used by fix_error_loop and agentic_multishot to categorize failures. Covers five FailureKind values; budget_truncated and infrastructure_error are set programmatically by the multi-shot controller rather than derived from output text.",
    "dependencies": [],
    "priority": 64,
    "filename": "failure_classification_python.prompt",
    "filepath": "pdd/failure_classification.py",
    "tags": [
      "fix",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "classify_failure",
            "signature": "(text: str) -> FailureKind",
            "returns": "FailureKind"
          },
          {
            "name": "extract_failure_signature",
            "signature": "(text: str) -> str",
            "returns": "str"
          },
          {
            "name": "failure_classification_hint",
            "signature": "(kind: FailureKind) -> str",
            "returns": "str"
          },
          {
            "name": "format_signature_hint",
            "signature": "(sig: str) -> str",
            "returns": "str"
          }
        ]
      }
    }
  },
  {
    "reason": "Sequential multi-shot repeat-run controller for agentic CLI harnesses; wraps run_agentic_task with per-shot budget/timeout, hidden-verifier selection, and append-only JSONL telemetry.",
    "description": "Orchestration controller for sequential multi-shot CLI repeat-runs. Wraps run_agentic_task with per-shot and total budget/timeout caps, hidden-verifier candidate selection, append-only JSONL persistence, and stop-early behavior on first pass.",
    "dependencies": [
      "agentic_common_python.prompt",
      "failure_classification_python.prompt"
    ],
    "priority": 65,
    "filename": "agentic_multishot_python.prompt",
    "filepath": "pdd/agentic_multishot.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "dataclasses": [
          {
            "name": "MultishotCandidateRecord",
            "fields": [
              {
                "name": "shot_id",
                "type": "int"
              },
              {
                "name": "task_id",
                "type": "str"
              },
              {
                "name": "multishot_run_id",
                "type": "str"
              },
              {
                "name": "cli_version",
                "type": "str"
              },
              {
                "name": "config_id",
                "type": "str"
              },
              {
                "name": "verdict",
                "type": "str"
              },
              {
                "name": "failure_class",
                "type": "Optional[str]"
              },
              {
                "name": "cost_usd",
                "type": "float"
              },
              {
                "name": "latency_ms",
                "type": "float"
              },
              {
                "name": "started_at",
                "type": "str"
              },
              {
                "name": "finished_at",
                "type": "str"
              },
              {
                "name": "stop_reason",
                "type": "Optional[str]"
              }
            ]
          },
          {
            "name": "MultishotResult",
            "fields": [
              {
                "name": "winner",
                "type": "Optional[MultishotCandidateRecord]"
              },
              {
                "name": "candidates",
                "type": "List[MultishotCandidateRecord]"
              },
              {
                "name": "total_cost_usd",
                "type": "float"
              },
              {
                "name": "total_latency_ms",
                "type": "float"
              }
            ]
          }
        ],
        "functions": [
          {
            "name": "run_multishot_candidates",
            "signature": "(instruction, cwd, *, n_shots=1, per_shot_budget, total_budget, per_shot_timeout_s, total_timeout_s, stop_on_first_pass=True, verifier_fn, config_id, task_id, jsonl_path=None, **kwargs) -> MultishotResult",
            "returns": "MultishotResult"
          }
        ]
      }
    }
  },
  {
    "reason": "Iterative test-fix loop with verification, failure-aware retries, and agentic fallback for unresolved failures.",
    "description": "Runs fix-test cycles with configurable retry limits. Tracks progress and cost across iterations. Supports two-phase focused repair for large dev units via fix_focus, and failure-aware early exit for syntax/import and timeout/flaky failures.",
    "dependencies": [
      "failure_classification_python.prompt",
      "fix_focus_python.prompt",
      "compressed_sync_context_python.prompt",
      "test_context_packer_python.prompt"
    ],
    "priority": 65,
    "filename": "fix_error_loop_python.prompt",
    "filepath": "pdd/fix_error_loop.py",
    "tags": [
      "fix",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "cloud_fix_errors",
            "signature": "(unit_test, code, prompt, error, error_file, strength, temperature, verbose = False, time = DEFAULT_TIME, code_file_ext = '.py', protect_tests = False, failure_classification = None, compressed_context = None)",
            "returns": "Tuple[bool, bool, str, str, str, float, str]"
          },
          {
            "name": "fix_error_loop",
            "signature": "(unit_test_file, code_file, prompt_file, prompt, verification_program, strength, temperature, max_attempts, budget, error_log_file='error_log.txt', verbose=False, time=DEFAULT_TIME, agentic_fallback=True, protect_tests=False, use_cloud=False, test_files=None, failure_aware_retries=True, no_local_fallback=False, compress_test_context=False, context_compression=None, compression_fallback=None, compressed_context=None, agentic_fallback_events=None)",
            "returns": "Tuple[bool, str, str, int, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 9280,
      "y": 1600
    }
  },
  {
    "reason": "Static analysis helpers for focused repair: skeleton extraction, function slicing, and reconstruction. Zero I/O. Phase-1 diagnosis uses one lightweight llm_invoke call when fast-path traceback parsing finds no targets or when fast-path targets do not match product-code slices.",
    "description": "AST-based module used by fix_error_loop and fix_main for two-phase focused repair of large dev units. Provides skeleton extraction, function slicing, traceback parsing, focused test slicing, and source reconstruction. The prepare_focused_inputs Phase-1 path calls llm_invoke (lazy import) when fast-path traceback parsing returns no function names OR when fast-path names match no product-code slices (e.g. assertion-only failures where only test functions appear in the traceback), but skips that LLM call and returns [] when ast.parse(code) raises SyntaxError so syntax-broken large files fall back instead of sending an unchanged full source payload. Focused test slicing preserves setup/teardown, fixtures, class attributes, non-test helper methods, and referenced non-collecting top-level support definitions such as TestBase; it falls back to the full test file when the dependency-preserving slice is not meaningfully smaller.",
    "dependencies": [
      "llm_invoke_python.prompt"
    ],
    "priority": 64,
    "filename": "fix_focus_python.prompt",
    "filepath": "pdd/fix_focus.py",
    "tags": [
      "fix",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "is_large",
            "signature": "(code: str, unit_test: str) -> bool",
            "returns": "bool"
          },
          {
            "name": "build_skeleton",
            "signature": "(code: str) -> str",
            "returns": "str"
          },
          {
            "name": "extract_function_names_from_traceback",
            "signature": "(error: str) -> list[str]",
            "returns": "list[str]"
          },
          {
            "name": "prepare_focused_inputs",
            "signature": "(code: str, unit_test: str, error: str, strength: float, temperature: float, time: float, verbose: bool, language: Optional[str]) -> Optional[FocusedInputs]",
            "returns": "Optional[FocusedInputs]"
          },
          {
            "name": "reconstruct_code",
            "signature": "(original_code: str, fixed_focused_code: str, slices: list[FunctionSlice]) -> str",
            "returns": "str"
          }
        ],
        "models": [
          {
            "name": "DiagnosisResult",
            "fields": [
              "function_names: list[str]"
            ]
          },
          {
            "name": "FunctionSlice",
            "fields": [
              "name: str",
              "qualname: str",
              "start_line: int",
              "end_line: int",
              "source: str",
              "indent: int"
            ]
          },
          {
            "name": "FocusedInputs",
            "fields": [
              "focused_code: str",
              "focused_tests: str",
              "slices: list[FunctionSlice]"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 9280,
      "y": 1760
    }
  },
  {
    "reason": "CLI wrapper for unit-test repair with loop mode, cloud/local execution, and compressed repair context.",
    "description": "Command-line interface for fix. Parses arguments, orchestrates loop and single-pass fix workflows, formats output, and preserves Issue #888 focused repair outside loop mode. In single-pass cloud and local modes, large code/test inputs are reduced through fix_focus.prepare_focused_inputs before the LLM call; focused calls force effective test protection, reconstruct full code with fix_focus.reconstruct_code before validation/writes, validate against the full original test file, and never write returned sliced test content. The post-success auto-submit branch skips when CloudConfig.is_running_in_cloud() is true, calls CloudConfig.ensure_default_env() before lower-level cached JWT lookup, and wraps the asyncio JWT request in asyncio.wait_for (PDD_AUTO_SUBMIT_AUTH_TIMEOUT_S, default 300s / 5 min, sized for interactive Device Flow auth; defensive parse falls back to 300s on malformed values).",
    "dependencies": [
      "fix_error_loop_python.prompt",
      "fix_focus_python.prompt",
      "agentic_langtest_python.prompt",
      "auth_service_python.prompt",
      "core/cloud_python.prompt",
      "compressed_sync_context_python.prompt"
    ],
    "priority": 66,
    "filename": "fix_main_python.prompt",
    "filepath": "pdd/fix_main.py",
    "tags": [
      "cli",
      "fix",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "fix_main",
            "signature": "(ctx: click.Context, prompt_file: str, code_file: str, unit_test_file: str, error_file: str, output_test: Optional[str], output_code: Optional[str], output_results: Optional[str], loop: bool, verification_program: Optional[str], max_attempts: int, budget: float, auto_submit: bool, agentic_fallback: bool = True, strength: Optional[float] = None, temperature: Optional[float] = None, protect_tests: bool = False, test_files: list[str] | None = None, failure_aware_retries: bool = True, compress_test_context: bool | None = None, context_compression: str | None = None, compression_fallback: str | None = None, compressed_context: Mapping[str, Any] | None = None, agentic_fallback_events: list[dict[str, Any]] | None = None) -> Tuple[bool, str, str, int, float, str]",
            "returns": "Tuple[bool, str, str, int, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 10800,
      "y": 2000
    }
  },
  {
    "reason": "Determines the next sync operation, including isolated replay/repair decisions.",
    "description": "Analyzes prompt state, file timestamps, and previous results to decide which workflow steps are needed.",
    "dependencies": [
      "agentic_langtest_python.prompt"
    ],
    "priority": 67,
    "filename": "sync_determine_operation_python.prompt",
    "filepath": "pdd/sync_determine_operation.py",
    "tags": [
      "module",
      "python",
      "sync"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_pdd_file_paths",
            "signature": "(basename: str, language: str, prompts_dir: str = \"prompts\", context_override: Optional[str] = None) -> Dict[str, Path]",
            "returns": "Dict[str, Path]"
          },
          {
            "name": "sync_determine_operation",
            "signature": "(basename: str, language: str, target_coverage: float, budget: float = 10.0, log_mode: bool = False, prompts_dir: str = \"prompts\", skip_tests: bool = False, skip_verify: bool = False, context_override: Optional[str] = None, read_only: bool = False, isolated_replay_or_repair: bool = False) -> SyncDecision",
            "returns": "SyncDecision"
          },
          {
            "name": "analyze_conflict_with_llm",
            "signature": "(basename: str, language: str, fingerprint: Fingerprint, changed_files: List[str], prompts_dir: str = \"prompts\", context_override: Optional[str] = None) -> SyncDecision",
            "returns": "SyncDecision"
          }
        ]
      }
    },
    "position": {
      "x": 14400,
      "y": 1200
    }
  },
  {
    "reason": "Generates unit tests for PDD-generated code modules.",
    "description": "Creates pytest-compatible test files based on module prompts and generated code. Tests accumulate over time to prevent regressions.",
    "dependencies": [
      "bug_to_unit_test_python.prompt"
    ],
    "priority": 68,
    "filename": "generate_test_python.prompt",
    "filepath": "pdd/generate_test.py",
    "tags": [
      "generation",
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "generate_test",
            "signature": "(prompt: str, code: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: float = DEFAULT_TIME, language: str = 'python', verbose: bool = False, source_file_path: Optional[str] = None, test_file_path: Optional[str] = None, module_name: Optional[str] = None, existing_tests: Optional[str] = None) -> Tuple[str, float, str]",
            "returns": "Tuple[str, float, str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10200,
      "y": 1600
    }
  },
  {
    "reason": "Autonomously fixes failing tests with exploration.",
    "description": "Investigates test failures, explores codebase for context, and generates targeted fixes. Supports harvest-only mode.",
    "dependencies": [],
    "priority": 69,
    "filename": "agentic_fix_python.prompt",
    "filepath": "pdd/agentic_fix.py",
    "tags": [
      "agentic",
      "fix",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_agent_command",
            "signature": "(provider: str, instruction_file: Path) -> List[str]",
            "returns": "List[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "find_llm_csv_path",
            "signature": "() -> Optional[Path]",
            "returns": "Optional[Path]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "run_agentic_fix",
            "signature": "(prompt_file: str, code_file: str, unit_test_file: str, error_log_file: str, verify_cmd: Optional[str] = None, cwd: Optional[Path] = None) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple[bool, str, float, str, List[str]]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 7480,
      "y": 1200
    }
  },
  {
    "reason": "Coordinates sync workflow steps and state management. Manages workflow state, determines next steps, tracks progress, and handles step dependencies.",
    "description": "Manages workflow state, determines next steps, tracks progress, and handles step dependencies. For generate operations, catches ArchitectureConformanceError, PublicSurfaceRegressionError, and TestChurnError (issue #1012), accumulates failed-attempt total_cost/model_name, retries code_generator_main up to MAX_CONFORMANCE_ATTEMPTS with PDD_REPAIR_DIRECTIVE set to the previous failure's repair_directive while budget remains, short-circuits on repeated failure signatures (missing-symbol set / removed-symbol set / (rounded churn ratio, pre_line_count)) and additionally caps TestChurnError retries at one attempt, reuses the already-resolved .pddrc context_override/strength/temperature/context_config across retries (no mid-loop .pddrc re-resolution), relies on code_generator_main reading the pre-generation code file from disk for the public-surface snapshot, restores the prior environment value, includes failed-attempt cost in success and exception logs, and emits the appropriate build_conformance_hard_failure_from_error / build_public_surface_hard_failure_from_error / build_test_churn_hard_failure_from_error diagnostic block before re-raising the final failure.",
    "dependencies": [
      "code_generator_main_python.prompt",
      "agentic_sync_runner_python.prompt",
      "compressed_sync_context_python.prompt"
    ],
    "priority": 70,
    "filename": "sync_orchestration_python.prompt",
    "filepath": "pdd/sync_orchestration.py",
    "tags": [
      "module",
      "python",
      "sync"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "sync_orchestration",
            "signature": "(basename: str, target_coverage: float = 90.0, language: str = \"python\", prompts_dir: str = \"prompts\", code_dir: str = \"src\", examples_dir: str = \"examples\", tests_dir: str = \"tests\", max_attempts: int = 3, budget: float = 10.0, skip_verify: bool = False, skip_tests: bool = False, dry_run: bool = False, force: bool = False, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time_param: float = 0.25, verbose: bool = False, quiet: bool = False, output_cost: Optional[str] = None, review_examples: bool = False, local: bool = False, context_config: Optional[Dict[str, str]] = None, context_override: Optional[str] = None, confirm_callback: Optional[Callable[[str, str], bool]] = None, no_steer: bool = False, steer_timeout: float = DEFAULT_STEER_TIMEOUT_S, agentic_mode: bool = False, snapshot_context: bool = False, compressed_context: bool = False) -> Dict[str, Any]",
            "returns": "Dict[str, Any]"
          }
        ]
      }
    },
    "position": {
      "x": 12000,
      "y": 2620
    }
  },
  {
    "reason": "Configuration data for Makefile.",
    "description": "Defines Makefile settings and data. Used by runtime modules.",
    "dependencies": [],
    "priority": 71,
    "filename": "Makefile_makefile.prompt",
    "filepath": "Makefile",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 4800,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for agentic bug step1 duplicate operations.",
    "description": "Defines the prompt structure for agentic bug step1 duplicate. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 72,
    "filename": "agentic_bug_step1_duplicate_LLM.prompt",
    "filepath": "prompts/agentic_bug_step1_duplicate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15280,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for agentic bug step2 docs operations.",
    "description": "Defines the prompt structure for agentic bug step2 docs. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 73,
    "filename": "agentic_bug_step2_docs_LLM.prompt",
    "filepath": "prompts/agentic_bug_step2_docs_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15560,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for agentic bug step3 triage operations.",
    "description": "Defines the prompt structure for agentic bug step3 triage. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 74,
    "filename": "agentic_bug_step3_triage_LLM.prompt",
    "filepath": "prompts/agentic_bug_step3_triage_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15000,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for agentic bug step4 reproduce operations.",
    "description": "Defines the prompt structure for agentic bug step4 reproduce. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 75,
    "filename": "agentic_bug_step5_reproduce_LLM.prompt",
    "filepath": "prompts/agentic_bug_step5_reproduce_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15280,
      "y": 660
    }
  },
  {
    "reason": "Analyzes bug root cause and classifies scope as LOCALIZED/SIBLING_PATTERN/CROSS_CUTTING with mandatory sibling search.",
    "description": "Defines the prompt structure for agentic bug step6 root cause. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 76,
    "filename": "agentic_bug_step6_root_cause_LLM.prompt",
    "filepath": "prompts/agentic_bug_step6_root_cause_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15600,
      "y": 0
    }
  },
  {
    "reason": "Guides LLM to design observable-behavior test plans with full execution-path coverage during pdd bug step 8.",
    "description": "Defines the prompt structure for agentic bug step8 test plan. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 77,
    "filename": "agentic_bug_step8_test_plan_LLM.prompt",
    "filepath": "prompts/agentic_bug_step8_test_plan_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15880,
      "y": 220
    }
  },
  {
    "reason": "Generates failing unit tests that detect the bug, with scope cross-check against Step 6 findings.",
    "description": "Defines the prompt structure for agentic bug step9 generate. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 78,
    "filename": "agentic_bug_step9_generate_LLM.prompt",
    "filepath": "prompts/agentic_bug_step9_generate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "generation",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 16160,
      "y": 0
    }
  },
  {
    "reason": "Verifies unit test correctness and classifies whether E2E testing is needed.",
    "description": "Defines the prompt structure for agentic bug step10 verify. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 79,
    "filename": "agentic_bug_step10_verify_LLM.prompt",
    "filepath": "prompts/agentic_bug_step10_verify_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15600,
      "y": 660
    }
  },
  {
    "reason": "Creates draft PR with failing tests and PDD fix command for step 10 of agentic bug workflow.",
    "description": "Step 10 (final step) of the agentic bug workflow. Creates a draft pull request with failing tests and links it to the GitHub issue. Includes pdd fix command for resolving the bug.",
    "dependencies": [],
    "priority": 80,
    "filename": "agentic_bug_step12_pr_LLM.prompt",
    "filepath": "prompts/agentic_bug_step12_pr_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "command",
      "command": {
        "name": "agentic_bug_step10",
        "description": "Generate draft PR with failing tests and link to GitHub issue"
      }
    },
    "position": {
      "x": 15000,
      "y": 220
    }
  },
  {
    "reason": "Fallback fix strategy for non-Python PDD projects where normal fix loop failed.",
    "description": "LLM prompt template for fixing failing tests in non-Python PDD projects. Runs as fallback after PDD normal fix loop failed multiple times. Follows PDD principle that prompt file is source of truth.",
    "dependencies": [],
    "priority": 81,
    "filename": "agentic_fix_nonpython_LLM.prompt",
    "filepath": "prompts/agentic_fix_nonpython_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 4760,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for agentic fix primary operations.",
    "description": "Defines the prompt structure for agentic fix primary. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 82,
    "filename": "agentic_fix_primary_LLM.prompt",
    "filepath": "prompts/agentic_fix_primary_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 4200,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for selecting relevant code and documentation includes for a prompt.",
    "description": "Analyzes prompt content to determine which context files should be included. Reduces manual dependency management.",
    "dependencies": [],
    "priority": 83,
    "filename": "auto_include_LLM.prompt",
    "filepath": "prompts/auto_include_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 0,
      "y": 0
    }
  },
  {
    "reason": "Automatically updates prompts across the codebase.",
    "description": "Propagates changes through dependent prompts. Maintains consistency after modifications.",
    "dependencies": [],
    "priority": 84,
    "filename": "auto_update_python.prompt",
    "filepath": "pdd/auto_update.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "detect_installation_method",
            "signature": "(sys_executable)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_upgrade_command",
            "signature": "(package_name, installation_method)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "auto_update",
            "signature": "(package_name: str = 'pdd-cli', latest_version: str = None) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 8680,
      "y": 0
    }
  },
  {
    "reason": "Converts bug reports into failing unit tests.",
    "description": "Transforms GitHub issues or bug descriptions into reproducible test cases. Ensures bugs are captured before fixing.",
    "dependencies": [],
    "priority": 85,
    "filename": "bug_to_unit_test_LLM.prompt",
    "filepath": "prompts/bug_to_unit_test_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 3600,
      "y": 0
    }
  },
  {
    "reason": "Modifies prompts based on change instructions.",
    "description": "Applies specified modifications to prompt content. Preserves structure while incorporating requested changes.",
    "dependencies": [],
    "priority": 86,
    "filename": "change_LLM.prompt",
    "filepath": "prompts/change_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 280,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for code patcher operations.",
    "description": "Defines the prompt structure for code patcher. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 87,
    "filename": "code_patcher_LLM.prompt",
    "filepath": "prompts/code_patcher_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 560,
      "y": 0
    }
  },
  {
    "reason": "Formats comment lines for different languages.",
    "description": "Generates properly formatted comments. Handles indentation and language conventions.",
    "dependencies": [],
    "priority": 88,
    "filename": "comment_line_python.prompt",
    "filepath": "pdd/comment_line.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "comment_line",
            "signature": "(code_line, comment_characters)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 8680,
      "y": 880
    }
  },
  {
    "reason": "LLM prompt template for conflict operations.",
    "description": "Defines the prompt structure for conflict. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 89,
    "filename": "conflict_LLM.prompt",
    "filepath": "prompts/conflict_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 0,
      "y": 440
    }
  },
  {
    "reason": "Continues incomplete LLM outputs that were truncated.",
    "description": "Detects when LLM output is incomplete and continues generation from where it stopped. Handles token limit issues gracefully.",
    "dependencies": [],
    "priority": 90,
    "filename": "continue_generation_LLM.prompt",
    "filepath": "prompts/continue_generation_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 280,
      "y": 660
    }
  },
  {
    "reason": "Identifies which prompts need updates based on changes.",
    "description": "Analyzes change descriptions to determine affected prompts. Supports batch change detection.",
    "dependencies": [],
    "priority": 91,
    "filename": "detect_change_LLM.prompt",
    "filepath": "prompts/detect_change_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 560,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for diff analyzer operations.",
    "description": "Defines the prompt structure for diff analyzer. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 92,
    "filename": "diff_analyzer_LLM.prompt",
    "filepath": "prompts/diff_analyzer_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 0,
      "y": 880
    }
  },
  {
    "reason": "LLM prompt template for example generator operations.",
    "description": "Defines the prompt structure for example generator. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 94,
    "filename": "example_generator_LLM.prompt",
    "filepath": "prompts/example_generator_LLM.prompt",
    "tags": [
      "config",
      "generation",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 3000,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for extract code operations.",
    "description": "Defines the prompt structure for extract code. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 96,
    "filename": "extract_code_LLM.prompt",
    "filepath": "prompts/extract_code_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2080,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for extract conflict operations.",
    "description": "Defines the prompt structure for extract conflict. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 97,
    "filename": "extract_conflict_LLM.prompt",
    "filepath": "prompts/extract_conflict_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2360,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for extract detect change operations.",
    "description": "Defines the prompt structure for extract detect change. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 98,
    "filename": "extract_detect_change_LLM.prompt",
    "filepath": "prompts/extract_detect_change_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 1800,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for extract program code fix operations.",
    "description": "Defines the prompt structure for extract program code fix. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 99,
    "filename": "extract_program_code_fix_LLM.prompt",
    "filepath": "prompts/extract_program_code_fix_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2080,
      "y": 660
    }
  },
  {
    "reason": "LLM prompt template for extract prompt change operations.",
    "description": "Defines the prompt structure for extract prompt change. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 100,
    "filename": "extract_prompt_change_LLM.prompt",
    "filepath": "prompts/extract_prompt_change_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2360,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for extract prompt split operations.",
    "description": "Defines the prompt structure for extract prompt split. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 101,
    "filename": "extract_prompt_split_LLM.prompt",
    "filepath": "prompts/extract_prompt_split_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 1800,
      "y": 880
    }
  },
  {
    "reason": "LLM prompt template for extract prompt update operations.",
    "description": "Defines the prompt structure for extract prompt update. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 102,
    "filename": "extract_prompt_update_LLM.prompt",
    "filepath": "prompts/extract_prompt_update_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2080,
      "y": 1100
    }
  },
  {
    "reason": "LLM prompt template for extract promptline operations.",
    "description": "Defines the prompt structure for extract promptline. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 103,
    "filename": "extract_promptline_LLM.prompt",
    "filepath": "prompts/extract_promptline_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2400,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for extract unit code fix operations.",
    "description": "Defines the prompt structure for extract unit code fix. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 104,
    "filename": "extract_unit_code_fix_LLM.prompt",
    "filepath": "prompts/extract_unit_code_fix_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2680,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for extract xml operations.",
    "description": "Defines the prompt structure for extract xml. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 105,
    "filename": "extract_xml_LLM.prompt",
    "filepath": "prompts/extract_xml_LLM.prompt",
    "tags": [
      "config",
      "extraction",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 2960,
      "y": 220
    }
  },
  {
    "reason": "Locates sections within prompt files.",
    "description": "Finds specific sections in prompts by header or content. Used for targeted modifications.",
    "dependencies": [],
    "priority": 106,
    "filename": "find_section_python.prompt",
    "filepath": "pdd/find_section.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "find_section",
            "signature": "(lines, start_index = 0, sub_section = False)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 8680,
      "y": 1100
    }
  },
  {
    "reason": "LLM prompt template for find verification errors operations.",
    "description": "Defines the prompt structure for find verification errors. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 107,
    "filename": "find_verification_errors_LLM.prompt",
    "filepath": "prompts/find_verification_errors_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 280,
      "y": 1100
    }
  },
  {
    "reason": "Fixes runtime crashes in generated code modules.",
    "description": "Analyzes stack traces and error messages to fix crashed code. Handles both syntax and runtime errors.",
    "dependencies": [],
    "priority": 108,
    "filename": "fix_code_module_errors_LLM.prompt",
    "filepath": "prompts/fix_code_module_errors_LLM.prompt",
    "tags": [
      "config",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 4200,
      "y": 440
    }
  },
  {
    "reason": "Fixes issues found during functional verification.",
    "description": "Corrects code that fails verification checks. Addresses logic errors and specification mismatches.",
    "dependencies": [],
    "priority": 109,
    "filename": "fix_verification_errors_LLM.prompt",
    "filepath": "prompts/fix_verification_errors_LLM.prompt",
    "tags": [
      "config",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 4480,
      "y": 660
    }
  },
  {
    "reason": "Generates unit tests for PDD-generated code modules with auth-aware test patterns.",
    "description": "Creates pytest-compatible test files based on module prompts and generated code. Tests accumulate over time to prevent regressions. Prefers pattern-based pytest.raises(match=r'...') assertions over exact-string matches.",
    "dependencies": [],
    "priority": 110,
    "filename": "generate_test_LLM.prompt",
    "filepath": "prompts/generate_test_LLM.prompt",
    "tags": [
      "generation",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "generate_test",
            "signature": "(prompt_content: str, code_content: str, language: str)",
            "returns": "str (test file content)"
          }
        ]
      }
    },
    "position": {
      "x": 3880,
      "y": 220
    }
  },
  {
    "reason": "Expands test coverage for existing test suites.",
    "description": "Analyzes existing tests and generates additional test cases to improve coverage. Focuses on edge cases and error conditions.",
    "dependencies": [],
    "priority": 111,
    "filename": "increase_tests_LLM.prompt",
    "filepath": "prompts/increase_tests_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 3600,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for inserting dependency includes into a prompt with doc placement.",
    "description": "Adds <include> tags for dependencies. Supports auto-dependency detection.",
    "dependencies": [],
    "priority": 112,
    "filename": "insert_includes_LLM.prompt",
    "filepath": "prompts/insert_includes_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 600,
      "y": 220
    }
  },
  {
    "reason": "Installs shell completion for PDD CLI.",
    "description": "Configures bash/zsh/fish completion for pdd commands. Improves CLI usability.",
    "dependencies": [],
    "priority": 113,
    "filename": "install_completion_python.prompt",
    "filepath": "pdd/install_completion.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_local_pdd_path",
            "signature": "() -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_shell_rc_path",
            "signature": "(shell: str) -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_current_shell",
            "signature": "() -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_completion_script_extension",
            "signature": "(shell: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "install_completion",
            "signature": "(quiet: bool = False)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 9000,
      "y": 1320
    }
  },
  {
    "reason": "Configuration data for language format.",
    "description": "Defines language format settings and data. Used by runtime modules.",
    "dependencies": [],
    "priority": 114,
    "filename": "language_format_csv.prompt",
    "filepath": "pdd/data/language_format.csv",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 5080,
      "y": 0
    }
  },
  {
    "reason": "Defines the llm_model.csv catalog schema: provider, pricing, raw Arena ELO, DeepSWE-first rank score, structured_output, and interactive_only columns.",
    "description": "Defines llm model settings and data. Used by runtime modules.",
    "dependencies": [],
    "priority": 115,
    "filename": "llm_model_csv.prompt",
    "filepath": "pdd/data/llm_model.csv",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 5360,
      "y": 220
    }
  },
  {
    "reason": "Generates the task_routing.csv skeleton: per-task static routing table for the E[pass]−λ·cost router in llm_invoke.",
    "description": "Defines the static per-task config routing table (task_class, model, temperature, effort, shots, pass_rate, avg_cost_usd) consumed by llm_invoke()'s E[pass]−λ·cost router when PDD_ENABLE_TASK_ROUTING=1.",
    "dependencies": [
      "llm_invoke_python.prompt"
    ],
    "priority": 115,
    "filename": "task_routing_csv.prompt",
    "filepath": "pdd/data/task_routing.csv",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": [
          "task_class",
          "model",
          "temperature",
          "effort",
          "shots",
          "pass_rate",
          "avg_cost_usd"
        ]
      }
    }
  },
  {
    "reason": "Shell completion script for bash.",
    "description": "Provides command-line completion for pdd in bash. Lists commands and options.",
    "dependencies": [],
    "priority": 116,
    "filename": "pdd_completion_bash.prompt",
    "filepath": "pdd/pdd_completion.sh",
    "tags": [
      "cli",
      "shell"
    ],
    "interface": {
      "type": "cli"
    },
    "position": {
      "x": 4800,
      "y": 660
    }
  },
  {
    "reason": "Shell completion script for fish.",
    "description": "Provides command-line completion for pdd in fish. Lists commands and options.",
    "dependencies": [],
    "priority": 117,
    "filename": "pdd_completion_fish.prompt",
    "filepath": "pdd/pdd_completion.fish",
    "tags": [
      "cli",
      "shell"
    ],
    "interface": {
      "type": "cli"
    },
    "position": {
      "x": 5080,
      "y": 880
    }
  },
  {
    "reason": "Shell completion script for zsh.",
    "description": "Provides command-line completion for pdd in zsh. Lists commands and options.",
    "dependencies": [],
    "priority": 118,
    "filename": "pdd_completion_zsh.prompt",
    "filepath": "pdd/pdd_completion.zsh",
    "tags": [
      "cli",
      "shell"
    ],
    "interface": {
      "type": "cli"
    },
    "position": {
      "x": 5360,
      "y": 660
    }
  },
  {
    "reason": "Configuration data for pdd theme.",
    "description": "Defines pdd theme settings and data. Used by runtime modules.",
    "dependencies": [],
    "priority": 119,
    "filename": "pdd_theme_json.prompt",
    "filepath": "prompts/pdd_theme_json.prompt",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 4800,
      "y": 1100
    }
  },
  {
    "reason": "Module that post-process the string output of a LLM so that the code can be run.",
    "description": "Implements functionality to post-process the string output of a LLM so that the code can be run. Integrates with PDD workflow.",
    "dependencies": [],
    "priority": 120,
    "filename": "postprocess_0_python.prompt",
    "filepath": "pdd/postprocess_0.py",
    "tags": [
      "module",
      "processing",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "postprocess_0",
            "signature": "(llm_output: str, language: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10160,
      "y": 1320
    }
  },
  {
    "reason": "Implements pypi description functionality.",
    "description": "Provides pypi description capabilities for PDD operations.",
    "dependencies": [],
    "priority": 121,
    "filename": "pypi_description_restructuredtext.prompt",
    "filepath": "pypi_description.rst",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 5400,
      "y": 0
    }
  },
  {
    "reason": "Configuration data for pyproject.",
    "description": "Defines pyproject settings and data. Used by runtime modules.",
    "dependencies": [],
    "priority": 122,
    "filename": "pyproject_toml.prompt",
    "filepath": "pyproject.toml",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 5680,
      "y": 220
    }
  },
  {
    "reason": "Parses pytest output for automated error handling.",
    "description": "Extracts test results, failures, and error messages from pytest output. Provides structured data for fix workflows.",
    "dependencies": [],
    "priority": 123,
    "filename": "pytest_output_python.prompt",
    "filepath": "pdd/pytest_output.py",
    "tags": [
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "extract_failing_files_from_output",
            "signature": "(pytest_output: str) -> list[str]",
            "returns": "list[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "run_pytest_and_capture_output",
            "signature": "(test_file: str) -> dict",
            "returns": "dict",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "save_output_to_json",
            "signature": "(output: dict, output_file: str = 'pytest.json')",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "main",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "pytest_runtest_logreport",
            "signature": "(self, report)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "pytest_sessionfinish",
            "signature": "(self, session)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "capture_logs",
            "signature": "(self)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_logs",
            "signature": "(self)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 6280,
      "y": 0
    }
  },
  {
    "reason": "Implements regression analysis functionality.",
    "description": "Provides regression analysis capabilities for PDD operations.",
    "dependencies": [],
    "priority": 124,
    "filename": "regression_analysis_log.prompt",
    "filepath": "prompts/regression_analysis_log.prompt",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 5960,
      "y": 0
    }
  },
  {
    "reason": "Implements regression functionality.",
    "description": "Provides regression capabilities for PDD operations.",
    "dependencies": [],
    "priority": 125,
    "filename": "regression_bash.prompt",
    "filepath": "prompts/regression_bash.prompt",
    "tags": [
      "cli",
      "shell"
    ],
    "interface": {
      "type": "cli"
    },
    "position": {
      "x": 5400,
      "y": 440
    }
  },
  {
    "reason": "Renders Mermaid diagrams to HTML.",
    "description": "Converts Mermaid diagram specifications to HTML visualization. Used for architecture diagrams.",
    "dependencies": [],
    "priority": 126,
    "filename": "render_mermaid_python.prompt",
    "filepath": "pdd/render_mermaid.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "write_pretty_architecture_json",
            "signature": "(arch_file, architecture)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "generate_mermaid_code",
            "signature": "(architecture, app_name = 'System')",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "generate_html",
            "signature": "(mermaid_code, architecture, app_name)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10200,
      "y": 0
    }
  },
  {
    "reason": "Generates run generated from specifications.",
    "description": "Creates run generated using LLM. Handles input processing, generation, and output validation.",
    "dependencies": [],
    "priority": 127,
    "filename": "run_generated_python.prompt",
    "filepath": "prompts/run_generated_python.prompt",
    "tags": [
      "generation",
      "module",
      "python"
    ],
    "interface": {
      "type": "module"
    },
    "position": {
      "x": 6880,
      "y": 0
    }
  },
  {
    "reason": "Orchestrates pdd setup with OpenCode- and Antigravity-aware CLI bootstrap and deterministic model configuration.",
    "description": "Runs the interactive pdd setup flow with agentic CLI bootstrapping, API-key discovery, model configuration, multi-provider curation for local routing (prompts which provider(s) pdd --local should use when several are configured, persists the choice to ~/.pdd/setup_preferences.json, backs up before removing rows), .pddrc initialization, and summary output.",
    "dependencies": [
      "cli_detector_python.prompt",
      "cli_branding_python.prompt"
    ],
    "priority": 129,
    "filename": "setup_tool_python.prompt",
    "filepath": "pdd/setup_tool.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_setup",
            "signature": "()",
            "returns": "None"
          }
        ]
      }
    },
    "position": {
      "x": 12000,
      "y": 400
    }
  },
  {
    "reason": "Detects and bootstraps Claude, Gemini/Antigravity, Codex, and OpenCode CLIs for pdd setup.",
    "description": "Detects installed agentic CLIs, bootstraps missing tools, checks OAuth/config/API credentials, and returns setup status rows.",
    "dependencies": [],
    "priority": 130,
    "filename": "cli_detector_python.prompt",
    "filepath": "pdd/cli_detector.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "detect_and_bootstrap_cli",
            "signature": "() -> List[CliBootstrapResult]",
            "returns": "List[CliBootstrapResult]"
          },
          {
            "name": "detect_cli_tools",
            "signature": "() -> None",
            "returns": "None"
          }
        ],
        "dataclasses": [
          {
            "name": "CliBootstrapResult",
            "fields": [
              "cli_name: str",
              "provider: str",
              "cli_path: str",
              "api_key_configured: bool",
              "skipped: bool",
              "cli_version: str"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 12280,
      "y": 580
    }
  },
  {
    "reason": "LLM prompt template for summarizing code and documentation files in ~200 words.",
    "description": "Defines the prompt structure for summarize file. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 131,
    "filename": "summarize_file_LLM.prompt",
    "filepath": "prompts/summarize_file_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 600,
      "y": 660
    }
  },
  {
    "reason": "Provides text UI for interactive sync operations.",
    "description": "Terminal user interface for controlling and monitoring sync workflows. Supports interactive mode.",
    "dependencies": [],
    "priority": 132,
    "filename": "sync_tui_python.prompt",
    "filepath": "pdd/sync_tui.py",
    "tags": [
      "module",
      "python",
      "sync"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "show_exit_animation",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, basename: str, budget: Optional[float], worker_func: Callable[[], Any], function_name_ref: List[str], cost_ref: List[float], prompt_path_ref: List[str], code_path_ref: List[str], example_path_ref: List[str], tests_path_ref: List[str], prompt_color_ref: List[str], code_color_ref: List[str], example_color_ref: List[str], tests_color_ref: List[str], stop_event: threading.Event, progress_callback_ref: Optional[List[Optional[Callable[[int, int], None]]]] = None)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "compose",
            "signature": "(self) -> ComposeResult",
            "returns": "ComposeResult",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "on_button_pressed",
            "signature": "(self, event: Button.Pressed) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "action_confirm_yes",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "action_confirm_no",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, basename: str, budget: Optional[float], worker_func: Callable[[], Any], function_name_ref: List[str], cost_ref: List[float], prompt_path_ref: List[str], code_path_ref: List[str], example_path_ref: List[str], tests_path_ref: List[str], prompt_color_ref: List[str], code_color_ref: List[str], example_color_ref: List[str], tests_color_ref: List[str], stop_event: threading.Event, progress_callback_ref: Optional[List[Optional[Callable[[int, int], None]]]] = None)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "compose",
            "signature": "(self) -> ComposeResult",
            "returns": "ComposeResult",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "on_mount",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "on_button_pressed",
            "signature": "(self, event: Button.Pressed) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "on_input_submitted",
            "signature": "(self, event: Input.Submitted) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "action_cancel",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, basename: str, budget: Optional[float], worker_func: Callable[[], Any], function_name_ref: List[str], cost_ref: List[float], prompt_path_ref: List[str], code_path_ref: List[str], example_path_ref: List[str], tests_path_ref: List[str], prompt_color_ref: List[str], code_color_ref: List[str], example_color_ref: List[str], tests_color_ref: List[str], stop_event: threading.Event, progress_callback_ref: Optional[List[Optional[Callable[[int, int], None]]]] = None)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "readable",
            "signature": "(self) -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "writable",
            "signature": "(self) -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "readline",
            "signature": "(self, limit: int = -1) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "read",
            "signature": "(self, size: int = -1) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "set_prompt",
            "signature": "(self, prompt: str) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, basename: str, budget: Optional[float], worker_func: Callable[[], Any], function_name_ref: List[str], cost_ref: List[float], prompt_path_ref: List[str], code_path_ref: List[str], example_path_ref: List[str], tests_path_ref: List[str], prompt_color_ref: List[str], code_color_ref: List[str], example_color_ref: List[str], tests_color_ref: List[str], stop_event: threading.Event, progress_callback_ref: Optional[List[Optional[Callable[[int, int], None]]]] = None)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "write",
            "signature": "(self, s: str) -> int",
            "returns": "int",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "flush",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "captured_logs",
            "signature": "(self) -> List[str]",
            "returns": "List[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, basename: str, budget: Optional[float], worker_func: Callable[[], Any], function_name_ref: List[str], cost_ref: List[float], prompt_path_ref: List[str], code_path_ref: List[str], example_path_ref: List[str], tests_path_ref: List[str], prompt_color_ref: List[str], code_color_ref: List[str], example_color_ref: List[str], tests_color_ref: List[str], stop_event: threading.Event, progress_callback_ref: Optional[List[Optional[Callable[[int, int], None]]]] = None)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "write",
            "signature": "(self, s: str) -> int",
            "returns": "int",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "flush",
            "signature": "(self)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, basename: str, budget: Optional[float], worker_func: Callable[[], Any], function_name_ref: List[str], cost_ref: List[float], prompt_path_ref: List[str], code_path_ref: List[str], example_path_ref: List[str], tests_path_ref: List[str], prompt_color_ref: List[str], code_color_ref: List[str], example_color_ref: List[str], tests_color_ref: List[str], stop_event: threading.Event, progress_callback_ref: Optional[List[Optional[Callable[[int, int], None]]]] = None)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "captured_logs",
            "signature": "(self) -> List[str]",
            "returns": "List[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "compose",
            "signature": "(self) -> ComposeResult",
            "returns": "ComposeResult",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "on_mount",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "run_worker_task",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "update_animation",
            "signature": "(self) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "request_confirmation",
            "signature": "(self, message: str, title: str = 'Confirmation Required') -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "request_input",
            "signature": "(self, message: str, title: str = 'Input Required', default: str = '', password: bool = False) -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "schedule_modal",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "schedule_modal",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 14680,
      "y": 220
    }
  },
  {
    "reason": "Manages PDD prompt template discovery and loading.",
    "description": "Registers and retrieves prompt templates. Supports template inheritance and versioning.",
    "dependencies": [],
    "priority": 133,
    "filename": "template_registry_python.prompt",
    "filepath": "pdd/template_registry.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "list_templates",
            "signature": "(filter_tag: Optional[str] = None) -> List[Dict[str, Any]]",
            "returns": "List[Dict[str, Any]]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "load_template",
            "signature": "(name: str) -> Dict[str, Any]",
            "returns": "Dict[str, Any]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "show_template",
            "signature": "(name: str) -> Dict[str, Any]",
            "returns": "Dict[str, Any]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "copy_template",
            "signature": "(name: str, dest_dir: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "alias",
            "signature": "(self) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "register",
            "signature": "(meta: TemplateMeta) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10200,
      "y": 2200
    }
  },
  {
    "reason": "Maps generated code lines back to source prompt lines.",
    "description": "Traces code locations to their originating prompt sections. Useful for debugging and understanding generation.",
    "dependencies": [],
    "priority": 135,
    "filename": "trace_LLM.prompt",
    "filepath": "prompts/trace_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 880,
      "y": 880
    }
  },
  {
    "reason": "LLM prompt template for trim results operations.",
    "description": "Defines the prompt structure for trim results. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 136,
    "filename": "trim_results_LLM.prompt",
    "filepath": "prompts/trim_results_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 1160,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for trim results start operations.",
    "description": "Defines the prompt structure for trim results start. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [],
    "priority": 137,
    "filename": "trim_results_start_LLM.prompt",
    "filepath": "prompts/trim_results_start_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 600,
      "y": 1100
    }
  },
  {
    "reason": "Detects whether LLM output is complete or needs continuation.",
    "description": "Analyzes LLM responses to determine if code is complete. Checks for truncation markers, unclosed blocks, and incomplete functions.",
    "dependencies": [],
    "priority": 138,
    "filename": "unfinished_prompt_LLM.prompt",
    "filepath": "prompts/unfinished_prompt_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 880,
      "y": 1320
    }
  },
  {
    "reason": "Updates LLM model cost data.",
    "description": "Fetches and stores current pricing for LLM models. Keeps cost tracking accurate.",
    "dependencies": [],
    "priority": 139,
    "filename": "update_model_costs_python.prompt",
    "filepath": "pdd/update_model_costs.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "update_model_data",
            "signature": "(csv_path: str) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "main",
            "signature": "()",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 10760,
      "y": 1760
    }
  },
  {
    "reason": "Updates prompts to reflect code modifications.",
    "description": "Back-propagates code changes into the source prompt. Keeps prompts synchronized with manually edited code.",
    "dependencies": [],
    "priority": 140,
    "filename": "update_prompt_LLM.prompt",
    "filepath": "prompts/update_prompt_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 1200,
      "y": 0
    }
  },
  {
    "reason": "Returns comment syntax for a programming language.",
    "description": "Provides language-specific comment delimiters. Supports single-line and multi-line comments.",
    "dependencies": [],
    "priority": 141,
    "filename": "get_comment_python.prompt",
    "filepath": "pdd/get_comment.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_comment",
            "signature": "(language: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 11080,
      "y": 400
    }
  },
  {
    "reason": "Module that be instructions on how to generate python code for the bug_main.",
    "description": "Implements functionality to be instructions on how to generate python code for the bug_main. Integrates with PDD workflow.",
    "dependencies": [
      "preprocess_main_python.prompt"
    ],
    "priority": 142,
    "filename": "main_gen_prompt.prompt",
    "filepath": "prompts/main_gen_prompt.prompt",
    "tags": [
      "config"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 12000,
      "y": 2920
    }
  },
  {
    "reason": "Module that test the prompt for a given prompt file.",
    "description": "Implements functionality to test the prompt for a given prompt file. Integrates with PDD workflow.",
    "dependencies": [],
    "priority": 143,
    "filename": "prompt_tester_python.prompt",
    "filepath": "prompts/prompt_tester_python.prompt",
    "tags": [
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module"
    },
    "position": {
      "x": 6280,
      "y": 1020
    }
  },
  {
    "reason": "Provides real-time progress visualization for sync.",
    "description": "Displays animated progress indicators, step status, and cost tracking during sync operations.",
    "dependencies": [],
    "priority": 144,
    "filename": "sync_animation_python.prompt",
    "filepath": "pdd/sync_animation.py",
    "tags": [
      "module",
      "python",
      "sync"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "sync_animation",
            "signature": "(function_name_ref: List[str], stop_event: threading.Event, basename: str, cost_ref: List[float], budget: Optional[float], prompt_color: List[str], code_color: List[str], example_color: List[str], tests_color: List[str], prompt_path_ref: List[str], code_path_ref: List[str], example_path_ref: List[str], tests_path_ref: List[str]) -> None",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "__init__",
            "signature": "(self, basename: str, budget: Optional[float])",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "update_dynamic_state",
            "signature": "(self, function_name: str, cost: float, prompt_path: str, code_path: str, example_path: str, tests_path: str)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "set_box_colors",
            "signature": "(self, prompt_color: str, code_color: str, example_color: str, tests_color: str)",
            "returns": "None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_elapsed_time_str",
            "signature": "(self) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_emoji_for_box",
            "signature": "(self, box_name: str, blink_on: bool) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 15000,
      "y": 1060
    }
  },
  {
    "reason": "LLM prompt template for xml convertor operations.",
    "description": "Defines the prompt structure for xml convertor. Invoked by the corresponding Python module to format LLM requests.",
    "dependencies": [
      "split_LLM.prompt"
    ],
    "priority": 145,
    "filename": "xml_convertor_LLM.prompt",
    "filepath": "prompts/xml_convertor_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 9000,
      "y": 2220
    }
  },
  {
    "reason": "CLI entry point for the detect change command.",
    "description": "Command-line interface for detect change. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 146,
    "filename": "detect_change_main_python.prompt",
    "filepath": "pdd/detect_change_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "detect_change_main",
            "signature": "(ctx: click.Context, prompt_files: List[str], change_file: str, output: Optional[str]) -> Tuple[List[Dict], float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 13200,
      "y": 2220
    }
  },
  {
    "reason": "CLI entry point for the split command.",
    "description": "Command-line interface for split. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 147,
    "filename": "split_main_python.prompt",
    "filepath": "pdd/split_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "split_main",
            "signature": "(ctx: click.Context, input_prompt_file: str, input_code_file: str, example_code_file: str, output_sub: Optional[str], output_modified: Optional[str]) -> Tuple[Dict[str, str], float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 13480,
      "y": 1200
    }
  },
  {
    "reason": "CLI entry point for the unified sync workflow, handling both single-module and global project-wide synchronization.",
    "description": "Orchestrates the complete PDD workflow: auto-deps, generate, example, crash, verify, test, fix, update. The primary command for most users. In one-session mode, the generation phase catches ArchitectureConformanceError, PublicSurfaceRegressionError, and TestChurnError (issue #1012), accumulates failed-attempt total_cost/model_name into pre_cost, retries code_generator_main up to MAX_CONFORMANCE_ATTEMPTS with PDD_REPAIR_DIRECTIVE set to the previous failure's repair_directive while budget remains, short-circuits on repeated failure signatures (missing-symbol set / removed-symbol set / (rounded churn ratio, pre_line_count)) and additionally caps TestChurnError retries at one attempt, reuses the already-resolved .pddrc context_override/strength/temperature/context_config across retries (no mid-loop .pddrc re-resolution), relies on code_generator_main reading the pre-generation code file from disk for the public-surface snapshot, restores the prior environment value, passes only remaining budget after generation into one-session sync, and prints build_conformance_hard_failure_from_error / build_public_surface_hard_failure_from_error / build_test_churn_hard_failure_from_error diagnostics before re-raising the final failure. _auto_submit_example skips when CloudConfig.is_running_in_cloud() is true, calls CloudConfig.ensure_default_env() before lower-level cached JWT lookup, and wraps the asyncio JWT call in asyncio.wait_for so a stuck Device Flow on a dev machine cannot eat the whole sync.",
    "dependencies": [
      "core/cloud_python.prompt",
      "code_generator_main_python.prompt",
      "agentic_sync_runner_python.prompt",
      "compressed_sync_context_python.prompt"
    ],
    "priority": 148,
    "filename": "sync_main_python.prompt",
    "filepath": "pdd/sync_main.py",
    "tags": [
      "cli",
      "python",
      "sync"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "sync_main",
            "signature": "(ctx: click.Context, basename: str, max_attempts: Optional[int], budget: Optional[float], skip_verify: bool, skip_tests: bool, target_coverage: float, dry_run: bool, no_steer: bool = False, steer_timeout: Optional[float] = None, agentic_mode: bool = False, one_session: bool = False, compress: bool = False, evidence: bool = False, snapshot_context: bool = False, compressed_context: Optional[bool] = None) -> Tuple[Dict[str, Any], float, str]",
            "returns": "Tuple[Dict[str, Any], float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 12000,
      "y": 3240
    }
  },
  {
    "reason": "Analyzes prompt changes and incrementally patches existing code or triggers full regeneration.",
    "description": "Detects changes between prompt versions and generates minimal code patches. More efficient than full regeneration for small modifications.",
    "dependencies": [
      "llm_invoke_python.prompt",
      "preprocess_python.prompt",
      "postprocess_python.prompt"
    ],
    "priority": 149,
    "filename": "incremental_code_generator_python.prompt",
    "filepath": "pdd/incremental_code_generator.py",
    "tags": [
      "generation",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "incremental_code_generator",
            "signature": "(original_prompt: str, new_prompt: str, existing_code: str, language: str, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: float = 0.25, force_incremental: bool = False, verbose: bool = False, preprocess_prompt: bool = True) -> Tuple[Optional[str], bool, float, str]",
            "returns": "Tuple[Optional[str], bool, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 6600,
      "y": 1200
    }
  },
  {
    "reason": "CLI entry point for the change command.",
    "description": "Command-line interface for change. Parses arguments, orchestrates the workflow, and formats output.",
    "dependencies": [],
    "priority": 150,
    "filename": "change_main_python.prompt",
    "filepath": "pdd/change_main.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "change_main",
            "signature": "(ctx: click.Context, change_prompt_file: str, input_code: str, input_prompt_file: Optional[str], output: Optional[str], use_csv: bool, budget: float) -> Tuple[str, float, str]"
          }
        ]
      }
    },
    "position": {
      "x": 11080,
      "y": 1640
    }
  },
  {
    "reason": "Fixes code based on failing unit test output.",
    "description": "Analyzes test failures and generates code or test corrections. Determines whether to fix the code or the test based on error patterns.",
    "dependencies": [
      "code_generator_python.prompt",
      "conflicts_in_prompts_python.prompt",
      "context_generator_python.prompt",
      "detect_change_python.prompt"
    ],
    "priority": 151,
    "filename": "fix_errors_from_unit_tests_LLM.prompt",
    "filepath": "prompts/fix_errors_from_unit_tests_LLM.prompt",
    "tags": [
      "config",
      "fix",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 9600,
      "y": 3100
    }
  },
  {
    "reason": "Orchestrates the multi-step agentic architecture workflow with gates, validation, state persistence, and context accumulation.",
    "description": "Multi-step orchestrator for architecture generation from a GitHub issue PRD. Runs the gated Step 1-13 workflow, including complexity and codebase scans, completeness gates, architecture self-review, shared context-doc merge, prompt generation, cross-file and cross-issue consistency checks, validation/fix loops, state persistence, target-directory support, and post-processing via render_mermaid.",
    "dependencies": [],
    "priority": 152,
    "filename": "agentic_architecture_orchestrator_python.prompt",
    "filepath": "pdd/agentic_architecture_orchestrator.py",
    "tags": [
      "module",
      "python",
      "agentic"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_architecture_orchestrator",
            "signature": "(issue_url: str, issue_content: str, repo_owner: str, repo_name: str, issue_number: int, issue_author: str, issue_title: str, *, cwd: Path, verbose: bool, quiet: bool, timeout_adder: float, use_github_state: bool, skip_prompts: bool, target_dir: Optional[str], force_single: bool, sibling_architectures: str, existing_pddrc: str, existing_architecture: str, related_issues: Optional[List[int]])",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          }
        ]
      }
    },
    "position": {
      "x": 21000,
      "y": 0
    }
  },
  {
    "reason": "Architecture registry: tracks per-issue generation provenance, merges entries, and resolves project root via tiered marker precedence.",
    "description": "Maintains .pdd/architecture_registry.json (provenance of which modules came from which GitHub issue), provides architecture-merging and topological priority renumbering, discovers nested architecture.json files, and resolves the project root: PDD markers (.pddrc/.pdd/ Tier A or sources/ + PRD/spec markdown Tier B) and .git (Tier C) are project boundaries, and the nearest boundary found while walking upward wins. It skips Path.home() and system temp roots (/tmp, TMPDIR, tempfile.gettempdir()) for PDD-marker checks so user-global or CI cache markers do not anchor unrelated projects. A self-contained pdd project nested inside an unrelated outer git repo is correctly identified as its own project root, while an outer PDD marker cannot override a nearer inner git repository.",
    "dependencies": [],
    "priority": 208,
    "filename": "architecture_registry_python.prompt",
    "filepath": "pdd/architecture_registry.py",
    "tags": [
      "module",
      "python",
      "architecture",
      "registry"
    ],
    "interface": {
      "type": "module",
      "module": {
        "constants": [
          {
            "name": "BUNDLED_SAMPLE_TOPLEVEL_DIRS",
            "type": "FrozenSet[str]"
          }
        ],
        "functions": [
          {
            "name": "extract_modules",
            "signature": "(data: Any)",
            "returns": "List[Dict[str, Any]]"
          },
          {
            "name": "extract_prd_files",
            "signature": "(data: Any)",
            "returns": "List[str]"
          },
          {
            "name": "load_registry",
            "signature": "(project_root: Path)",
            "returns": "dict"
          },
          {
            "name": "save_registry",
            "signature": "(project_root: Path, registry: dict)",
            "returns": "None"
          },
          {
            "name": "record_generation",
            "signature": "(project_root: Path, issue_number: int, issue_url: str, modules_added: List[str], modules_updated: List[str], target_dir: Optional[str] = None)",
            "returns": "None"
          },
          {
            "name": "merge_architecture",
            "signature": "(existing_arch: List[dict], new_arch: List[dict], issue_number: int, issue_url: str)",
            "returns": "Tuple[List[dict], dict]"
          },
          {
            "name": "find_architecture_for_project",
            "signature": "(project_root: Path, *, skip_bundled_sample_arch: bool = True)",
            "returns": "List[Path]"
          },
          {
            "name": "find_project_root",
            "signature": "(start: Optional[Path] = None)",
            "returns": "Path"
          },
          {
            "name": "find_git_toplevel",
            "signature": "(start: Optional[Path] = None)",
            "returns": "Optional[Path]"
          },
          {
            "name": "load_combined_architecture_data",
            "signature": "(project_root: Path, *, skip_bundled_sample_arch: bool = True)",
            "returns": "Tuple[Optional[List[Dict[str, Any]]], Path]"
          },
          {
            "name": "get_modules_for_issue",
            "signature": "(arch_data: List[dict], issue_number: int)",
            "returns": "List[dict]"
          }
        ]
      }
    }
  },
  {
    "reason": "CLI entry point for agentic architecture workflow: detects GitHub issue URLs, fetches issue content, invokes orchestrator.",
    "description": "Entry point module for agentic architecture generation. Detects GitHub issue URLs, fetches issue content and comments via gh API, ensures repository context, then invokes the orchestrator. Also wraps guarded incremental PRD-to-architecture propagation for issue URLs and local PRD files, including shell-safe target-directory validation, repo-relative status paths, subproject sync hints, and GitHub status comment redaction.",
    "dependencies": [
      "agentic_architecture_orchestrator_python.prompt",
      "incremental_prd_architecture_python.prompt",
      "architecture_registry_python.prompt"
    ],
    "priority": 209,
    "filename": "agentic_architecture_python.prompt",
    "filepath": "pdd/agentic_architecture.py",
    "tags": [
      "module",
      "python",
      "agentic"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_architecture",
            "signature": "(issue_url: str, *, verbose: bool, quiet: bool, timeout_adder: float, use_github_state: bool, skip_prompts: bool, target_dir: Optional[str], force_single: bool, project_root: Optional[str])",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          },
          {
            "name": "run_incremental_architecture",
            "signature": "(prd_source: str, *, dry_run: bool, verbose: bool, quiet: bool, use_github_state: bool, target_dir: Optional[str], strength: float, temperature: float, time: float, project_root: Optional[str])",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          },
          {
            "name": "_is_github_issue_url",
            "signature": "(url: str)",
            "returns": "bool"
          },
          {
            "name": "_parse_github_url",
            "signature": "(url: str)",
            "returns": "Optional[Tuple[str, str, int]]"
          }
        ]
      }
    },
    "position": {
      "x": 21000,
      "y": 620
    }
  },
  {
    "reason": "CLI commands for the Generate Suite: generate, test, and example, with GitHub URL detection for agentic modes.",
    "description": "Implements the pdd generate, test, and example CLI commands. Routes standard prompt-to-code generation, legacy incremental code patching, explicit experimental incremental PRD propagation, full agentic architecture generation, context examples, and agentic/manual test generation while preserving dry-run and GitHub-state guardrails.",
    "dependencies": [
      "agentic_architecture_python.prompt",
      "agentic_test_python.prompt",
      "cmd_test_main_python.prompt",
      "code_generator_main_python.prompt",
      "context_generator_main_python.prompt",
      "operation_log_python.prompt",
      "template_registry_python.prompt",
      "track_cost_python.prompt",
      "user_story_tests_python.prompt"
    ],
    "priority": 234,
    "filename": "commands/generate_python.prompt",
    "filepath": "pdd/commands/generate.py",
    "tags": [
      "module",
      "python",
      "cli",
      "generation"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "generate",
            "description": "Generate code from a prompt, patch existing code incrementally, or run guarded agentic architecture generation with explicit experimental PRD propagation opt-in."
          },
          {
            "name": "test",
            "description": "Generate or enhance unit tests, or generate/link prompt user stories."
          },
          {
            "name": "example",
            "description": "Generate context examples from a prompt and code file."
          }
        ]
      }
    },
    "position": {
      "x": 21400,
      "y": 620
    }
  },
  {
    "reason": "LLM prompt template for guarded incremental PRD architecture patches.",
    "description": "Converts a PRD diff, current architecture.json, and validation retry feedback into a minimal structured ArchitecturePatch for deterministic application.",
    "dependencies": [],
    "priority": 207,
    "filename": "incremental_prd_architecture_patch_LLM.prompt",
    "filepath": "prompts/incremental_prd_architecture_patch_LLM.prompt",
    "tags": [
      "llm",
      "architecture",
      "incremental"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": [
          "PRD_SOURCE",
          "PRD_DIFF",
          "CURRENT_ARCHITECTURE",
          "VALIDATION_ERRORS"
        ]
      }
    },
    "position": {
      "x": 20720,
      "y": 1240
    }
  },
  {
    "reason": "Incrementally propagates PRD changes into architecture.json and affected prompt Requirements using validated targeted patches.",
    "description": "Reads a PRD diff from hash/provenance metadata and ignored local raw-baseline cache, asks the LLM for a structured architecture patch, validates dependency, prompt filename, filepath, and file invariants deterministically, rejects unsafe or secret-like code filepaths before reads, retries invalid patches with explicit validation feedback, writes targeted architecture updates atomically, validates prompt previews under the write lock to avoid lost edits, syncs prompt metadata tags, creates prompts for new modules, deletes prompts for removed modules, updates prompt Requirements through detect_change/change, and avoids persisting raw PRD or GitHub issue/comment text in tracked metadata.",
    "dependencies": [
      "architecture_sync_python.prompt",
      "change_python.prompt",
      "detect_change_python.prompt",
      "incremental_prd_architecture_patch_LLM.prompt"
    ],
    "priority": 208,
    "filename": "incremental_prd_architecture_python.prompt",
    "filepath": "pdd/incremental_prd_architecture.py",
    "tags": [
      "module",
      "python",
      "architecture",
      "incremental"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_incremental_prd_propagation",
            "signature": "(prd_source: str, *, architecture_path: Path | str = \"architecture.json\", prompts_dir: Path | str = \"prompts\", project_root: Path | str | None = None, issue_content: Optional[str] = None, dry_run: bool = False, verbose: bool = False, quiet: bool = False, strength: float = DEFAULT_STRENGTH, temperature: float = 0.0, time: float = DEFAULT_TIME, max_patch_attempts: int = 3, update_prompt_requirements: bool = True, generate_new_prompts: bool = True) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple of success flag, message, cost, model, and changed files"
          },
          {
            "name": "apply_architecture_patch",
            "signature": "(raw_architecture: Any, patch: ArchitecturePatch) -> PatchApplication",
            "returns": "Validated in-memory architecture patch application result"
          }
        ]
      }
    },
    "position": {
      "x": 21000,
      "y": 1240
    }
  },
  {
    "reason": "Analyzes PRD from GitHub issue: extracts features, tech stack, requirements, and identifies gaps for architecture generation.",
    "description": "Step 1 of the agentic architecture workflow. Receives GitHub issue content (PRD), extracts structured information including project title, goals, features, non-functional requirements, and tech stack hints. Identifies ambiguities and assesses PRD completeness.",
    "dependencies": [],
    "priority": 154,
    "filename": "agentic_arch_step1_analyze_prd_LLM.prompt",
    "filepath": "prompts/agentic_arch_step1_analyze_prd_LLM.prompt",
    "tags": [
      "llm",
      "agentic",
      "architecture"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "analyze_prd",
            "signature": "(issue_content: str)",
            "returns": "str (structured PRD summary or 'PRD Content Insufficient')"
          }
        ]
      }
    },
    "position": {
      "x": 20400,
      "y": 0
    }
  },
  {
    "reason": "Deep analysis of PRD: feature decomposition, module boundaries, shared concerns, and tech stack determination.",
    "description": "Step 2 of the agentic architecture workflow. Performs deep analysis of the PRD to identify module boundaries, shared concerns (auth, error handling, validation), and determines the complete tech stack. Outputs module candidates with responsibilities.",
    "dependencies": [
      "agentic_arch_step1_analyze_prd_LLM.prompt"
    ],
    "priority": 155,
    "filename": "agentic_arch_step2_analyze_LLM.prompt",
    "filepath": "prompts/agentic_arch_step2_analyze_LLM.prompt",
    "tags": [
      "llm",
      "agentic",
      "architecture"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "deep_analyze",
            "signature": "(issue_content: str, step1_output: str)",
            "returns": "str (structured analysis with module candidates and tech stack)"
          }
        ]
      }
    },
    "position": {
      "x": 20400,
      "y": 400
    }
  },
  {
    "reason": "Researches tech stack documentation, project structure conventions, and best practices via web search.",
    "description": "Step 3 of the agentic architecture workflow. Researches each technology in the identified stack, finding official documentation URLs, project structure conventions, code patterns, and best practices. Collects URLs for use as context_urls in architecture.json.",
    "dependencies": [
      "agentic_arch_step2_analyze_LLM.prompt"
    ],
    "priority": 156,
    "filename": "agentic_arch_step3_research_LLM.prompt",
    "filepath": "prompts/agentic_arch_step3_research_LLM.prompt",
    "tags": [
      "llm",
      "agentic",
      "architecture"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "research_tech_stack",
            "signature": "(issue_content: str, step1_output: str, step2_output: str)",
            "returns": "str (research findings with documentation URLs)"
          }
        ]
      }
    },
    "position": {
      "x": 20400,
      "y": 800
    }
  },
  {
    "reason": "Recommends the correct issue-driven PDD workflow before a bug, fix, change, or sync command is selected.",
    "description": "Public route recommendation prompt for issue-driven PDD command selection. Gives runtime defect signals priority over prompt/source-truth phrasing, recommends bug -> fix for current runtime symptoms, change -> sync for source-truth/spec/product changes with no current failure, and clarification for ambiguous issues.",
    "dependencies": [],
    "priority": 165,
    "filename": "agentic_issue_route_LLM.prompt",
    "filepath": "prompts/agentic_issue_route_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15240,
      "y": 220
    }
  },
  {
    "reason": "Classifies whether a bug root cause is in code implementation or prompt specification.",
    "description": "Step 5.5 of the agentic bug workflow. Classifies whether a bug root cause is in the code implementation or in the prompt specification itself.",
    "dependencies": [],
    "priority": 166,
    "filename": "agentic_bug_step7_prompt_classification_LLM.prompt",
    "filepath": "prompts/agentic_bug_step7_prompt_classification_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15560,
      "y": 440
    }
  },
  {
    "reason": "Generates E2E tests that verify the bug at a system level.",
    "description": "Step 9 of the agentic bug workflow. Generates and runs end-to-end tests that verify a bug at the system integration level.",
    "dependencies": [],
    "priority": 167,
    "filename": "agentic_bug_step11_e2e_test_LLM.prompt",
    "filepath": "prompts/agentic_bug_step11_e2e_test_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 15880,
      "y": 440
    }
  },
  {
    "reason": "Orchestrates the 13-step agentic change workflow for implementing GitHub issues, with optional clean restart.",
    "description": "Orchestrates the 13-step agentic change workflow. Supports `clean_restart=True` to ignore any persisted solving state for the issue and start a fresh full pdd-issue flow from the target base branch (issue #1149). Hardens JSON/Markdown artifact handling by clearing artifacts before attempts and internal provider retries, persisting only accepted decision JSON, deriving Step 9 manual-review lines exactly from accepted Step 9 JSON, and requiring Step 13 PR success/recovery to validate an OPEN PR in the exact repo/base/head branch with non-empty PR head SHA plus remote/local head SHA alignment even when the provider result is classified as failed; Step 13 clears unaccepted Markdown before posting a trusted success comment after prose synthesis or exact-head recovery. Includes Step 8.5 (pre-flight drift heal) \u2014 detects prompts whose code has drifted and runs `pdd update` per module inside the worktree before Step 9 rewrites the prompts. Includes Step 10.5 (doc-sync contract verifier) \u2014 before Step 10, calls pdd.sync_order.discover_associated_documents to populate the LLM's associated_documents context, using the authoritative changed-file set so Step 9's worktree fallback path cannot bypass discovery when FILES_* markers are missing; after Step 10, enforces that every discovered doc appears in exactly one of ASSOCIATED_DOCS_MODIFIED / ASSOCIATED_DOCS_CONFLICTS / ASSOCIATED_DOCS_UNCHANGED. Silent drops and bucket overlaps are appended as ORCHESTRATOR_POSTCHECK_WARNINGS and routed to Step 11 via step10_output; PDD_STRICT_DOC_SYNC=1 turns violations into hard workflow aborts (issue #739).",
    "dependencies": [
      "architecture_sync_python.prompt",
      "agentic_common_python.prompt",
      "git_porcelain_python.prompt",
      "pre_checkup_gate_python.prompt"
    ],
    "priority": 168,
    "filename": "agentic_change_orchestrator_python.prompt",
    "filepath": "pdd/agentic_change_orchestrator.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_change_orchestrator",
            "signature": "(issue_url, issue_content, repo_owner, repo_name, issue_number, issue_author, issue_title, issue_updated_at, *, cwd, verbose, quiet, timeout_adder, use_github_state, reasoning_time, clean_restart)",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          },
          {
            "name": "_artifacts_dir_for",
            "signature": "(work_dir: Path, issue_number: int) -> Path",
            "returns": "Path"
          },
          {
            "name": "_clear_step_artifacts",
            "signature": "(artifacts_dir: Path, step_num: int, name: str) -> None",
            "returns": "None"
          },
          {
            "name": "_read_step_json",
            "signature": "(artifacts_dir: Path, filename: str) -> Optional[dict]",
            "returns": "Optional[dict]"
          },
          {
            "name": "_read_step_md",
            "signature": "(artifacts_dir: Path, filename: str) -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "_allow_json_repair_rerun",
            "signature": "(step_num: int) -> bool",
            "returns": "bool"
          },
          {
            "name": "_single_provider_attempt_for_step",
            "signature": "(step_num: int) -> bool",
            "returns": "bool"
          },
          {
            "name": "_hard_stop_from_json_with_presence",
            "signature": "(step_num: int, artifacts_dir: Path, name: str) -> Tuple[bool, Optional[str], List[str]]",
            "returns": "Tuple[bool, Optional[str], List[str]]"
          },
          {
            "name": "_valid_pr_url",
            "signature": "(value: Any, repo_owner: str = \"\", repo_name: str = \"\") -> bool",
            "returns": "bool"
          },
          {
            "name": "_git_head_oid",
            "signature": "(cwd: Path) -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "_remote_branch_oid",
            "signature": "(head_branch: str, cwd: Path) -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "_pr_number_from_url",
            "signature": "(pr_url: str, repo_owner: str, repo_name: str) -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "_gh_pr_list_candidates",
            "signature": "(base_args: Sequence[str], cwd: Optional[Path] = None) -> List[dict]",
            "returns": "List[dict]"
          },
          {
            "name": "_pr_url_matches_current_head",
            "signature": "(repo_owner: str, repo_name: str, pr_url: str, head_branch: str, base_branch: str, cwd: Path) -> bool",
            "returns": "bool"
          },
          {
            "name": "_open_pr_for_head_branch",
            "signature": "(repo_owner: str, repo_name: str, head_branch: str, base_branch: str, cwd: Path) -> Optional[dict]",
            "returns": "Optional[dict]"
          },
          {
            "name": "_check_existing_pr_candidates",
            "signature": "(repo_owner: str, repo_name: str, issue_number: int) -> List[dict]",
            "returns": "List[dict]"
          },
          {
            "name": "_existing_pr_matches_remote_head",
            "signature": "(repo_owner: str, repo_name: str, pr: dict, base_branch: str, cwd: Path) -> bool",
            "returns": "bool"
          },
          {
            "name": "_step9_changed_files_from_json",
            "signature": "(data: dict) -> Tuple[List[str], List[str], List[str], List[str]]",
            "returns": "Tuple[List[str], List[str], List[str], List[str]]"
          },
          {
            "name": "_step13_result_from_json_or_output",
            "signature": "(data: Optional[dict], output: str) -> Tuple[str, str]",
            "returns": "Tuple[str, str]"
          }
        ]
      }
    },
    "position": {
      "x": 14680,
      "y": 1420
    }
  },
  {
    "reason": "Autonomously implements GitHub feature request issues via 13-step workflow with optional clean restart.",
    "description": "Entry point for the agentic change command. Parses issue URL, extracts issue metadata from GitHub, and delegates to the change orchestrator. Supports `clean_restart` to ignore any persisted solving state and start a fresh full pdd-issue flow from the target base branch.",
    "dependencies": [
      "agentic_change_orchestrator_python.prompt",
      "agentic_common_python.prompt"
    ],
    "priority": 169,
    "filename": "agentic_change_python.prompt",
    "filepath": "pdd/agentic_change.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_change",
            "signature": "(issue_url: str, *, verbose: bool = False, quiet: bool = False, timeout_adder: float = 0.0, use_github_state: bool = True, reasoning_time: Optional[float] = None, clean_restart: bool = False) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple[bool, str, float, str, List[str]]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 15600,
      "y": 1600
    }
  },
  {
    "reason": "LLM prompt template for updating architecture metadata after changes.",
    "description": "Step 10 of the agentic change workflow. Updates architecture metadata (PDD tags) for prompts created or modified and updates architecture.json.",
    "dependencies": [
      "llm_invoke_python.prompt",
      "path_resolution_python.prompt"
    ],
    "priority": 170,
    "filename": "agentic_change_step10_architecture_update_LLM.prompt",
    "filepath": "prompts/agentic_change_step10_architecture_update_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 16800,
      "y": 800
    }
  },
  {
    "reason": "LLM prompt template for identifying issues in prompt changes.",
    "description": "Step 11 of the agentic change workflow. Identifies potential issues with prompt and documentation changes as part of a review loop (loops with step 12).",
    "dependencies": [],
    "priority": 171,
    "filename": "agentic_change_step11_identify_issues_LLM.prompt",
    "filepath": "prompts/agentic_change_step11_identify_issues_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 16800,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for fixing issues found in review loop.",
    "description": "Step 12 of the agentic change workflow. Fixes the issues identified by step 11 in the review loop (loops with step 11 up to 5 iterations).",
    "dependencies": [],
    "priority": 172,
    "filename": "agentic_change_step12_fix_issues_LLM.prompt",
    "filepath": "prompts/agentic_change_step12_fix_issues_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "fix"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17080,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for creating a PR from the change workflow.",
    "description": "Step 13 (final step) of the agentic change workflow. Pushes the worktree branch, checks for an existing open PR by head branch in the target repo before creating, creates or updates the pull request, and links it to the original GitHub issue.",
    "dependencies": [],
    "priority": 173,
    "filename": "agentic_change_step13_create_pr_LLM.prompt",
    "filepath": "prompts/agentic_change_step13_create_pr_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17360,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for checking duplicate issues in agentic change workflow.",
    "description": "Step 1 of the agentic change workflow. Checks if a change request issue is a duplicate of an existing GitHub issue.",
    "dependencies": [],
    "priority": 174,
    "filename": "agentic_change_step1_duplicate_LLM.prompt",
    "filepath": "prompts/agentic_change_step1_duplicate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 16800,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for checking if change is already implemented.",
    "description": "Step 2 of the agentic change workflow. Checks if the requested change is already implemented or documented; may terminate the workflow if so.",
    "dependencies": [],
    "priority": 175,
    "filename": "agentic_change_step2_docs_LLM.prompt",
    "filepath": "prompts/agentic_change_step2_docs_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17080,
      "y": 1100
    }
  },
  {
    "reason": "LLM prompt template for researching feature change requests.",
    "description": "Step 3 of the agentic change workflow. Researches the feature change request using web search to better understand requirements.",
    "dependencies": [],
    "priority": 176,
    "filename": "agentic_change_step3_research_LLM.prompt",
    "filepath": "prompts/agentic_change_step3_research_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17400,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for determining if change request needs clarification.",
    "description": "Step 4 of the agentic change workflow. Determines if the change request is clear enough to proceed or if clarifying questions are needed; may stop the workflow.",
    "dependencies": [],
    "priority": 177,
    "filename": "agentic_change_step4_clarify_LLM.prompt",
    "filepath": "prompts/agentic_change_step4_clarify_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17680,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for analyzing documentation changes needed.",
    "description": "Step 5 of the agentic change workflow. Analyzes how documentation should change to support the new feature.",
    "dependencies": [],
    "priority": 178,
    "filename": "agentic_change_step5_docs_change_LLM.prompt",
    "filepath": "prompts/agentic_change_step5_docs_change_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17960,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for identifying affected dev units.",
    "description": "Step 6 of the agentic change workflow. Identifies which dev units (prompt + code + example + test) are involved in implementing the change.",
    "dependencies": [],
    "priority": 179,
    "filename": "agentic_change_step6_devunits_LLM.prompt",
    "filepath": "prompts/agentic_change_step6_devunits_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17400,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for identifying architectural decisions.",
    "description": "Step 7 of the agentic change workflow. Identifies architectural or implementation decisions requiring user input before proceeding; may stop the workflow.",
    "dependencies": [],
    "priority": 180,
    "filename": "agentic_change_step7_architecture_LLM.prompt",
    "filepath": "prompts/agentic_change_step7_architecture_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17680,
      "y": 660
    }
  },
  {
    "reason": "LLM prompt template for analyzing prompt changes.",
    "description": "Step 8 of the agentic change workflow. Analyzes how identified prompts should change to implement the requested feature.",
    "dependencies": [],
    "priority": 181,
    "filename": "agentic_change_step8_analyze_LLM.prompt",
    "filepath": "prompts/agentic_change_step8_analyze_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17960,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for implementing prompt changes in a git worktree.",
    "description": "Step 9 of the agentic change workflow. Implements the prompt changes (modifying .prompt files and documentation) in a git worktree; scoped to prompt/doc edits only.",
    "dependencies": [],
    "priority": 182,
    "filename": "agentic_change_step9_implement_LLM.prompt",
    "filepath": "prompts/agentic_change_step9_implement_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 17400,
      "y": 880
    }
  },
  {
    "reason": "Orchestrates the 11-step agentic E2E fix workflow including post-push CI validation and code cleanup.",
    "description": "Coordinates running unit tests, E2E tests, root cause analysis, E2E test fixing, dev unit identification, unit test creation, test verification, pdd fix execution, CI validation, and a deterministic pre-checkup drift-sync gate.",
    "dependencies": [
      "agentic_bug_orchestrator_python.prompt",
      "ci_validation_python.prompt",
      "pre_checkup_gate_python.prompt"
    ],
    "priority": 183,
    "filename": "agentic_e2e_fix_orchestrator_python.prompt",
    "filepath": "pdd/agentic_e2e_fix_orchestrator.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_e2e_fix_orchestrator",
            "signature": "(issue_url, issue_content, repo_owner, repo_name, issue_number, issue_author, issue_title, *, cwd, timeout_adder=0.0, max_cycles=5, resume=True, verbose=False, quiet=False, use_github_state=True, protect_tests=False, ci_retries=3, skip_ci=False, skip_cleanup=False, reasoning_time=None, clean_restart=False)",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          },
          {
            "name": "push_with_retry",
            "signature": "(cwd: Path, *, remote: str = 'origin', repo_owner: str, repo_name: str, refspec: str = 'HEAD', set_upstream: bool = True, force_with_lease_on_non_fast_forward: bool = True) -> Tuple[bool, str]",
            "returns": "Tuple[bool, str]",
            "sideEffects": [
              "Runs git push <remote> <refspec>; retries with --force-with-lease on non-fast-forward only when force_with_lease_on_non_fast_forward is true",
              "On auth failure, reads a token from PDD_GH_TOKEN_FILE, then GH_TOKEN, GITHUB_TOKEN, and PDD_GITHUB_TOKEN, rewrites the remote URL to inject x-access-token, retries once, and restores the original URL in finally"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 15000,
      "y": 1420
    }
  },
  {
    "reason": "CLI entry point for the 11-step agentic E2E fix workflow; orchestrates GitHub issue data fetching and worktree discovery.",
    "description": "Entry point for the agentic E2E fix command. Parses issue URL, finds associated worktree, and delegates to the E2E fix orchestrator.",
    "dependencies": [],
    "priority": 184,
    "filename": "agentic_e2e_fix_python.prompt",
    "filepath": "pdd/agentic_e2e_fix.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_e2e_fix",
            "signature": "(issue_url, *, timeout_adder=0.0, max_cycles=5, resume=True, force=False, verbose=False, quiet=False, use_github_state=True, protect_tests=False, ci_retries=3, skip_ci=False, skip_cleanup=False, reasoning_time=None, clean_restart=False)",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          }
        ]
      }
    },
    "position": {
      "x": 14680,
      "y": 1820
    }
  },
  {
    "reason": "Step 1 of the agentic E2E fix workflow: runs issue-specific unit tests and fixes failures via pdd fix.",
    "description": "Step 1 of the agentic E2E fix workflow. Runs issue-specific unit tests (created by pdd bug) and fixes failures using pdd fix.",
    "dependencies": [],
    "priority": 185,
    "filename": "agentic_e2e_fix_step1_unit_tests_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step1_unit_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "fix",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19200,
      "y": 0
    }
  },
  {
    "reason": "Step 2 of the agentic E2E fix workflow: runs existing E2E tests to check if they all pass.",
    "description": "Step 2 of the agentic E2E fix workflow. Runs E2E tests and checks if they all pass, serving as the exit condition for the outer fix loop.",
    "dependencies": [],
    "priority": 186,
    "filename": "agentic_e2e_fix_step2_e2e_tests_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step2_e2e_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19480,
      "y": 220
    }
  },
  {
    "reason": "Step 3 of the agentic E2E fix workflow: root causes the E2E test failures.",
    "description": "Step 3 of the agentic E2E fix workflow. Performs root cause analysis on E2E test failures to determine whether the problem is in code, tests, or both.",
    "dependencies": [],
    "priority": 187,
    "filename": "agentic_e2e_fix_step3_root_cause_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step3_root_cause_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "fix"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19760,
      "y": 0
    }
  },
  {
    "reason": "Step 4 of the agentic E2E fix workflow: fixes E2E tests if they are the identified problem.",
    "description": "Step 4 of the agentic E2E fix workflow. Fixes E2E tests themselves if root cause analysis indicates the tests are the problem.",
    "dependencies": [],
    "priority": 188,
    "filename": "agentic_e2e_fix_step4_fix_e2e_tests_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step4_fix_e2e_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19200,
      "y": 440
    }
  },
  {
    "reason": "Step 5 of the agentic E2E fix workflow: identifies which dev units are involved in test failures.",
    "description": "Step 5 of the agentic E2E fix workflow. Identifies which dev units (modules) are responsible for remaining test failures.",
    "dependencies": [],
    "priority": 189,
    "filename": "agentic_e2e_fix_step5_identify_devunits_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step5_identify_devunits_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "fix"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19480,
      "y": 660
    }
  },
  {
    "reason": "Step 6 of the agentic E2E fix workflow: creates or appends unit tests for the involved dev units.",
    "description": "Step 6 of the agentic E2E fix workflow. Creates or appends unit tests for the dev units identified as causing E2E failures.",
    "dependencies": [],
    "priority": 190,
    "filename": "agentic_e2e_fix_step6_create_unit_tests_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step6_create_unit_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19760,
      "y": 440
    }
  },
  {
    "reason": "Step 7 of the agentic E2E fix workflow: verifies that the newly created unit tests detect the bugs.",
    "description": "Step 7 of the agentic E2E fix workflow. Verifies that the created unit tests actually fail on the current buggy code, confirming they detect the bug.",
    "dependencies": [],
    "priority": 191,
    "filename": "agentic_e2e_fix_step7_verify_tests_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step7_verify_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19800,
      "y": 220
    }
  },
  {
    "reason": "Step 8 of the agentic E2E fix workflow: runs pdd fix sequentially on dev units identified during E2E failure analysis.",
    "description": "Step 8 of the agentic E2E fix workflow. Runs pdd fix sequentially on dev units identified during E2E failure analysis to fix the underlying bugs.",
    "dependencies": [],
    "priority": 192,
    "filename": "agentic_e2e_fix_step8_run_pdd_fix_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step8_run_pdd_fix_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "fix",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 20080,
      "y": 0
    }
  },
  {
    "reason": "Step 9 of the agentic E2E fix workflow: performs final verification and controls the outer loop.",
    "description": "Step 9 of the agentic E2E fix workflow. Performs final verification and determines if another fix cycle is needed; controls the outer loop.",
    "dependencies": [],
    "priority": 193,
    "filename": "agentic_e2e_fix_step9_verify_all_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step9_verify_all_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "fix"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 20360,
      "y": 1100
    }
  },
  {
    "reason": "LLM prompt template for generating unit tests from prompt specification.",
    "description": "Generates unit tests for a PDD project based on the prompt specification and code under test.",
    "dependencies": [],
    "priority": 194,
    "filename": "agentic_test_generate_LLM.prompt",
    "filepath": "prompts/agentic_test_generate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18000,
      "y": 0
    }
  },
  {
    "reason": "Generates tests using agentic single-pass execution with file change detection.",
    "description": "Runs an agentic CLI to generate test files. Records file mtimes before/after agent execution, detects changed files, reads generated test content. Supports JSON metadata extraction from agent output.",
    "dependencies": [],
    "priority": 195,
    "filename": "agentic_test_generate_python.prompt",
    "filepath": "pdd/agentic_test_generate.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_test_generate",
            "signature": "(prompt_file: Path, code_file: Path, output_test_file: Path, *, verbose: bool = False, quiet: bool = False, repair_directive: str | None = None) -> TestResult",
            "returns": "TestResult",
            "sideEffects": [
              "Reads working-tree content of pre-existing test-like files (matching `_is_test_output_path`) under `project_root` BEFORE the agent runs so the alt-path churn check has a baseline that covers tracked-clean, tracked-dirty, and untracked alt-path tests uniformly; on `TestChurnError` restores the snapshotted pre-run content to the alt path so the repair loop sees the actual pre-sync state (NOT `git show HEAD`, which would clobber dirty local edits)."
            ]
          },
          {
            "name": "_get_file_mtimes",
            "signature": "(root: Path) -> dict[Path, float]",
            "returns": "dict[Path, float]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_snapshot_pre_test_contents",
            "signature": "(project_root: Path, candidate_paths: Iterable[Path]) -> dict[Path, str]",
            "returns": "dict[Path, str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_extract_json_from_text",
            "signature": "(text: str) -> dict[str, Any] | None",
            "returns": "dict[str, Any] | None",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_read_generated_test_file",
            "signature": "(test_file: Path) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_detect_changed_files",
            "signature": "(before, after, project_root) -> list[str]",
            "returns": "list[str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 13760,
      "y": 2520
    }
  },
  {
    "reason": "Orchestrates the 18-step agentic test workflow with exploratory testing and plan validation.",
    "description": "Coordinates duplicate checking, docs review, clarification, frontend detection, test planning, plan enhancement, coverage assessment, manual testing, regression tests, test generation, plan validation, and PR submission.",
    "dependencies": [
      "agentic_common_python.prompt",
      "load_prompt_template_python.prompt"
    ],
    "priority": 196,
    "filename": "agentic_test_orchestrator_python.prompt",
    "filepath": "pdd/agentic_test_orchestrator.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_test_orchestrator",
            "signature": "(issue_url: str, issue_content: str, repo_owner: str, repo_name: str, issue_number: int, issue_author: str, issue_title: str, *, cwd: Path, verbose: bool = False, quiet: bool = False, timeout_adder: float = 0.0, use_github_state: bool = True, clean_restart: bool = False) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          }
        ]
      }
    },
    "position": {
      "x": 14960,
      "y": 2080
    }
  },
  {
    "reason": "Autonomously generates UI tests from GitHub issue descriptions.",
    "description": "Entry point for the agentic test command. Parses issue URL, extracts issue metadata, and delegates to the test orchestrator. Supports both Playwright and Cypress test frameworks.",
    "dependencies": [],
    "priority": 197,
    "filename": "agentic_test_python.prompt",
    "filepath": "pdd/agentic_test.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_test",
            "signature": "(issue_url: str, *, verbose: bool = False, quiet: bool = False, timeout_adder: float = 0.0, use_github_state: bool = True, clean_restart: bool = False) -> Tuple[bool, str, float, str, List[str]]",
            "returns": "Tuple[bool, str, float, str, List[str]]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 14960,
      "y": 2480
    }
  },
  {
    "reason": "LLM prompt template for checking duplicate test generation requests.",
    "description": "Step 1 of the agentic test workflow. Checks if a test generation request is a duplicate of an existing issue.",
    "dependencies": [],
    "priority": 198,
    "filename": "agentic_test_step1_duplicate_LLM.prompt",
    "filepath": "prompts/agentic_test_step1_duplicate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18280,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for researching codebase for test generation.",
    "description": "Step 2 of the agentic test workflow. Researches the codebase to understand what needs to be tested.",
    "dependencies": [],
    "priority": 199,
    "filename": "agentic_test_step2_docs_LLM.prompt",
    "filepath": "prompts/agentic_test_step2_docs_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18560,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for assessing test generation feasibility.",
    "description": "Step 3 of the agentic test workflow. Assesses whether there is sufficient information to create comprehensive tests; may terminate the workflow.",
    "dependencies": [],
    "priority": 200,
    "filename": "agentic_test_step3_clarify_LLM.prompt",
    "filepath": "prompts/agentic_test_step3_clarify_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18000,
      "y": 660
    }
  },
  {
    "reason": "LLM prompt template for detecting test type and framework.",
    "description": "Step 4 of the agentic test workflow. Identifies the test type (frontend/backend) and determines the appropriate testing framework.",
    "dependencies": [],
    "priority": 201,
    "filename": "agentic_test_step4_detect_frontend_LLM.prompt",
    "filepath": "prompts/agentic_test_step4_detect_frontend_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18280,
      "y": 440
    }
  },
  {
    "reason": "LLM prompt template for creating comprehensive test plans.",
    "description": "Step 5 of the agentic test workflow. Creates a comprehensive test plan and verifies it is achievable; may terminate if blocked by missing information.",
    "dependencies": [],
    "priority": 202,
    "filename": "agentic_test_step5_test_plan_LLM.prompt",
    "filepath": "prompts/agentic_test_step5_test_plan_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18600,
      "y": 220
    }
  },
  {
    "reason": "Generates comprehensive test code from enhanced test plan including contract and accessibility tests.",
    "description": "Step 12 of the 18-step agentic test workflow. Generates comprehensive test code from the enhanced plan including behavioral, contract, and accessibility tests.",
    "dependencies": [],
    "priority": 203,
    "filename": "agentic_test_step6_generate_tests_LLM.prompt",
    "filepath": "prompts/agentic_test_step6_generate_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18880,
      "y": 0
    }
  },
  {
    "reason": "Runs generated tests with multi-cwd validation to catch CI-mirror path brittleness.",
    "description": "Step 13 of the 18-step agentic test workflow. Runs the generated tests with multi-cwd validation and reports results.",
    "dependencies": [],
    "priority": 204,
    "filename": "agentic_test_step7_run_tests_LLM.prompt",
    "filepath": "prompts/agentic_test_step7_run_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 19160,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt template for iteratively fixing failing tests.",
    "description": "Step 14 of the 18-step agentic test workflow. Fixes failing tests and re-runs them iteratively until they pass.",
    "dependencies": [],
    "priority": 205,
    "filename": "agentic_test_step8_fix_iterate_LLM.prompt",
    "filepath": "prompts/agentic_test_step8_fix_iterate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "fix",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18600,
      "y": 440
    }
  },
  {
    "reason": "Runs Step 15 tests with multi-cwd validation to catch CI-mirror path brittleness.",
    "description": "Step 16 of the 18-step agentic test workflow. Runs newly generated tests from Step 15 with multi-cwd validation and reports results.",
    "dependencies": [],
    "priority": 205,
    "filename": "agentic_test_step16_run_tests_LLM.prompt",
    "filepath": "prompts/agentic_test_step16_run_tests_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18880,
      "y": 440
    }
  },
  {
    "reason": "Creates draft PR with generated tests including coverage ratio and testing summaries.",
    "description": "Step 17 of the 18-step agentic test workflow. Creates a draft PR with coverage ratio, contract test summary, accessibility audit, and manual testing summary.",
    "dependencies": [],
    "priority": 206,
    "filename": "agentic_test_step9_submit_pr_LLM.prompt",
    "filepath": "prompts/agentic_test_step9_submit_pr_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 18880,
      "y": 660
    }
  },
  {
    "reason": "Provides bidirectional sync between architecture.json and prompt files using PDD metadata tags.",
    "description": "Bidirectional sync between architecture.json and prompt files using PDD metadata tags. Parses pdd-reason, pdd-interface, pdd-dependency tags from prompts and updates/creates architecture.json entries.",
    "dependencies": [],
    "priority": 207,
    "filename": "architecture_sync_python.prompt",
    "filepath": "pdd/architecture_sync.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "parse_prompt_tags",
            "signature": "(prompt_content: str) -> Dict[str, Any]",
            "returns": "Dict with keys: reason, interface, dependencies, has_dependency_tags"
          },
          {
            "name": "update_architecture_from_prompt",
            "signature": "(prompt_filename: str, prompts_dir: Path = PROMPTS_DIR, architecture_path: Path = ARCHITECTURE_JSON_PATH, dry_run: bool = False, prompt_content_override: Optional[str] = None) -> Dict[str, Any]",
            "returns": "Dict with keys: success, updated, changes, error, warnings"
          },
          {
            "name": "sync_all_prompts_to_architecture",
            "signature": "(prompts_dir: Path = PROMPTS_DIR, architecture_path: Path = ARCHITECTURE_JSON_PATH, dry_run: bool = False, only_files: Optional[set] = None) -> Dict[str, Any]",
            "returns": "Dict with keys: success, updated_count, skipped_count, results, errors, registered"
          },
          {
            "name": "validate_dependencies",
            "signature": "(dependencies: List[str], prompts_dir: Path = PROMPTS_DIR) -> Dict[str, Any]",
            "returns": "Dict with keys: valid, missing, duplicates"
          },
          {
            "name": "validate_interface_structure",
            "signature": "(interface: Dict[str, Any]) -> Dict[str, Any]",
            "returns": "Dict with keys: valid, errors"
          },
          {
            "name": "get_architecture_entry_for_prompt",
            "signature": "(prompt_filename: str, architecture_path: Path = ARCHITECTURE_JSON_PATH) -> Optional[Dict[str, Any]]",
            "returns": "Architecture entry dict or None"
          },
          {
            "name": "has_pdd_tags",
            "signature": "(prompt_content: str) -> bool",
            "returns": "True if prompt contains PDD metadata tags"
          },
          {
            "name": "generate_tags_from_architecture",
            "signature": "(arch_entry: Dict[str, Any]) -> str",
            "returns": "XML tags string ready to prepend to prompt"
          },
          {
            "name": "normalize_architecture_filenames",
            "signature": "(arch_data: List[Dict[str, Any]]) -> None",
            "returns": "None (mutates arch_data in place)"
          },
          {
            "name": "register_untracked_prompts",
            "signature": "(prompts_dir: Path = PROMPTS_DIR, architecture_path: Path = ARCHITECTURE_JSON_PATH, dry_run: bool = False, only_files: Optional[set] = None) -> Dict[str, Any]",
            "returns": "Dict with keys: registered, skipped, errors"
          }
        ]
      }
    },
    "position": {
      "x": 21280,
      "y": 220
    }
  },
  {
    "reason": "Manages JWT authentication tokens, refresh tokens, and auth state for PDD Cloud.",
    "description": "Provides auth status checking, JWT caching, token refresh, and logout functionality. Interfaces with Firebase auth for PDD Cloud authentication.",
    "dependencies": [
      "_keyring_timeout_python.prompt"
    ],
    "priority": 208,
    "filename": "auth_service_python.prompt",
    "filepath": "pdd/auth_service.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_jwt_cache_info",
            "signature": "() -> Dict[str, Any]",
            "returns": "Dict[str, Any]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "has_refresh_token",
            "signature": "() -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "clear_jwt_cache",
            "signature": "() -> None",
            "returns": "None",
            "sideEffects": [
              "Clears cached JWT"
            ]
          },
          {
            "name": "get_auth_status",
            "signature": "() -> Dict[str, Any]",
            "returns": "Dict[str, Any]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "logout",
            "signature": "() -> bool",
            "returns": "bool",
            "sideEffects": [
              "Clears tokens and cache"
            ]
          },
          {
            "name": "verify_auth",
            "signature": "() -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_cached_jwt",
            "signature": "() -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 12600,
      "y": 1720
    }
  },
  {
    "reason": "Generates unit tests from example usage files with pattern-based exception assertions.",
    "description": "Generates tests based on intended behavior from a prompt and an example usage file, appending to existing tests if present. Prefers pattern-based pytest.raises(match=r'...') assertions over exact-string matches.",
    "dependencies": [],
    "priority": 209,
    "filename": "generate_test_from_example_LLM.prompt",
    "filepath": "prompts/generate_test_from_example_LLM.prompt",
    "tags": [
      "llm",
      "template",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "generate_test_from_example",
            "signature": "(prompt_content: str, example_content: str, language: str)",
            "returns": "str (test file content)"
          }
        ]
      }
    },
    "position": {
      "x": 4160,
      "y": 0
    }
  },
  {
    "reason": "Provides sync operation logging, fingerprints, run reports, and compression metadata storage.",
    "description": "Decorator-based operation logging that tracks PDD command executions. Manages fingerprints, run reports, and event logs. Works with Click commands to extract module identity from prompt_file parameter.",
    "dependencies": [],
    "priority": 210,
    "filename": "operation_log_python.prompt",
    "filepath": "pdd/operation_log.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "log_operation",
            "signature": "(updates_fingerprint: bool, updates_run_report: bool, clears_run_report: bool) -> Callable",
            "returns": "Callable"
          },
          {
            "name": "append_log_entry",
            "signature": "(basename: str, language: str, entry: Mapping[str, Any], project_root=None, paths=None) -> None",
            "returns": "None"
          },
          {
            "name": "log_event",
            "signature": "(basename: str, language: str, event: str, details: Mapping[str, Any] | None = None, project_root=None, paths=None) -> None",
            "returns": "None"
          }
        ]
      }
    },
    "position": {
      "x": 10160,
      "y": 660
    }
  },
  {
    "reason": "LLM prompt template for analyzing prompt-vs-code regeneration gaps.",
    "description": "Strict code analysis that evaluates whether an LLM could regenerate the code from only the prompt. Finds gaps that would cause regeneration failures.",
    "dependencies": [],
    "priority": 211,
    "filename": "prompt_code_diff_LLM.prompt",
    "filepath": "prompts/prompt_code_diff_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 880,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt template for comparing two prompt versions.",
    "description": "Compares two versions of a prompt to identify semantic and linguistic differences between them.",
    "dependencies": [],
    "priority": 212,
    "filename": "prompt_diff_LLM.prompt",
    "filepath": "prompts/prompt_diff_LLM.prompt",
    "tags": [
      "config",
      "llm",
      "template"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 1160,
      "y": 220
    }
  },
  {
    "reason": "Manages remote PDD Cloud sessions with heartbeat, command polling, and execution.",
    "description": "Provides RemoteSessionManager class for cloud session lifecycle: registration, heartbeat, command polling/execution, and deregistration. Supports token refresh and async operations via httpx.",
    "dependencies": [
      "auth_service_python.prompt"
    ],
    "priority": 213,
    "filename": "remote_session_python.prompt",
    "filepath": "pdd/remote_session.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "RemoteSessionManager",
            "signature": "(jwt_token: str, server_port: int, session_name: Optional[str])",
            "returns": "RemoteSessionManager instance",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_active_session_manager",
            "signature": "() -> Optional[RemoteSessionManager]",
            "returns": "Optional[RemoteSessionManager]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "set_active_session_manager",
            "signature": "(manager: Optional[RemoteSessionManager]) -> None",
            "returns": "None",
            "sideEffects": [
              "Sets module-level state"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 12880,
      "y": 400
    }
  },
  {
    "reason": "Determines correct sync order for PDD modules based on dependency graph.",
    "description": "Analyzes <include> and <include-many> directives across prompt files to build a dependency graph, performs topological sort, generates ordered sync scripts, identifies affected modules when a prompt changes, and discovers documentation files associated with modified prompts for the Step 10.5 doc-sync contract (pdd change, issue #739).",
    "dependencies": [
      "auto_include_python.prompt",
      "agentic_common_python.prompt",
      "architecture_sync_python.prompt"
    ],
    "priority": 214,
    "filename": "sync_order_python.prompt",
    "filepath": "pdd/sync_order.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "extract_includes_from_file",
            "signature": "(file_path: Path) -> Set[str]",
            "returns": "Set[str]",
            "sideEffects": [
              "Reads file from disk",
              "Logs warning on missing file"
            ]
          },
          {
            "name": "extract_module_from_include",
            "signature": "(include_path: str) -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "build_dependency_graph",
            "signature": "(prompts_dir: Path) -> Dict[str, List[str]]",
            "returns": "Dict[str, List[str]]",
            "sideEffects": [
              "Reads prompt files from disk"
            ]
          },
          {
            "name": "topological_sort",
            "signature": "(graph: Dict[str, List[str]]) -> Tuple[List[str], List[List[str]]]",
            "returns": "Tuple[List[str], List[List[str]]]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "compute_sccs",
            "signature": "(graph: Dict[str, List[str]]) -> List[List[str]]",
            "returns": "List[List[str]]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "get_affected_modules",
            "signature": "(sorted_modules: List[str], modified: Set[str], graph: Dict[str, List[str]], cyclic_modules: Optional[Set[str]] = None) -> List[str]",
            "returns": "List[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "generate_sync_order_script",
            "signature": "(modules: List[str], output_path: Path, worktree_path: Optional[Path] = None) -> str",
            "returns": "str",
            "sideEffects": [
              "Writes executable shell script to output_path (chmod 0o755)"
            ]
          },
          {
            "name": "discover_associated_documents",
            "signature": "(modified_prompts: Set[Path], prompts_dir: Path, architecture_path: Optional[Path] = None, max_depth: int = 3) -> List[str]",
            "returns": "List[str]",
            "sideEffects": [
              "Reads prompt files from disk via extract_includes_from_file",
              "Reads architecture.json from disk when architecture_path is provided",
              "Used by agentic_change_orchestrator before Step 10 to populate context['associated_documents']"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 15000,
      "y": 2700
    }
  },
  {
    "reason": "Carries canonical per-module sync identity through agentic sync, runner, and durable worktree flows.",
    "description": "Defines ResolvedSyncUnit and resolve_sync_unit so every sync stage uses the same cwd, target basename, context, prompts directory, and advisory paths for a module.",
    "dependencies": [
      "construct_paths_python.prompt"
    ],
    "priority": 215,
    "filename": "resolved_sync_unit_python.prompt",
    "filepath": "pdd/resolved_sync_unit.py",
    "tags": [
      "sync",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "classes": [
          {
            "name": "ResolvedSyncUnit",
            "kind": "dataclass"
          }
        ],
        "functions": [
          {
            "name": "ResolvedSyncUnit.relocate",
            "signature": "(old_root: Path, new_root: Path) -> ResolvedSyncUnit",
            "returns": "ResolvedSyncUnit"
          },
          {
            "name": "resolve_sync_unit",
            "signature": "(key: str, target_basename: str, cwd: Path, *, requested_context: Optional[str] = None, architecture_path: Optional[Path] = None) -> ResolvedSyncUnit",
            "returns": "ResolvedSyncUnit"
          }
        ]
      }
    }
  },
  {
    "reason": "Global and GitHub issue-driven module identification plus parallel sync orchestration.",
    "description": "Entry point for no-argument global sync and agentic issue sync. Global sync scans architecture.json for stale/missing modules and dispatches AsyncSyncRunner in dependency order; issue sync parses a GitHub issue URL, identifies modules, validates dependencies, and dispatches AsyncSyncRunner — or DurableSyncRunner when invoked with durable=True. Issue sync uses build_dep_graph_from_architecture_data against in-memory combined architecture so nested-architecture edges are preserved (Phase 0 of #1328).",
    "dependencies": [
      "architecture_sync_python.prompt",
      "auto_deps_main_python.prompt",
      "agentic_sync_runner_python.prompt",
      "durable_sync_runner_python.prompt",
      "resolved_sync_unit_python.prompt"
    ],
    "priority": 215,
    "filename": "agentic_sync_python.prompt",
    "filepath": "pdd/agentic_sync.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_sync",
            "signature": "(issue_url: str, *, verbose: bool, quiet: bool, budget: Optional[float], skip_verify: bool, skip_tests: bool, dry_run: bool, agentic_mode: bool, no_steer: bool, max_attempts: Optional[int], timeout_adder: float, use_github_state: bool, one_session: bool, reasoning_time: Optional[float], durable: bool, durable_branch: Optional[str], no_resume: bool, durable_max_parallel: Optional[int], strength: Optional[float], temperature: Optional[float], context_override: Optional[str], compressed_context: bool = False) -> Tuple[bool, str, float, str]",
            "returns": "Tuple[bool, str, float, str]"
          },
          {
            "name": "run_global_sync",
            "signature": "(*, verbose: bool, quiet: bool, budget: Optional[float], skip_verify: bool, skip_tests: bool, agentic_mode: bool, no_steer: bool, max_attempts: Optional[int], dry_run: bool, target_coverage: Optional[float], one_session: bool, local: bool, timeout_adder: float, strength: Optional[float], temperature: Optional[float], context_override: Optional[str], compressed_context: bool = False) -> Tuple[bool, str, float, str]",
            "returns": "Tuple[bool, str, float, str]"
          },
          {
            "name": "_is_github_issue_url",
            "signature": "(s: str) -> bool",
            "returns": "bool"
          },
          {
            "name": "_parse_llm_response",
            "signature": "(response: str) -> Tuple[List[str], bool, List[Dict]]",
            "returns": "Tuple[List[str], bool, List[Dict]]"
          },
          {
            "name": "_parse_changed_modules_env",
            "signature": "(value: str) -> list[str]",
            "returns": "list[str]"
          }
        ]
      }
    },
    "position": {
      "x": 15600,
      "y": 2400
    }
  },
  {
    "reason": "Parallel dependency-aware sync runner that forwards compressed-context sync settings.",
    "description": "Manages pdd sync subprocesses using a ThreadPoolExecutor whose worker count is read by _read_sync_max_workers() from the PDD_SYNC_MAX_WORKERS env var at construction (default 4, non-integer falls back to 4, clamped to 1-4, forced to 1 when total_budget is set; MAX_WORKERS stays a backwards-compatible module constant), preserves architecture subdirectory basenames, strips architecture filename/target language suffixes via PDD's known-language catalog so compound languages such as TypeScriptReact and JavaScriptReact resolve correctly, respects dependency ordering from architecture.json, enforces total-budget sequential execution when requested, shrinks each child/retry budget by persisted module costs plus current-module in-flight retry cost, posts live progress to GitHub issue comments with a total body cap below GitHub's comment limit, keeps unrelated ready modules running after a module failure while blocking transitive failed-dependency dependents, and stops all new scheduling when total budget is exhausted. Detects four repairable generation failures from per-module sync subprocesses - architecture-conformance ('Architecture conformance error for ' line prefix, parsed by _parse_conformance_failure), public-surface regression ('Public surface regression for ' line prefix, parsed by _parse_public_surface_failure, issue #1012), test-churn ('Test churn threshold exceeded for ' line prefix, parsed by _parse_test_churn_failure, issue #1012), and prose-output ('Generation output extraction failure for ' line prefix, parsed by _parse_prose_output_failure, issue #1649, checked first and limited to 1 additional attempt) - and retries conformance/surface/churn failures up to MAX_CONFORMANCE_ATTEMPTS=3 with a PDD_REPAIR_DIRECTIVE env var that names the exact offending symbols / churn ratio and forbids editing architecture.json or rewriting unrelated tests. Every child/retry keeps the per-module resolved .pddrc context, --strength, and --temperature stable so a flaky regeneration cannot silently switch model or context between attempts. Stops retrying before launching another repair child if the in-flight repair cost exhausts total_budget, short-circuits when the failure signature (sorted missing/removed symbols, ('prose',), or (rounded churn_ratio, pre_line_count)) repeats, and on hard failure records the matching structured block - '=== architecture conformance failure ===', '=== public surface regression ===', '=== test churn threshold exceeded ===', or '=== generation output extraction failure ===' with extractor_result/raw_output_excerpt fields (built by build_conformance_hard_failure_from_error / build_public_surface_hard_failure_from_error / build_test_churn_hard_failure_from_error / build_prose_output_hard_failure_from_error, all importable from pdd.agentic_sync_runner so sync_main and sync_orchestration reuse them verbatim) - plus a 'Reproduce locally:' line and an '--- env ---' fingerprint (pdd.__file__, pdd --version, git SHA, dirty flag, repo-vs-site-packages) so the GitHub App surface is actionable without exceeding GitHub comment size limits. Bounds per-attempt child stdout/stderr capture with a _BoundedTextCapture tail (capped by STDOUT_CAPTURE_LINE_LIMIT=5000 lines and STDOUT_CAPTURE_BYTE_LIMIT=1 MiB per stream, allocated fresh each attempt) so verbose repair retries cannot grow memory unbounded and trigger SIGKILL on Cloud Run; reads child pipes as bytes and decodes UTF-8 incrementally; tracks dropped line/byte counts surfaced via _log_dropped_output() and appended to failure/timeout summaries without injecting diagnostics into captured child output. The 'Overall status: ... Failed' verdict is recorded as the stdout line streams in (sticky), so a failure marker evicted from the bounded tail by trailing output still yields a failed verdict and a correct failure reason.",
    "dependencies": [
      "architecture_sync_python.prompt",
      "agentic_langtest_python.prompt",
      "agentic_test_orchestrator_python.prompt",
      "sync_order_python.prompt",
      "resolved_sync_unit_python.prompt"
    ],
    "priority": 216,
    "filename": "agentic_sync_runner_python.prompt",
    "filepath": "pdd/agentic_sync_runner.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "AsyncSyncRunner",
            "signature": "(basenames: List[str], dep_graph: Dict[str, List[str]], sync_options: Dict[str, Any], github_info: Optional[Dict[str, Any]], quiet: bool = False, *, verbose: bool = False, issue_url: Optional[str] = None, module_cwds: Optional[Dict[str, Any]] = None, module_targets: Optional[Dict[str, str]] = None, module_contexts: Optional[Dict[str, Optional[str]]] = None, module_units: Optional[Dict[str, ResolvedSyncUnit]] = None, initial_cost: float = 0.0) -> AsyncSyncRunner",
            "returns": "AsyncSyncRunner"
          },
          {
            "name": "AsyncSyncRunner.run",
            "signature": "() -> Tuple[bool, str, float]",
            "returns": "Tuple[bool, str, float]"
          },
          {
            "name": "build_dep_graph_from_architecture",
            "signature": "(arch_path: Path, target_basenames: List[str]) -> DepGraphFromArchitectureResult",
            "returns": "DepGraphFromArchitectureResult"
          },
          {
            "name": "build_dep_graph_from_architecture_data",
            "signature": "(architecture: Any, target_basenames: List[str], *, source_name: str = \"architecture.json\") -> DepGraphFromArchitectureResult",
            "returns": "DepGraphFromArchitectureResult"
          },
          {
            "name": "build_conformance_hard_failure_from_error",
            "signature": "(exc: ArchitectureConformanceError, basename: str) -> str",
            "returns": "str"
          },
          {
            "name": "build_public_surface_hard_failure_from_error",
            "signature": "(exc: PublicSurfaceRegressionError, basename: str) -> str",
            "returns": "str"
          },
          {
            "name": "build_test_churn_hard_failure_from_error",
            "signature": "(exc: TestChurnError, basename: str) -> str",
            "returns": "str"
          },
          {
            "name": "build_prose_output_hard_failure_from_error",
            "signature": "(exc: ProseOutputError, basename: str) -> str",
            "returns": "str"
          }
        ]
      }
    },
    "position": {
      "x": 15600,
      "y": 2000
    }
  },
  {
    "reason": "Durable issue-sync execution engine that isolates each module in a dedicated git worktree and checkpoints successful module output to a durable branch.",
    "description": "Subclass of AsyncSyncRunner used when run_agentic_sync is invoked with durable=True. Worker concurrency follows the same precedence as the parent: total_budget forces one worker, an explicit durable_max_parallel overrides, otherwise it reads the shared PDD_SYNC_MAX_WORKERS default via _read_sync_max_workers(). Prepares a `sync/issue-<N>` durable branch (or an explicit override) and a `.pdd/worktrees/durable-issue-<N>` worktree, runs each scheduled module inside its own per-module worktree, and on success applies the module diff to the durable branch as a checkpoint commit carrying a `PDD-Sync-Checkpoint-V1: issue=<N> module=<basename>` trailer. Pushes checkpoints with bounded retries, halts further checkpointing on patch conflict or non-fast-forward push, refuses to operate on `main`/`master`/the repository default branch, ignores `.pdd/agentic_sync_state.json` for resume, and emits stdout markers (`PDD_CHECKPOINT: issue=<N> module=<m> commit=<sha> empty=<bool>`) only after a successful push.",
    "dependencies": [
      "agentic_sync_runner_python.prompt",
      "resolved_sync_unit_python.prompt"
    ],
    "priority": 216,
    "filename": "durable_sync_runner_python.prompt",
    "filepath": "pdd/durable_sync_runner.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "DurableSyncRunner",
            "signature": "(*, basenames: List[str], dep_graph: Dict[str, List[str]], sync_options: Dict[str, object], github_info: Optional[Dict[str, object]], issue_number: int, project_root: Path, durable_branch: Optional[str] = None, no_resume: bool = False, durable_max_parallel: Optional[int] = None, quiet: bool = False, verbose: bool = False, issue_url: Optional[str] = None, module_cwds: Optional[Dict[str, Path]] = None, module_targets: Optional[Dict[str, str]] = None, module_contexts: Optional[Dict[str, Optional[str]]] = None, module_units: Optional[Dict[str, ResolvedSyncUnit]] = None, initial_cost: float = 0.0)",
            "returns": "DurableSyncRunner",
            "sideEffects": [
              "Resolves durable branch name and durable worktree path; total_budget in sync_options forces sequential scheduling, else an explicit durable_max_parallel caps concurrency, else concurrency falls back to the shared PDD_SYNC_MAX_WORKERS default via agentic_sync_runner._read_sync_max_workers()"
            ]
          },
          {
            "name": "DurableSyncRunner.run",
            "signature": "() -> Tuple[bool, str, float]",
            "returns": "Tuple[bool, str, float]",
            "sideEffects": [
              "Validates git repository and origin remote, prepares the durable branch worktree, prunes orphan per-issue worktrees, optionally pushes any unpushed local checkpoints, marks already-checkpointed modules as resumed (unless no_resume), then delegates scheduling to AsyncSyncRunner.run"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 15800,
      "y": 2000
    }
  },
  {
    "reason": "Entry point for the agentic checkup workflow; in PR mode the linked issue is optional (own-merits review).",
    "description": "Accepts an optional GitHub issue URL; when one is provided it fetches issue content and comments, otherwise (PR mode only) it threads empty issue context so the PR is reviewed on its own merits. Loads architecture.json and .pddrc, optionally runs a bounded non-interactive prompt repair loop on changed .prompt files, then invokes the multi-step checkup orchestrator, the PR-mode primary-reviewer/fixer review loop, or (final_gate) the canonical two-layer final PR gate (issue #1406): Layer 1 PR-scoped checkup then Layer 2 review-loop on the resulting head, returning a real ship verdict derived from the review-loop final-state.json. PR review-loop context is bounded and includes PR body, changed files, comments, and submitted reviews.",
    "dependencies": [
      "agentic_common_python.prompt",
      "checkup_review_loop_python.prompt",
      "prompt_repair_python.prompt"
    ],
    "priority": 217,
    "filename": "agentic_checkup_python.prompt",
    "filepath": "pdd/agentic_checkup.py",
    "tags": [
      "agentic",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_checkup",
            "signature": "(issue_url: Optional[str] = None, *, verbose: bool = False, quiet: bool = False, no_fix: bool = False, timeout_adder: float = 0.0, use_github_state: bool = True, reasoning_time: Optional[float] = None, pr_url: Optional[str] = None, test_scope: str = \"full\", full_suite_source: str = \"local\", review_loop: bool = False, final_gate: bool = False, review_only: bool = False, reviewers: str = \"codex,claude\", reviewer: Optional[str] = None, fixer: Optional[str] = None, reviewer_fallback: Optional[str] = None, fixer_fallback: Optional[str] = None, max_review_rounds: int = 5, max_review_cost: float = 50.0, max_review_minutes: float = 90.0, require_all_reviewers_clean: bool = True, continue_on_reviewer_limit: bool = False, require_final_fresh_review: bool = True, blocking_severities: Optional[str] = None, clean_reviewer_states: Optional[str] = None, fallback_reviewer_on_failure: bool = False, allow_same_reviewer_fixer: bool = False, enable_gates: bool = True, gate_timeout: float = 60.0, gate_allow: Tuple[str, ...] = (), start_step_override: Optional[Union[int, float]] = None, cwd: Optional[Path] = None, prompt_repair: str = \"off\", max_prompt_repair_rounds: int = 1, max_prompt_token_growth: int = 1000, max_prompt_repair_seconds: float = 120.0) -> Tuple[bool, str, float, str]",
            "returns": "Tuple[bool, str, float, str]",
            "sideEffects": [
              "Fetches the GitHub issue (when provided) and PR context via gh api",
              "Dispatches to the checkup orchestrator and/or the PR review-loop; both may commit and push generated fixes to the PR head branch and post report comments to the issue/PR (suppressed when use_github_state is False). For review-loop/final-gate mode, forwards allow_same_reviewer_fixer into ReviewLoopConfig so the loop can explicitly opt into single-role review/fix semantics",
              "For final_gate, clears then re-reads the review-loop final-state.json under .pdd/checkup-review-loop/ to derive the ship verdict",
              "Posts an error comment on the source issue if the legacy orchestrator raises"
            ]
          },
          {
            "name": "_extract_json_from_text",
            "signature": "(text: str) -> Optional[Dict[str, Any]]",
            "returns": "Optional[Dict[str, Any]]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_load_pddrc_content",
            "signature": "(project_root: Path) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_fetch_comments",
            "signature": "(comments_url: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_fetch_pr_context",
            "signature": "(owner: str, repo: str, pr_number: int) -> str",
            "returns": "str",
            "sideEffects": [
              "Reads PR metadata, changed files, PR comments, and PR reviews with gh api"
            ]
          },
          {
            "name": "_discover_prompt_paths",
            "signature": "(cwd: Path) -> List[Path]",
            "returns": "List[Path]",
            "sideEffects": [
              "Runs git diff to detect tracked/new .prompt files; falls back to glob"
            ]
          },
          {
            "name": "_review_loop_ship_verdict",
            "signature": "(final_state: Optional[Dict[str, Any]], *, has_issue: bool) -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 15600,
      "y": 2800
    }
  },
  {
    "reason": "Implements the PR-mode primary-reviewer/fixer review loop for pdd checkup.",
    "description": "Runs one primary reviewer such as Codex against an existing PR, sends the reviewer's actionable findings to the resolved fixer (a separate role such as Claude by default, or the same role only when allow_same_reviewer_fixer explicitly opts into single-role review/fix mode), then has the active reviewer evaluate the fixer's changes and rationales in the same loop-owned worktree until clean or max rounds. An optional reviewer_fallback can take over once when the primary reviewer cannot complete, while preserving the original primary status as non-active diagnostic state; on a successful takeover the superseded primary's Per-Reviewer Status row is annotated `(optional, superseded by <fallback>)` so downstream verdict adapters resolve the report to ship_degraded rather than unknown. An optional fixer_fallback is the analog for the fix step: when the primary fixer reports success=False (typical cause: Claude Code subscription-tier credential exhaustion classified as credential-limit) the loop captures the pre-fix HEAD SHA, resets the worktree (`git reset --hard <pre_fix_sha>` + `git clean -fd`) so the failed primary's partial edits do not leak, alias-normalizes the fallback role, refuses it if it equals the primary fixer, the active reviewer, or the originally configured reviewer (preserved as ReviewLoopState.original_reviewer), then invokes the fallback once. On success the fallback role becomes ReviewLoopState.active_fixer and drives every subsequent round's fix step (the one-shot fallback is consumed); both the failed primary FixResult and the fallback FixResult are appended to state.fixes for an honest audit trail. When the fallback also fails its FixResult is still appended before the loop breaks; failed-fixer summaries are routed through _defang_adapter_trip_wires before rendering so [CRITICAL], issue_aligned: false, and max-*-reached: true tokens from raw subprocess output cannot downgrade an otherwise-verified fallback verdict. Review-only mode runs just the primary reviewer first pass without fixer, commit, or push. The parser filters findings that only describe external GitHub/CI readiness state with no repository-file evidence, so status-only auto-heal, Cloud Build, mergeability, and pending/action-required required-check findings do not keep the fixer loop running. The fixer may return fixed, not_valid, partially_fixed, or blocked, but only the active reviewer can close a finding. Issue #1088 trust boundary: findings only flip to status=fixed after the verifier clears the exact SHA the fixer just pushed. ReviewLoopState carries verified_head_sha (the pushed SHA the verifier most recently cleared), remote_pr_head_sha (observed at render time via a single _fetch_pr_metadata re-fetch), and verification_status_by_round (per-round outcome in {verified, unverified, stale, skipped}). When the fixer left the worktree unchanged (`No changes to push.`) or `git rev-parse HEAD` returns empty after a reported push, the loop skips the verifier, marks the round skipped, and stops; verification never runs against a SHA that did not land. When _finalize re-fetches the PR head and observes either a SHA mismatch or a missing head_sha, fresh-final downgrades to missing, any verified rounds are recorded as stale, and fixed findings revert to open in final-state.json so downstream consumers cannot treat the stale verifier verdict as ship-ready. FixResult and the per-round round-N-fix-...findings.json artifact persist fixer_result, push_status, local_fixer_commit_sha, and pushed_head_sha (rendered explicitly, even when null) so the on-disk audit trail matches the prompt contract. The loop persists prompt+output+normalized-findings+dedup-state artifacts per round (and a final-state.json), pushes fixes back to the PR head ref via the shared token-refresh helper from agentic_e2e_fix_orchestrator, and emits a Step-7-compatible final report whose findings table contains only remaining/unfixed findings.",
    "dependencies": [
      "agentic_common_python.prompt",
      "agentic_change_python.prompt",
      "agentic_checkup_orchestrator_python.prompt",
      "agentic_e2e_fix_orchestrator_python.prompt",
      "architecture_registry_python.prompt",
      "git_porcelain_python.prompt"
    ],
    "priority": 217.5,
    "filename": "checkup_review_loop_python.prompt",
    "filepath": "pdd/checkup_review_loop.py",
    "tags": [
      "agentic",
      "checkup",
      "review-loop",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_checkup_review_loop",
            "signature": "(*, context: ReviewLoopContext, config: ReviewLoopConfig, cwd: Path, verbose: bool, quiet: bool, use_github_state: bool) -> Tuple[bool, str, float, str]",
            "returns": "Tuple[bool, str, float, str]",
            "sideEffects": [
              "Creates one PR worktree for the entire loop session via _setup_pr_worktree",
              "Runs primary-reviewer/fixer/verifier CLI agents against that worktree",
              "Can invoke one normalized reviewer_fallback if the primary reviewer cannot complete; the fallback becomes active for subsequent review/fix/verify gating",
              "Requires distinct reviewer/fixer roles by default; when ReviewLoopConfig.allow_same_reviewer_fixer is true and the resolved roles are equal, runs explicit single-role review/fix mode without writing the legacy fixer sentinel into reviewer_status. The final report renders same-role-review-fix: true and final-state.json records same_role_review_fix=true with mode=single-role-review-fix",
              "Can invoke one alias-normalized fixer_fallback when the primary fixer reports success=False (typical: credential-limit). Before the fallback runs, captures the pre-fix HEAD SHA and resets the worktree (git reset --hard <sha> + git clean -fd) so failed primary edits do not leak into the next commit. The fallback is skipped when it resolves to the primary fixer, the active reviewer, or the originally configured reviewer (ReviewLoopState.original_reviewer) to preserve reviewer/fixer role independence. On success the fallback becomes ReviewLoopState.active_fixer and drives every subsequent round; the fallback is one-shot for the entire loop. Both the failed primary FixResult and the fallback FixResult (success or failure) are appended to state.fixes for the audit trail; failed-fixer summaries are routed through _defang_adapter_trip_wires before rendering so trip-wire tokens cannot downgrade an otherwise-verified fallback verdict",
              "In review-only mode, runs only the primary reviewer and never fetches push metadata, commits, or pushes",
              "Commits with PDD Bot identity and pushes fixes to the PR head ref via the shared push_with_retry helper with force_with_lease_on_non_fast_forward disabled (refuses to leave tokenized URL in git config)",
              "If the PR head advances remotely during the fixer turn, fetches refs/heads/<head_ref> from the updated head repo with token auth fallback, runs a plain git rebase --onto FETCH_HEAD <upstream> HEAD where <upstream> is local_base_sha when supplied (typically pre_fix_sha, per Issue #1433 Bug #3 + codex pass-2 F3 to replay the FULL multi-commit local range from agentic autoheal) and falls back to HEAD~1 for the legacy single-commit case, then retries the push instead of force-pushing over remote work; rebase conflicts abort before verifier",
              "When a budget cap is crossed during a successful fixer turn, still commits and pushes the completed fixes before stopping prior to verifier execution",
              "Feeds fixer rejections back to the primary reviewer and keeps reviewer-rejected findings open until fixed or max rounds",
              "Issue #1088 SHA-backed verification trust boundary: captures the post-push HEAD SHA via _git_rev_parse_head (never inferred from fixer prose), populates FixResult.push_status (pushed | push_failed | not_attempted), FixResult.local_fixer_commit_sha, and FixResult.pushed_head_sha after every fix turn, and rewrites the per-round fix findings.json artifact so the on-disk audit trail carries those fields. The verifier only runs when push_status == pushed AND a non-empty pushed_head_sha was observed; otherwise the round is marked skipped/unverified and the loop stops without claiming the findings as fixed. A clean verify pass pins state.verified_head_sha to that pushed SHA",
              "Issue #1088 final stale-head re-fetch: _finalize calls _fetch_pr_metadata exactly once at render to read the live remote head_sha whenever there is anything to verify — state.verified_head_sha is set (a verifier pinned a SHA), state.fresh_final_status == clean (a reviewer cleared the actual worktree HEAD without a subsequent fixer push), any fix was pushed, or any finding is marked fixed. The comparison target is state.verified_head_sha when a verifier ran clean, otherwise the most recent FixResult.pushed_head_sha for partial verifier acceptance, otherwise state.reviewed_head_sha captured from git rev-parse HEAD in the worktree the reviewer inspected (never from later PR metadata). If the remote head differs from the comparison target, the comparison target was never observed, or the re-fetch returned no head_sha, downgrades fresh_final_status to missing, downgrades every verification_status_by_round entry from verified to stale, and reverts every findings_by_key entry from fixed to open so final-state.json cannot present a stale verdict. state.final_refetch_attempted is set so the render layer can distinguish a failed re-fetch (remote-pr-head-sha: unknown) from no re-fetch at all (remote-pr-head-sha: none)",
              "Qualifies all unverified fixer rationale prose in the final report as fixer=<role> fixer_disposition=<value> / fixer_rationale=<value> and appends verification=unverified, so bare fixer claims such as 'claude: fixed - ...' never appear as verifier evidence",
              "Writes per-round prompt/output/normalized-findings/dedup-state artifacts and a final-state.json under .pdd/checkup-review-loop/issue-{N}-pr-{M}/. final-state.json includes same_role_review_fix, mode, verified_head_sha, remote_pr_head_sha, reviewed_head_sha, source_of_truth, gates, and verification_status_by_round so downstream consumers can re-verify the trust boundary and distinguish independent reviewer/fixer runs from explicit single-role review/fix runs",
              "Posts the final report to the source issue and PR only when use_github_state=True (writes-only suppression flag)",
              "Issue #1092: at every clean-exit site (round-start LLM clean, review-only clean, post-verify clean, pending-findings-empty early exit, and fallback-reviewer clean), invokes _enforce_gates_before_clean to run pdd.checkup_gates over the loop-owned worktree; any failing deterministic gate (PR-range git diff --check, prettier --check, a non-mutating Python syntax check via builtin compile(), optional ruff/black/mypy/`npx --no-install tsc --noEmit`) is converted to a synthetic blocker ReviewFinding (reviewer prefix `gate:`) that the loop feeds back into the fixer rather than declaring clean",
              "Issue #1092 base-ref refresh: when config.enable_gates is True the loop calls pdd.agentic_checkup_orchestrator._refresh_pr_base_ref after _fetch_pr_metadata, fetching the PR's base branch into the dedicated tracking ref refs/remotes/pdd-checkup/pr-{N}/base (NEVER refs/remotes/origin/<base>, which would mutate the operator's tracking refs when their origin is a fork) and populating pr_metadata['base_local_ref']. The fetch is bounded by a 60s subprocess timeout so a stalled transport cannot hold the review loop forever. The refresh helper resolves a trusted absolute git binary through pdd.checkup_gates and uses the sanitized gate env for its git-root, remote, and fetch subprocesses, so PATH=.:$PATH cannot execute a PR-shipped ./git before gate discovery. The review-loop short-circuit that skipped _fetch_pr_metadata in review-only mode is suppressed when gates are enabled so review-only on non-main-base PRs still gets the PR-range git-diff-check guarantee. discover_gates resolves base_ref from pr_metadata['base_local_ref'] first and falls back to the raw base_ref name when the refresh helper succeeded but did not land the ref",
              "Issue #1092 fail-closed on refresh error: when _refresh_pr_base_ref populates pr_metadata['base_ref_fetch_error'] (subprocess CalledProcessError, TimeoutExpired, or other failure), _enforce_gates_before_clean appends a crash-row to state.gate_runs (phase='base-ref-refresh') and returns a single synthetic gate:base-ref blocker ReviewFinding. The loop refuses the clean verdict the same way it does for a crashed gate runner — never silently falls back to origin/main or worktree-only git diff --check, both of which would let an LLM clean verdict ride over a check we cannot prove ran against the right base",
              "Issue #1092 fail-closed on metadata fetch: when _fetch_pr_metadata returns no usable base_ref (gh API outage, auth error, invalid JSON), the loop sets pr_metadata['base_ref_fetch_error'] BEFORE the refresh-call site so the same gate:base-ref fail-closed path engages — the pre-fix guard (if pr_metadata.get('base_ref')) silently skipped the refresh and let gates run with base_ref=None",
              "Issue #1092 fail-closed on changed-files fallback: _pr_changed_files_all walks the base-candidate list using a trusted absolute git binary plus the sanitized gate env, then falls back to HEAD~1...HEAD only when every authoritative base diff failed. When the caller passed a real pr_metadata (base_ref or base_local_ref set) and the scanner had to fall back, the scanner records pr_metadata['changed_files_fallback']. _enforce_gates_before_clean then emits a single gate:changed-files blocker (phase='changed-files-resolution') so iter-30 node_modules and iter-27/iter-32 config-touched skips cannot be silently bypassed by a truncated inventory missing earlier-commit poisoning (iter-34 Finding 1). The same trusted git path is used by the sibling _pr_changed_python_files scanner so static drift detection cannot execute a PR-shipped ./git either. The same sentinel is also set — and same blocker emitted — when _pr_changed_files_all RAISES; the previous 'swallow into []' path proceeded with an empty inventory and let the safety skips no-op (iter-35 Finding 1). Exception text is scrubbed before storage. The fallback path stays valid when no base_ref was expected (unit-test paths)",
              "Issue #1433 Bug #1 (pre-flight base-merge probe): immediately after _setup_pr_worktree and _refresh_pr_base_ref but BEFORE the first reviewer round, calls _detect_pr_base_merge_conflict(worktree, pr_metadata['base_local_ref']). The helper verifies the base ref with git rev-parse --verify, then runs `git merge-tree --write-tree --no-messages <base_local_ref> HEAD` (requires git >= 2.38; exit 0 = clean, exit 1 = conflict, other = cannot decide). Fail-OPEN on cannot-decide so non-PR worktrees, old git, or unresolvable refs do not become a new spurious block. When a real conflict is detected the loop creates a synthetic blocker ReviewFinding (reviewer='preflight:base-merge', area='pr-merge-conflict', round_number=0), persists preflight-base-merge-conflict.txt under artifacts_dir, sets reviewer_status[reviewer]='findings' + stop_reason, renders the final report via _finalize, posts via _post_review_loop_report, and returns with zero LLM spend. Codex review pass-2 finding F5: pre-flight only probes against base_local_ref freshly fetched by _refresh_pr_base_ref — a stale origin/<base> fallback would risk false positives on fork-origin operators. When base_ref_fetch_error is set or base_local_ref is unavailable the pre-flight is skipped and the existing gate:base-ref blocker handles the base problem",
              "Issue #1433 Bug #3 (push fixer-subprocess commits): _commit_and_push_if_changed accepts a pre_fix_sha kwarg the loop captures via git rev-parse HEAD before invoking the fixer. When the fixer subprocess (typically codex autoheal) already committed inside the worktree, _git_changed_files returns empty (working tree is clean) but HEAD has moved past pre_fix_sha. The pre-fix function returned 'No changes to push' and the autoheal commit silently rotted. The helper now treats current_head != pre_fix_sha as has_unpushed_local_commits INDEPENDENTLY of the dirty flag (codex pass-2 F2 — the previous `not changed and ...` gate failed when the worktree had only filtered .pdd/checkup-context / .pdd/meta artifacts) and pushes the existing commits AS-IS, preserving the original author/message (e.g., 'Codex Local Autoheal') without creating a redundant PDD-Bot commit on top",
              "Issue #1433 Bug #3 + codex pass-2 F3 (rebase full local range): _rebase_onto_updated_pr_head accepts a local_base_sha kwarg (typically pre_fix_sha). When supplied, the rebase upstream is `<local_base_sha>` instead of the hard-coded `HEAD~1`, so a multi-commit local range (typical for agentic autoheal that lands two or more commits per round) replays in full onto FETCH_HEAD. The pre-F3 single-commit HEAD~1..HEAD form is preserved when local_base_sha is None or equal to fixer_sha so legacy/test callers see no behaviour change",
              "Issue #1433 Bug #3 (extended guard attention): the loop body unions _git_changed_files(worktree) with _files_changed_since(worktree, pre_fix_sha) before invoking _check_architecture_registry_edit_guard and _check_prompt_source_guard. _files_changed_since parses `git diff --name-status -z --find-renames <pre_fix_sha>..HEAD` and surfaces BOTH old and new paths for R records (matching the _git_changed_files contract via git_porcelain) and the destination only for C records (codex pass-2 F4 — the previous --name-only form let a committed rename of a prompt-owned code file out of pdd/ slip past the guard). The union ensures the #1063 prompt-source and #1081 registry-edit guards still fire on changes the fixer subprocess COMMITTED rather than leaving in the working tree",
              "Issue #1433 Bug #3 + codex pass-2 F1 (guard baseline pinned to pre_fix_sha): _check_prompt_source_guard, _check_architecture_registry_edit_guard, _load_prompt_source_map, and _path_exists_at_head all accept a head_ref kwarg (default 'HEAD' preserves backward compat for non-loop callers including the reviewer-prompt companion-files collector). The loop pins head_ref=pre_fix_sha when computing the guard baseline so the comparison is against the pre-fixer snapshot. Without this, a fixer subprocess that COMMITTED a registry repoint/removal would compare the post-fix registry against itself — zero diff — and bypass both guards entirely (defeating the protections #1063 and #1081 originally added). _path_exists_at_head's 'did this path exist at baseline?' check also threads head_ref so the unregistered-new-code scan judges additions against the pre-fix tree",
              "Issue #1433 Bug #4 (reviewer companion source-of-truth attention): _run_review computes _collect_companion_source_of_truth_files(worktree, pr_metadata) and threads it into _review_prompt via the companion_source_of_truth kwarg. _format_companion_source_of_truth renders a `## Companion Source-Of-Truth Files To Inspect` section listing {code_path, prompt_path, prompt_in_diff, architecture_in_diff} for every code file in the PR diff. Codex pass-6 finding 1: the skip condition is `prompt_in_diff AND arch_entry_in_diff` where arch_entry_in_diff is a PER-ENTRY signal computed via _arch_entries_changed_set (canonical sorted-keys JSON comparison of base vs HEAD architecture.json entries per code_path); pass-5's prompt-only skip rule missed the case where module A's prompt was co-edited but A's architecture entry was unchanged (an arch.json edit for unrelated module B set the global signal True and hid A's at-risk arch surface). Reuses _load_prompt_source_map (which reads `git show HEAD:architecture.json` to derive the canonical code <-> prompt mapping) and _pr_changed_files_all. Always fail-open: any git/IO/JSON error returns [] so the section just disappears from the prompt rather than breaking the round. Codex pass-4 finding 2 + pass-5 finding 2 + pass-6 finding 2: when the eligible set exceeds max_entries (default 200) the collector appends a synthetic marker dict {`__truncated__`: True, `total_eligible`, `shown`, `omitted_code_paths` bounded at `max_entries // 2`, `omitted_paths_remaining`}; _run_review additionally persists the FULL eligible list as a sidecar JSON artifact (`round-{N}-{mode}-companion-source-of-truth.json` under artifacts_dir) and passes the artifact path to the formatter so the truncation note instructs the reviewer to either confirm artifact-based coverage or REPORT a `severity=blocker` finding (reviewer='companion-scope', area='workflow'). The bounded prompt section + sidecar artifact together keep the prompt context budget predictable while giving the reviewer (and post-hoc audits) the complete coverage record"
            ]
          },
          {
            "name": "parse_reviewers",
            "signature": "(value: str | Sequence[str] | None) -> Tuple[str, ...]",
            "returns": "Tuple[str, ...]",
            "sideEffects": [
              "None"
            ]
          }
        ],
        "dataclasses": [
          "ReviewLoopContext",
          "ReviewLoopConfig",
          "ReviewLoopState",
          "ReviewFinding",
          "ReviewResult",
          "FixResult"
        ],
        "config_knobs": [
          "reviewers",
          "reviewer",
          "fixer",
          "reviewer_fallback",
          "fixer_fallback",
          "review_only",
          "max_rounds",
          "max_cost",
          "max_minutes",
          "require_all_reviewers_clean",
          "continue_on_reviewer_limit",
          "require_final_fresh_review",
          "blocking_severities",
          "clean_reviewer_states",
          "fallback_reviewer_on_failure",
          "timeout_adder",
          "reasoning_time",
          "enable_gates",
          "gate_timeout",
          "gate_allow",
          "enable_source_of_truth_repair",
          "allow_same_reviewer_fixer"
        ],
        "artifacts_layout": {
          "directory": ".pdd/checkup-review-loop/issue-{issue_number}-pr-{pr_number}/",
          "per_round": [
            "round-{N}-{review|verify|fallback}-{role}.prompt.txt",
            "round-{N}-{review|verify|fallback}-{role}.output.txt",
            "round-{N}-{review|verify|fallback}-{role}.findings.json",
            "round-{N}-fix-{fixer}-for-{reviewer}.prompt.txt",
            "round-{N}-fix-{fixer}-for-{reviewer}.output.txt",
            "round-{N}-fix-{fixer}-for-{reviewer}.findings.json",
            "dedup-state-round-{N}.json"
          ],
          "final": [
            "final-report.md",
            "final-state.json"
          ]
        }
      }
    },
    "position": {
      "x": 15600,
      "y": 3200
    }
  },
  {
    "reason": "Enforces deterministic-gate checks (linter, syntax, and doc-contract gates) in checkup review loops.",
    "description": "Discovers and executes a conservative, allowlisted set of repo-local checks (PR-range git diff --check, doc-contract, prettier --check via package.json, a non-mutating Python syntax check via the builtin compile() builtin per changed .py file, optional ruff/black/mypy when configured in pyproject.toml, optional `npx --no-install tsc --noEmit` when typescript is declared AND node_modules/typescript/bin/tsc is on disk). The doc-contract gate supports repo-declared `.pdd/doc_contract.json` / `.pdd/doc-contract.json` rules for non-PDD projects; when a base-tree config exists it is authoritative so a reviewed PR cannot weaken its own documentation contract. Per-file gates pass POSIX `--` end-of-options separator so paths cannot be parsed as flags. Each gate runs as a list-form subprocess with bounded timeout, output truncation, PDD_* env scrubbing, sanitized PATH, and secret redaction; gate discovery and changed-file git probes resolve a trusted absolute git binary outside the PR worktree so PATH=.:$PATH cannot run a PR-shipped ./git. Failing gates are converted into synthetic blocker ReviewFinding rows with reviewer prefix 'gate:' so the review loop refuses the clean verdict and routes the deterministic failure through the fixer.",
    "dependencies": [
      "checkup_review_loop_python.prompt"
    ],
    "priority": 217.6,
    "filename": "checkup_gates_python.prompt",
    "filepath": "pdd/checkup_gates.py",
    "tags": [
      "agentic",
      "checkup",
      "review-loop",
      "gates",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "discover_gates",
            "signature": "(worktree: Path, changed_files: Sequence[str], *, extra_allow: Sequence[str] = (), base_ref: Optional[str] = None) -> List[Gate]",
            "returns": "List[Gate]",
            "sideEffects": [
              "Reads package.json, pyproject.toml, tsconfig.json, and .git markers from the loop-owned worktree (read-only)",
              "Probes PATH for ruff/black/mypy/npx via shutil.which (read-only)",
              "Stats node_modules/typescript/bin/tsc before emitting the tsc-noemit gate; the argv is `npx --no-install tsc --noEmit --incremental false --composite false --tsBuildInfoFile /dev/null` so the gate runs the local binary (no registry install) AND cannot dirty the worktree with .tsbuildinfo even when tsconfig sets incremental: true, AND cannot trip TS6379 when the tsconfig extends chain sets composite: true. Also skips the gate when the PR diff modified tsconfig*.json (PR-controlled compilerOptions/paths/plugins are RCE-equivalent) OR any path under node_modules/ (iter-30 Finding 1 generalises iter-29 Finding 3 — every path under node_modules/ is a potential plugin/transformer/binary the gate would execute)",
              "Skips npm-script gates (format:check / prettier:check / lint:check / typecheck / tsc / tsc:noemit) when the PR diff modified the tool config the script would load (prettier.config.* / .prettierrc* / eslint.config.* / .eslintrc* / tsconfig*.json) OR when the PR diff modified any package.json (root or nested) — corepack reads the top-level packageManager field and fetches+execs the PR-selected version on first invocation, turning the local gate into a registry download (iter-40 Finding 2). Those config files are loaded as executable JavaScript or interpreted compiler options by the corresponding tool, so a fork PR could otherwise achieve RCE through the gate (iter-27 Finding 3). Prettier/eslint script gates additionally skip on ANY PR-modified .cjs/.mjs/.js file because their configs can require() arbitrary local JS plugins anywhere in the worktree and we cannot statically follow the require chain (iter-29 Finding 2). tsc-style script gates additionally skip when ANY tsconfig in the extends chain sets incremental: true or composite: true — the script body cannot inject --incremental false like the direct gate, so running it would write tsbuildinfo (iter-29 Finding 1; walked with bounded depth to defend against cyclic configs). The emit-signal walk starts not only from the worktree-root tsconfig.json but from EVERY custom -p/--project target referenced by a recognised script (file or directory shape — the directory form resolves to <dir>/tsconfig.json). Without that extension a stable `tsc -p config/build.json --noEmit` whose config/build.json (or its extends chain) sets incremental: true would slip past the root-only walk and write .tsbuildinfo (iter-38 Finding 2). The chain walk treats package-name extends (e.g. '@demo/tsconfig') as potentially emit-signalling because the resolved config lives under node_modules where the gate layer cannot read it without parsing the resolver, AND a fork PR can install/modify the package (iter-30 Finding 2). ALL npm script gates (and the direct tsc-noemit gate) additionally skip when the PR diff modified anything under node_modules/ — every script invokes a binary or transitively loads code from there and a fork PR can land a poisoned shim at e.g. node_modules/.bin/prettier (iter-30 Finding 1; the package-lock.json / yarn.lock files live OUTSIDE node_modules/ so the common 'add a dependency' PR is unaffected). The ENTIRE npm-family gate path (script gates AND direct tsc-noemit) short-circuits when a package-manager config in the worktree can redirect script execution: .npmrc/.pnpmrc declaring script-shell, .yarnrc declaring script-shell or yarn-path, .yarnrc.yml declaring yarnPath, the presence of .yarn/releases/, OR a PR diff touching .npmrc/.pnpmrc/.yarnrc/.yarnrc.yml or anything under .yarn/. Confirmed against npm/pnpm/yarn 1 and npx 10.x: each honours script-shell so a PR-supplied ./evil-sh wraps the validated argv before it runs (iter-38 Finding 1)",
              "Uses tokenized `--noEmit` detection in script acceptance so `tsc --noEmitOnError` (different flag, still emits) and `tsc --noEmit false` (explicit disable) cannot bypass the substring check (iter-27 Finding 2). Also rejects tsc-flavoured scripts that carry --watch/-w (would hang the gate), --incremental (writes tsbuildinfo regardless of --noEmit), --generateTrace, --generateCpuProfile, --listFiles, --listEmittedFiles, --diagnostics, --extendedDiagnostics, --traceResolution — the script-based path cannot inject overrides like the direct gate can (iter-33). The custom-config scan uses shlex tokenisation (handles quoted paths with embedded spaces, iter-36 Finding 2) and a single-prefix `./` strip (so hidden-directory paths like `.config/lint.json` survive comparison, iter-36 Finding 1). For tsc the scan also walks the local extends chain of `tsconfig.json` AND of every CUSTOM tsconfig referenced by a script via `-p` / `--project` (file or directory form — the directory form resolves to `<dir>/tsconfig.json`), bounded by the depth limit, refusing to leave the worktree, and deduped by resolved path. A PR-modified custom-named base (e.g. `base.json`) skips the script gate even when reached only through a non-root tsconfig chain (iter-36 Finding 3 + iter-37 Findings 1 + 2). Path comparison normalises both sides via `os.path.normpath` so non-canonical paths like `--config config/../config/lint.json` still match the PR diff (iter-37 Finding 3)",
              "_script_is_acceptable rejects npm scripts whose body starts with `npx ` UNLESS `--no-install` is also present; bare `npx <tool>` would otherwise let `npm run <script>` reach the same registry-install fallback the tsconfig path guards against, defeating the issue #1092 non-mutation contract via the package.json discovery surface",
              "Emits py-compile gates with `python -B -c 'import sys; src = open(sys.argv[1], ...).read(); compile(src, sys.argv[1], \"exec\")' <abs>` argv. The builtin compile() validates syntax without writing ANY bytecode — neither -B alone nor py_compile(cfile=os.devnull) work (the former because py_compile writes the artifact explicitly, the latter because py_compile uses an atomic rename onto cfile that cannot target /dev/null). Without this guard the loop's downstream commit-and-push helper would stage generated bytecode on repos whose .gitignore does not exclude __pycache__/",
              "Skips ruff/black/mypy gates when the PR diff modified any Python tool config (pyproject.toml / setup.cfg / ruff.toml / .ruff.toml / tox.ini / mypy.ini / .mypy.ini). A fork PR can otherwise poison [tool.black] force-exclude or [tool.ruff] exclude to silently pass over real failures (iter-32 Findings 2 + 3). Additionally skips mypy when the EXISTING [tool.mypy] config declares any local-path plugin (relative/absolute/.py-suffixed) — mypy imports and executes plugin code during type-checking, so a PR that modifies just the referenced plugin file still achieves RCE (iter-32 Finding 1). iter-38 Finding 3 extends the skip: even a 'pure package' plugin name (e.g. `my_project.mypy_plugin`) disables the gate when the worktree contains a top-level `my_project/` package (`__init__.py`, namespace-dir, or `my_project.py`) — under an editable install or PYTHONPATH-points-at-worktree env mypy's plugin import would otherwise resolve to PR-controlled worktree code. iter-39 Finding 2 widens the worktree-match probe to the src/ layout so a matching `worktree/src/<top_level>/` (or namespace-dir, or `worktree/src/<top_level>.py`) also disables the gate — editable installs of src-layout projects add `src/` to `sys.path`. Third-party pure-package plugins (mypy_django_plugin.main) whose top-level name appears NEITHER at the worktree root NOR under src/ stay supported",
              "When base_ref resolves in the worktree, emits git-diff-check across <base>...HEAD using a trusted absolute git binary resolved through the sanitized PATH, so a committed whitespace/conflict-marker failure is caught even on a clean worktree without executing a worktree-local ./git shim",
              "Issue #1433 Bug #2 + codex pass-6 through pass-12 (CI parity for `ruff format --check`): emits a separate `ruff-format` gate with argv `<ruff_abs> format --check -- <ruff_format_py>` INDEPENDENTLY of the ruff lint gate. Format-gate prerequisites: changed_py present + ruff binary on sanitized PATH + python_tool_config_touched False + ONE of: (a) [tool.ruff.format] explicitly declared in pyproject.toml; (b) `_ruff_format_signaled_by_ci` returns a truthy signal from pre-commit or workflow/CloudBuild files. CI-signal detection: `_ruff_format_signaled_by_ci` returns (pre_commit_signaled, workflow_signaled). .pre-commit-config.yaml signal is STAGE-AWARE via `_pre_commit_has_active_ruff_format` (issue #1433 review): an `id: ruff-format` hook counts only when it runs automatically — a manual-only hook (explicit `stages: [manual]` or inherited top-level `default_stages: [manual]`) does NOT opt in. Pre-commit / workflow / CloudBuild signals are independent. Workflow/CloudBuild files use `_workflow_text_has_ruff_format` which parses the YAML with `yaml.safe_load` and recursively walks it (`_node_has_ruff_format`), matching `ruff format` ONLY in the values of exec-intent keys `_RUFF_FORMAT_EXEC_KEYS = {run, script, command, entrypoint, args}` (pass-13 F2/F3): string values (incl. `run: |` block scalars, with `#`-comments stripped per line) and `args` lists (joined into an argv string) are matched; non-exec keys like `name:` or an `env: NOTE: |` block are never scanned, and an inline `args: [\"-c\", \"ruff format --check .\"]` CloudBuild step is detected. Fail-open on `yaml.YAMLError` (returns False). Gate emission splits by signal source: pyproject or workflow signal → ruff_format_py = list(changed_py) (no pre-commit scope filter); pre-commit-only signal → ruff_format_py = _filter_changed_py_for_ruff_format (union of all hook scopes). Per-file pre-vs-post comparison for touched CI configs: `_file_has_unchanged_ruff_format_signal` uses `_parse_ruff_format_hooks_from_text` to compare full hook dicts (including stages/args/always_run AND top-level default_stages propagated into hooks without their own stages) serialised as sorted JSON sets — scope, stages, and args changes all detected. YAML comments stripped before all regex scans. Pre-commit hook files:/exclude: scope filtering unions across all id:ruff-format hooks (pass-9/10 F3). Kept as a SEPARATE gate so per-tool --gate-allow filtering still works."
            ]
          },
          {
            "name": "run_gates",
            "signature": "(worktree: Path, gates: Sequence[Gate], *, artifacts_dir: Path, round_number: int, mode: str, default_timeout: float = 60.0) -> List[GateResult]",
            "returns": "List[GateResult]",
            "sideEffects": [
              "Spawns each gate as a list-form subprocess with cwd=worktree, env scrubbed of PDD_*, the Python import-path family (PYTHONPATH/PYTHONHOME/PYTHONSTARTUP/PYTHONUSERBASE/PYTHONNOUSERSITE), AND the npm/Node config + import-path family (NPM_CONFIG_*/npm_config_* — both case forms are independent env keys — plus NODE_OPTIONS and NODE_PATH), then PATH is sanitized to drop empty entries, '.', relative entries, and any entry that resolves inside the worktree, and forced CI=1/NO_COLOR=1/FORCE_COLOR=0/COREPACK_ENABLE_AUTO_PIN=0. Stripping the PYTHON* family is iter-38 Finding 3's primary defence. Stripping the NPM_CONFIG/npm_config family is iter-39 Finding 3's primary defence: NPM_CONFIG_SCRIPT_SHELL (confirmed against npm 10.x) redirects `npm run` to a PR-controlled shell even on a clean .npmrc. NODE_OPTIONS=--require=./evil.js injects arbitrary JS into the `npx --no-install tsc` gate; NODE_PATH is Node's PYTHONPATH analogue. Forcing COREPACK_ENABLE_AUTO_PIN=0 (iter-39 Finding 1) keeps Corepack-managed yarn/pnpm from mutating package.json by auto-pinning a `packageManager` field on first run — the gate's cwd IS the loop-owned PR worktree and the downstream commit/push helper would otherwise stage the mutation. The PATH sanitization (iter-40 Finding 1 + Greg PR #1095 review) prevents an operator's PATH=.:$PATH (or any worktree-resolving entry) from letting a PR-shipped ./git/./mypy/./npm/./npx shim become the gate binary. Gate.cmd ALSO stores absolute tool paths (resolved via shutil.which against the same sanitized PATH at discovery time), including the git-diff-check git binary, so subprocess.run never re-consults PATH for the top-level binary; the env-side sanitization is defence in depth for nested tool spawns by node/npm",
              "Captures stdout/stderr, scrubs through _scrub_secrets, THEN truncates to ~10KB each side (scrub-before-truncate so secrets crossing the boundary still match the anchored regex)",
              "Scrubs GateResult.error at the source in _execute_one — FileNotFoundError's repr embeds the full argv tuple, so an operator-supplied token in gate.cmd would otherwise land verbatim in ReviewFinding.evidence and the per-gate artifact",
              "Writes per-gate human-readable artifacts and a JSON manifest under artifacts_dir/round-{R}-{mode}-gate-*.txt and round-{R}-{mode}-gates.json; both paths scrub the exception text on persistence failures before logging at WARNING and do not re-emit raw tracebacks with exc_info=True",
              "Gate.to_dict() scrubs every string field (name/cmd/source/required_fix_hint) so the JSON manifest and final-state.json gates entries are safe to ship to long-term audit storage even when a fork PR poisons package.json scripts with literal tokens; the bare gate.cmd list still flows through _execute_one unchanged for subprocess invocation",
              "Per-file gates (py-compile/ruff/black/mypy) name themselves as `py-compile:<rel>` and set `source=<rel>` to the changed file path — both fields are scrubbed at every consumer site (artifact filename via _safe_slug(_scrub(name)), artifact body, _build_evidence/_build_finding_message, ReviewFinding.reviewer/location) so a PR file whose path contains a token-shaped substring (e.g. pkg/ghp_xxx.py) cannot leak into the public PR comment or final-state.json (iter-25 Finding 1)",
              "ruff/black/mypy argvs emit a POSIX `--` separator before the changed-file list (e.g. `ruff check -- <changed_py>`) so a PR file named like `--config=evil.py` is treated as a positional path, never as a flag (iter-25 Finding 2)",
              "Persistence-failure logging scrubs gate.name AND the exception text before WARNING, and does NOT re-emit the raw traceback via exc_info=True at DEBUG (traceback.format_exception would otherwise re-render the raw exception message and defeat the WARNING-line scrub) (iter-25 Finding 3)",
              "Never raises: timeouts, missing binaries, and OSErrors are captured as GateResult rows with exit_code=None and non-empty error"
            ]
          },
          {
            "name": "gate_results_to_findings",
            "signature": "(results: Sequence[GateResult], *, round_number: int) -> List[ReviewFinding]",
            "returns": "List[ReviewFinding]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "run_doc_contract_check",
            "signature": "(worktree_path: Path, base_ref: Optional[str] = None) -> int",
            "returns": "int"
          }
        ],
        "dataclasses": [
          "Gate",
          "GateResult"
        ],
        "config_knobs": [
          "DEFAULT_GATE_TIMEOUT_SECONDS"
        ],
        "artifacts_layout": {
          "directory": ".pdd/checkup-review-loop/issue-{issue_number}-pr-{pr_number}/",
          "per_round": [
            "round-{N}-{mode}-gates.json",
            "round-{N}-{mode}-gate-{slug}.txt"
          ]
        }
      }
    },
    "position": {
      "x": 15700,
      "y": 3200
    }
  },
  {
    "reason": "Multi-step orchestrator for pdd checkup — 8 steps with iterative fix-verify loop, worktree isolation, and fix-capable PR mode that pushes back to the same PR. In PR mode the linked issue is OPTIONAL: with no issue (issue_url/issue_number absent) the PR is reviewed on its own merits and _step7_passed drops the issue_aligned requirement (verdict = code findings only), the issue-thread per-step/error/final posts are skipped, and _post_pr_mode_final_report posts only to the PR thread; with an issue present the issue-alignment gate is unchanged. Issue #1212 contract: Step 5 emits a structured failure_signal block normalised into context['step5_failure_signal']; Step 6.1's prompt renders that block. The Step 5 status word is parsed into three states — pass/ok/success/passed/clean (logically clean), skipped/skip/no_tests/n/a/n_a (verification did not run; sets context['step5_verification_skipped']=1 and is detected from the parsed status alone regardless of the provider success flag, round-10 #1), and everything else including a missing/malformed block (logical failure, fail closed). The fixer is skipped for both clean and skipped; a dedicated pre-push gate refuses any push when the skipped flag is set because tests never ran — including when the worktree is clean (round-10 #2: returning 'Checkup complete' on a clean worktree without test evidence misleads the user). The gate re-derives the skipped state from the persisted step_outputs['5'] via _step5_was_skipped at gate time rather than trusting only the in-memory context cache (round-10 #3: context is not part of the saved workflow state, so a mid-loop interruption can resume with the cached Step 5 skipped block but an empty context cache). The scope guard builds pr_file_set from the UNION of pr_metadata['api_changed_files_full'|'api_changed_files'] AND context['pr_scope_changed_files'] (the local merge-base diff), tolerating GitHub API outages when the local base is healthy. The scope-allowed set extends pr_file_set with Step 5 failure paths and justified EXPANSION_ITEMS paths so the guard matches the Step 6 prompt's allowance for editing failing test files / causally-connected files without an explicit marker; the causal guard remains the backstop for zero-overlap fixes. EXPANSION_ITEMS justification is per-marker; trailing continuation lines only count when they carry semantic content (prefix marker, path mention, or causal keyword) so indented/bulleted padding cannot whitelist a path. fixer_invoked is seeded ONLY from persisted step_outputs['6_1'|'6_2'|'6_3'] (not from start_step) so a manual --start-step 7 still trips the side-effect guard. A clean Steps-3/4/5 run never commits or pushes side-effect edits left by non-fixer tooling. In --no-fix --pr mode the orchestrator applies the same three-state Step 5 parse AFTER the Steps-3/4/5 loop (reading from step_outputs['5'] to cover both fresh and resumed runs); the parse is NOT guarded on the raw output being truthy, so an empty/missing Step 5 output is itself a fail-closed condition (no test evidence) that routes to the logical-failure branch (pass-17 #1: provider success with empty Step 5 output must not slip through as a verified result). A skipped status writes .pdd/checkup-pr-N/nofix-step5-skipped-refusal.txt, calls _post_pr_mode_final_report(refusal_message) to post the terminal status to PR/issue threads, calls clear_workflow_state(...) to discard the saved cache so the next rerun reruns Step 5 instead of replaying the stale skipped/failed output (and avoids a duplicate final-report comment), and returns (False, f'{refusal}{suffix}', ...) before Step 7 runs; a logical failure (fail/error/empty/missing block) writes .pdd/checkup-pr-N/nofix-step5-failure-refusal.txt and follows the same post-then-clear-then-return pattern. Because load_workflow_state loads the GitHub state comment with PRIORITY over local state (round-17), clear_workflow_state now returns a bool and github_clear_state falls back to neutralising the state-comment body (stripping the PDD_WORKFLOW_STATE marker via _github_edit_comment) when a DELETE fails, so a token without delete scope can no longer leave stale remote state that replays the refusal forever; the orchestrator appends a cleanup warning to the returned message when the clear is still unconfirmed. The SAME unconfirmed-clear warning is appended on the terminal-success path (external review #1215 Finding 2): that path captures clear_workflow_state's bool instead of discarding it so a failed remote clear cannot let the next run resume from completed state — the checkup still returns success, the warning is informational. As a second, independent backstop for the same hole, load_workflow_state-side resume DISCARDS any cached state whose (FAILED-corrected) last_completed_step has already reached STEP_ORDER[-1] (the terminal step) when no start_step_override is given: a completed run's lingering state (left behind precisely because its best-effort remote clear failed) must not be resumed, or the rerun would skip every step and replay the cached Step 7 verdict as 'Checkup complete' without re-verifying the PR head. On discard it resets last_completed_step to 0 but preserves github_comment_id=loaded_gh_id so the fresh run PATCHes the lingering state comment rather than creating a duplicate. Because context persists across fix-verify iterations and context['step5_failure_signal'] is written ONLY on the failing Step 5 path, the orchestrator clears it (and step5_failure_signal_missing) to '' whenever a later iteration's Step 5 is clean or skipped so Step 6 is never driven by a prior iteration's stale failure block (external review #1215 Finding 1); the provider-success/logical-failure branch also prints the scrubbed failure detail unconditionally (even in quiet mode) like the provider-failure path (external review #1215 Finding 3). Non-PR no-fix runs are unaffected. The pre-push diff size gate (PDD_CHECKUP_DIFF_LOC_LIMIT, default 800 added LoC; 0 disables; unparseable falls back to default) refuses oversized pushes unless every OVERSIZED dirty path (>= OVERSIZED_PATH_ADDED_LOC_FLOOR added lines, default 50) is covered by its OWN justified EXPANSION_ITEMS entry. Small companion files are exempt; diffuse death-by-a-thousand-cuts diffs still require every path covered.",
    "description": "Orchestrates the 8-step agentic checkup workflow: discover, deps, build, interfaces, test, fix (6.1/6.2/6.3), verify, create PR. Steps 3-7 run in an iterative while loop (max 3 iterations) with exit on 'All Issues Fixed'; exhausting the loop without that exact signal returns failure and does not push fixes or create/skip through a successful PR gate. Supports resume via workflow state persistence, git worktree isolation, --no-fix mode, start-step recovery, and PR verification mode. In PR mode it checks out the PR head in a dedicated worktree using a checkout-scoped local branch name, refreshes the PR base into a dedicated local ref before recording merge-base changed files for PR-scoped test prompts, resolves a trusted absolute git binary through checkup_gates for the base-refresh and changed-file prompt probes so PATH=.:$PATH cannot execute a PR-shipped ./git, falls back to GitHub's PR files API when the local base refresh is unavailable, runs the full fix-capable checkup on that PR code, commits eligible generated fixes, pushes them back to the same PR branch, re-runs Step 7 after a rebase-on-updated-head push, posts final PR/issue reports only after the pushed PR head is verified, and skips Step 8 because the PR already exists. In PR mode _handle_step_result suppresses its per-step issue comment for Step 7 (the orchestrator's canonical final report already covers it) so the issue thread does not get duplicate Step 7 reporting; earlier-step progress comments and issue-mode Step 7 comments are unaffected (external review #1215 Finding 3). Issue #1212 safety contract: (a) Step 5 MUST emit a structured ```failure_signal``` block (command, exit_code, status, failing_ids, artifact_path, output); the orchestrator parses it via _parse_failure_signal_block, treats empty fail/error-status fields as missing, synthesises a normalised block into context['step5_failure_signal'] that Step 6's prompt explicitly references, and maps the status word to one of three states (clean / skipped / failure) with fail-closed defaults — a missing block or unrecognised status counts as a logical failure (round-8 #1) so the fixer runs rather than silently passing. A 'skipped'/'skip'/'no_tests'/'n/a'/'n_a' status sets context['step5_verification_skipped']=1 — detected from the parsed status INDEPENDENT of the provider success flag (round-10 #1: an agent can emit a coherent skipped block even when its provider call timed out) — and triggers a dedicated pre-push gate that refuses any commit/push because tests never ran (round-9 #1), including the no-changes-clean-worktree path (round-10 #2: returning 'Checkup complete' on a clean worktree without test evidence misleads the user); the gate re-derives the skipped state from the persisted step_outputs['5'] via _step5_was_skipped at gate time so a mid-loop interruption + resume cannot slip past via a missing in-memory cache (round-10 #3). (b) The scope guard builds pr_file_set from the UNION of pr_metadata['api_changed_files_full'|'api_changed_files'] AND the local merge-base diff in context['pr_scope_changed_files'] (round-8 #2 — tolerates GitHub API outages), parses both formats with regex `^-\\s+([A-Z][A-Z0-9]*):\\s*(.+?)\\s*$` (uppercase words from the API formatter, short git statuses M/A/R100 from the local-diff formatter), extends the scope-allowed set with Step 5 failure paths (round-8 #3 — matches the Step 6 prompt's allowance for editing failing test files and causally-connected files without an EXPANSION_ITEMS marker; the causal guard remains the backstop for zero-overlap fixes), parses EXPANSION_ITEMS as a structured per-marker allowlist requiring BOTH a non-empty path list AND a per-marker causal justification (_parse_expansion_items); follow-up lines only count when they carry semantic content (round-7 #1: prefix marker, path mention, or causal keyword — bare indentation/bullets do not qualify). (c) A clean PR run (Steps 3, 4, AND 5 all reported success → fixer skipped) MUST NOT commit or push worktree changes from non-fixer steps; a dedicated clean-run side-effect guard fires before scope/causal/size checks. fixer_invoked is seeded ONLY from persisted step_outputs['6_1'|'6_2'|'6_3'] so a manual --start-step 7 without prior fixer state still trips the guard (round-7 #2). (d) Pre-push diff sanity gate (_diff_size_added_lines_by_path returning per-path counts; total via _diff_size_added_lines + DIFF_SIZE_ADDED_LOC_LIMIT, overridable via PDD_CHECKUP_DIFF_LOC_LIMIT — 0 disables, unparseable falls back to default) refuses pushes adding more than the configured added-line threshold unless every OVERSIZED dirty path (>= OVERSIZED_PATH_ADDED_LOC_FLOOR added lines, default 50) is covered by its OWN justified EXPANSION_ITEMS entry; small companion files are exempt (round-9 #2). A diffuse over-limit diff with no individually oversized path falls back to the original strict 'every changed path covered' rule so death-by-a-thousand-cuts diffs are still blocked. In --no-fix --pr mode the orchestrator applies the same three-state Step 5 parse AFTER the Steps-3/4/5 loop (reading from step_outputs['5'] to cover both fresh and resumed runs); the parse is not guarded on truthiness so an empty/missing Step 5 output fails closed as a logical failure (pass-17 #1). A skipped status writes .pdd/checkup-pr-N/nofix-step5-skipped-refusal.txt and returns (False, ...) — calling _post_pr_mode_final_report(refusal_message) before returning so PR/issue threads receive a terminal status comment even without a Step 7 output, then clear_workflow_state(...) so the next rerun reruns Step 5 rather than replaying the stale cache; a logical failure (fail/error/empty/missing block) writes .pdd/checkup-pr-N/nofix-step5-failure-refusal.txt and returns (False, ...) the same way. clear_workflow_state returns a bool and github_clear_state neutralises a state comment that cannot be DELETEd (round-17) so stale GitHub state — which load_workflow_state prefers over local — cannot replay the refusal on the next run. Non-PR no-fix runs are unaffected.",
    "dependencies": [
      "agentic_common_python.prompt",
      "checkup_review_loop_python.prompt",
      "checkup_gates_python.prompt"
    ],
    "priority": 218,
    "filename": "agentic_checkup_orchestrator_python.prompt",
    "filepath": "pdd/agentic_checkup_orchestrator.py",
    "tags": [
      "agentic",
      "orchestrator",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_checkup_orchestrator",
            "signature": "(issue_url: str, issue_content: str, repo_owner: str, repo_name: str, issue_number: int, issue_title: str, architecture_json: str, pddrc_content: str, *, cwd: Path, verbose: bool = False, quiet: bool = False, no_fix: bool = False, timeout_adder: float = 0.0, use_github_state: bool = True, reasoning_time: Optional[float] = None, pr_url: Optional[str] = None, pr_owner: Optional[str] = None, pr_repo: Optional[str] = None, pr_number: Optional[int] = None, test_scope: str = \"full\", start_step_override: Optional[Union[int, float]] = None) -> Tuple[bool, str, float, str]",
            "returns": "Tuple[bool, str, float, str]",
            "sideEffects": [
              "Creates or reuses checkup worktrees",
              "Runs LLM checkup steps",
              "Persists workflow state",
              "In PR mode, may commit and push generated fixes back to the existing PR branch",
              "In PR mode, may post final PR and issue comments after push/reverify success",
              "In PR fix mode, automatically restarts the inner orchestrator against the new head when the remote PR head advances mid-checkup (issue #1116); bounded by MAX_PR_HEAD_REFRESHES=2 with a durable per-PR sidecar counter at .pdd/checkup-pr-{n}/pr_head_refreshes"
            ]
          },
          {
            "name": "_next_step",
            "signature": "(current: Union[int, float]) -> Union[int, float]",
            "returns": "Union[int, float]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_step7_passed",
            "signature": "(step7_output: str, pr_mode: bool, has_issue: bool = True) -> Tuple[bool, str]",
            "returns": "Tuple[bool, str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_format_pr_mode_final_report",
            "signature": "(step7_output: str, push_message: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_refresh_pr_context_from_worktree",
            "signature": "(context: Dict[str, str], worktree_path: Path) -> None",
            "returns": "None",
            "sideEffects": [
              "Mutates context in place with PR-worktree architecture.json, .pddrc, and project_root values"
            ]
          },
          {
            "name": "_format_pr_changed_files_for_prompt",
            "signature": "(worktree: Path, pr_metadata: Optional[Dict[str, str]] = None) -> str",
            "returns": "str",
            "sideEffects": [
              "Reads local git refs and diff output",
              "Uses pr_metadata['api_changed_files'] as an authoritative fallback when the refreshed PR base ref is unavailable",
              "Keeps the API fallback bounded so very large PRs do not bloat Step 5/7 prompts",
              "Writes pr_metadata['api_changed_files_full'] to .pdd/checkup-context/pr-changed-files-api.txt when present"
            ]
          },
          {
            "name": "_is_step_timeout_failure",
            "signature": "(output: str) -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_is_provider_failure",
            "signature": "(output: str) -> bool",
            "returns": "bool",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_format_step_abort_message",
            "signature": "(step_num: Union[int, float], output: str) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_pr_worktree_branch_name",
            "signature": "(git_root: Path, pr_number: int) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_pr_base_tracking_ref",
            "signature": "(pr_number: int) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_refresh_pr_base_ref",
            "signature": "(worktree: Path, pr_owner: str, pr_repo: str, pr_number: int, pr_metadata: Dict[str, str], quiet: bool) -> None",
            "returns": "None",
            "sideEffects": [
              "Fetches the PR base branch into a dedicated local tracking ref",
              "Mutates pr_metadata with base_local_ref or base_ref_fetch_error"
            ]
          },
          {
            "name": "_setup_worktree",
            "signature": "(cwd: Path, issue_number: int, quiet: bool, resume_existing: bool) -> Tuple[Optional[Path], Optional[str]]",
            "returns": "Tuple[Optional[Path], Optional[str]]",
            "sideEffects": [
              "Creates git worktree and branch"
            ]
          },
          {
            "name": "_setup_pr_worktree",
            "signature": "(cwd: Path, pr_owner: str, pr_repo: str, pr_number: int, quiet: bool, resume_existing: bool = False) -> Tuple[Optional[Path], Optional[str]]",
            "returns": "Tuple[Optional[Path], Optional[str]]",
            "sideEffects": [
              "Fetches PR head",
              "Creates git worktree for the existing PR branch using a checkout-scoped local branch name"
            ]
          },
          {
            "name": "_fetch_pr_metadata",
            "signature": "(owner: str, repo: str, pr_number: int) -> Dict[str, str]",
            "returns": "Dict[str, str]",
            "sideEffects": [
              "Reads pull request metadata through GitHub CLI",
              "Includes prompt-ready GitHub PR files API changed-file lines for degraded PR-scoped test context",
              "Bounds API fallback changed-file context with PR_API_CHANGED_FILES_MAX_LINES and PR_API_CHANGED_FILES_MAX_CHARS",
              "Preserves the complete API changed-file list as api_changed_files_full when the prompt preview is truncated"
            ]
          },
          {
            "name": "_commit_and_push_if_changed",
            "signature": "(worktree: Path, pr_metadata: Dict[str, str], message: str) -> Tuple[bool, str]",
            "returns": "Tuple[bool, str]",
            "sideEffects": [
              "May create a commit and push it to the PR head branch"
            ]
          },
          {
            "name": "_git_changed_files",
            "signature": "(worktree: Path) -> List[str]",
            "returns": "List[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_git_rev_parse_head",
            "signature": "(worktree: Path) -> str",
            "returns": "str",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_check_prompt_source_guard",
            "signature": "(worktree: Path, changed_files: List[str]) -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_check_architecture_registry_edit_guard",
            "signature": "(worktree: Path, changed_files: List[str]) -> Optional[str]",
            "returns": "Optional[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_parse_changed_files",
            "signature": "(output: str) -> List[str]",
            "returns": "List[str]",
            "sideEffects": [
              "None"
            ]
          },
          {
            "name": "_build_state",
            "signature": "(issue_number: int, issue_url: str, last_completed_step: Union[int, float], step_outputs: Dict[str, str], total_cost: float, model_used: str, github_comment_id: Optional[int], changed_files: Optional[List[str]] = None, worktree_path: Optional[Path] = None, fix_verify_iteration: int = 0, previous_fixes: str = \"\", mode: str = \"issue\", pr_number: Optional[int] = None, pr_owner: Optional[str] = None, pr_repo: Optional[str] = None, pr_head_sha: Optional[str] = None, pr_test_scope: Optional[str] = None, step_comments: Optional[List[int]] = None, step_telemetry: Optional[List[dict]] = None) -> Dict",
            "returns": "Dict",
            "sideEffects": [
              "None"
            ]
          }
        ]
      }
    },
    "position": {
      "x": 13800,
      "y": 1200
    }
  },
  {
    "reason": "Agentic instruction for assigning logical graph positions to modules in an\narchitecture file. The agent reads the file and an optional PRD/README, reasons about the\nlogical structure of the system, groups modules into functional swimlanes, and writes the\nupdated file in-place so pdd connect shows a graph that makes the architecture easy to\nunderstand at a glance.",
    "description": "Takes {project_root} and {architecture_path} as inputs. Agent reads the architecture file and any PRD/README. Reasons about functional groupings, pipeline stages, and dependency relationships. Places modules in swimlane columns (one per major functional area, shared/hub modules in center). Dependency depth drives y-axis (depth × 500px). Writes updated file in-place with position: {x, y} on every module. Outputs a one-line summary.",
    "dependencies": [],
    "priority": 219,
    "filename": "arrange_graph_layout_LLM.prompt",
    "filepath": "pdd/prompts/arrange_graph_layout_LLM.prompt",
    "tags": [
      "utility",
      "graph",
      "layout",
      "llm",
      "agentic",
      "architecture"
    ],
    "interface": {
      "type": "prompt",
      "inputs": {
        "project_root": "Absolute path to the project root directory",
        "architecture_path": "Absolute path to the architecture JSON file to update"
      }
    }
  },
  {
    "reason": "Implements the pdd fix command with agentic E2E, user story, and manual fix modes.",
    "description": "Implements the pdd fix command with agentic E2E, user story, and manual fix modes.",
    "dependencies": [
      "agentic_e2e_fix_python.prompt",
      "fix_main_python.prompt",
      "track_cost_python.prompt"
    ],
    "priority": 220,
    "filename": "commands/fix_python.prompt",
    "filepath": "pdd/commands/fix.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "fix",
            "description": "Iteratively fix code and tests or orchestrate an agentic GitHub issue fix"
          }
        ]
      }
    }
  },
  {
    "reason": "Module providing CI polling, log retrieval, and fix-iteration loop infrastructure for post-push CI validation.",
    "description": "Module providing CI polling, log retrieval, and fix-iteration loop infrastructure for post-push CI validation.",
    "dependencies": [
      "agentic_common_python.prompt"
    ],
    "priority": 221,
    "filename": "ci_validation_python.prompt",
    "filepath": "pdd/ci_validation.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_ci_validation_loop",
            "signature": "(cwd, repo_owner, repo_name, issue_number, max_retries, step_template, run_agentic_task_fn, timeout, quiet, expected_head_sha_override=None)",
            "returns": "Tuple[bool, str, float]",
            "sideEffects": [
              "without expected_head_sha_override, polls the PR's live head SHA instead of cwd's local HEAD so stale cloud worktrees do not burn the timeout",
              "expected_head_sha_override forces the poll to wait for a specific verified PR head SHA; if GitHub never reports that SHA as live, the loop fails with an explicit stale-head diagnostic",
              "poll timeouts with observed required checks are treated as inconclusive and best-effort instead of driving the fix loop",
              "required checks whose only terminal non-success state is ACTION_REQUIRED are treated as manual-action inconclusive instead of code failures",
              "when .pddrc ci.manual_trigger_comment or ci.manual_triggers is configured for ACTION_REQUIRED checks, posts each matching trigger comment once and repolls before falling back to inconclusive, ignoring passed checks for trigger matching",
              "missing or unknown CI check buckets are treated as failed/unknown rather than passing from a successful gh exit code, except ACTION_REQUIRED state remains manual-action even with a missing bucket",
              "required check failures whose logs clearly show external CI setup/auth issues such as missing GitHub Actions Google/Firebase secrets are treated as inconclusive instead of driving the CI-fix loop only when .pddrc ci.external_setup_fail_open is exactly true; by default failed checks remain fail-closed/repairable"
            ]
          },
          {
            "name": "run_github_checks_gate",
            "signature": "(cwd, repo_owner, repo_name, pr_number, quiet, expected_head_sha=None, required_only=False)",
            "returns": "Tuple[bool, str, str]"
          },
          {
            "name": "post_ci_failure_comment",
            "signature": "(repo_owner, repo_name, pr_number, failures, attempts, cwd)",
            "returns": "bool"
          },
          {
            "name": "detect_ci_system",
            "signature": "(cwd)",
            "returns": "str"
          }
        ]
      }
    }
  },
  {
    "reason": "Step 10 of the agentic E2E fix workflow: interprets CI failure logs and applies targeted fixes to unblock failing required checks.",
    "description": "Step 10 of the agentic E2E fix workflow: interprets CI failure logs and applies targeted fixes to unblock failing required checks.",
    "dependencies": [],
    "priority": 222,
    "filename": "agentic_e2e_fix_step10_ci_validation_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step10_ci_validation_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "fix"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": [
          "issue_url",
          "repo_owner",
          "repo_name",
          "issue_number",
          "ci_attempt",
          "ci_max_retries",
          "ci_check_results",
          "ci_failure_logs",
          "ci_system"
        ]
      }
    }
  },
  {
    "reason": "Step 11 of the agentic E2E fix workflow: reviews all code changes from the bug+fix workflow and cleans up quality issues in a single pass.",
    "description": "Step 11 of the agentic E2E fix workflow: reviews all code changes from the bug+fix workflow and cleans up quality issues in a single pass.",
    "dependencies": [],
    "priority": 223,
    "filename": "agentic_e2e_fix_step11_code_cleanup_LLM.prompt",
    "filepath": "prompts/agentic_e2e_fix_step11_code_cleanup_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "fix"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": [
          "issue_url",
          "repo_owner",
          "repo_name",
          "issue_number",
          "issue_description",
          "full_diff",
          "changed_files_content"
        ]
      }
    }
  },
  {
    "reason": "Provides embedding-based coarse retrieval for ranking text candidates by semantic similarity.",
    "description": "Computes embeddings via litellm.embedding() and cosine similarity to pre-filter large candidate sets before LLM reranking.",
    "dependencies": [],
    "priority": 38,
    "filename": "embed_retrieve_python.prompt",
    "filepath": "pdd/embed_retrieve.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "embed_and_retrieve",
            "signature": "(query: str, candidates: List[str], top_n: int = 50)",
            "returns": "List[Tuple[str, float]]"
          }
        ]
      }
    },
    "position": {
      "x": 11680,
      "y": 1020
    }
  },
  {
    "reason": "Registers all PDD subcommands with the main CLI group in canonical order, determining `pdd --help` command listing.",
    "description": "Aggregates command functions from sibling modules in pdd/commands and registers them with the Click CLI group. Keeps the main cli.py clean and pins the registration order required by `pdd --help`.",
    "dependencies": [
      "commands/replay_python.prompt",
      "commands/context_python.prompt",
      "commands/contracts_python.prompt"
    ],
    "priority": 70,
    "filename": "commands/__init___python.prompt",
    "filepath": "pdd/commands/__init__.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "register_commands",
            "description": "Register all subcommands with the main CLI group in canonical order"
          }
        ]
      }
    },
    "position": {
      "x": 13000,
      "y": 2000
    }
  },
  {
    "reason": "Registers maintenance CLI commands: sync, sync-architecture, auto-deps, and setup with all options.",
    "description": "Contains maintenance and setup commands for the PDD CLI, including single-module sync, no-argument project-wide sync, GitHub issue sync dispatch, prompt-to-architecture sync, auto-deps, and setup.",
    "dependencies": [
      "auto_deps_main_python.prompt",
      "architecture_sync_python.prompt"
    ],
    "priority": 70,
    "filename": "commands/maintenance_python.prompt",
    "filepath": "pdd/commands/maintenance.py",
    "tags": [
      "command",
      "python"
    ],
    "interface": {
      "type": "command",
      "command": {
        "commands": [
          {
            "name": "sync",
            "description": "Synchronize a prompt with its code, tests, and examples"
          },
          {
            "name": "sync-architecture",
            "description": "Sync architecture.json from prompt metadata tags"
          },
          {
            "name": "auto-deps",
            "description": "Analyze and insert dependencies into a prompt file"
          },
          {
            "name": "setup",
            "description": "Install shell completion and run setup utility"
          }
        ]
      }
    },
    "position": {
      "x": 13200,
      "y": 2000
    }
  },
  {
    "reason": "Click commands for modifying prompts: split (agentic + legacy), change (agentic + manual), and update.",
    "description": "Provides Click commands `split`, `change`, `update` that bridge the CLI to main logic. Split delegates to run_agentic_split (default) or split_main (legacy); change to run_agentic_change or change_main; update to update_main.",
    "dependencies": [
      "split_main_python.prompt",
      "agentic_split_python.prompt",
      "change_main_python.prompt",
      "agentic_change_python.prompt",
      "update_main_python.prompt"
    ],
    "priority": 70,
    "filename": "commands/modify_python.prompt",
    "filepath": "pdd/commands/modify.py",
    "tags": [
      "command",
      "python"
    ],
    "interface": {
      "type": "command",
      "command": {
        "commands": [
          {
            "name": "split",
            "description": "Split large dev units into smaller ones (agentic default, --legacy for old path)."
          },
          {
            "name": "change",
            "description": "Modify prompt based on change prompt or issue."
          },
          {
            "name": "update",
            "description": "Update prompt file based on code changes."
          }
        ]
      }
    },
    "position": {
      "x": 13400,
      "y": 2000
    }
  },
  {
    "reason": "Registers the pdd checkup CLI command, which orchestrates project-wide health checks and fixes.",
    "description": "Defines the checkup Click command including pdd checkup lint dispatch, validate-arch-includes, and agentic checkup integration. In PR mode (--pr) the --issue option is optional (PR reviewed on its own merits when omitted); --pr stays mutually exclusive with the positional TARGET, and --issue without --pr is rejected.",
    "dependencies": [
      "agentic_checkup_python.prompt",
      "commands/prompt_python.prompt",
      "commands/contracts_python.prompt",
      "commands/gate_python.prompt"
    ],
    "priority": 236,
    "filename": "commands/checkup_python.prompt",
    "filepath": "pdd/commands/checkup.py",
    "tags": [
      "command",
      "python"
    ],
    "interface": {
      "type": "command",
      "command": {
        "commands": [
          {
            "name": "checkup",
            "description": "Run agentic health checkup or local diagnostics including lint"
          }
        ]
      }
    },
    "position": {
      "x": 13700,
      "y": 2000
    }
  },
  {
    "reason": "Deterministic prompt and user-story lint for vague terms and observable contract outcomes.",
    "description": "Scans .prompt contract sections and story__*.md acceptance criteria for undefined vague terms and missing observable-outcome verbs. Optional read-only LLM ambiguity pass uses prompt_lint_LLM.prompt without preprocessing or executing directives from the lint target.",
    "dependencies": [
      "prompt_lint_LLM.prompt"
    ],
    "priority": 237,
    "filename": "prompt_lint_python.prompt",
    "filepath": "pdd/prompt_lint.py",
    "tags": [
      "module",
      "python",
      "lint"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "scan_prompt",
            "signature": "(path: Path, *, strict: bool = False, llm_template: Optional[bool] = None)",
            "returns": "LintResult"
          },
          {
            "name": "scan_stories",
            "signature": "(stories_dir: Path, *, strict: bool = False)",
            "returns": "list[LintResult]"
          },
          {
            "name": "run_llm_ambiguity_pass",
            "signature": "(path: Path, *, strength: float = 0.5, temperature: float = 0.0, time: Optional[float] = None, verbose: bool = False, use_cloud: Optional[bool] = None)",
            "returns": "list[LintIssue]"
          }
        ]
      }
    },
    "position": {
      "x": 13600,
      "y": 2000
    }
  },
  {
    "reason": "Shared Click implementation for pdd checkup lint (not a top-level pdd prompt command).",
    "description": "Defines the prompt_lint Click command invoked via pdd checkup lint dispatch. Supports deterministic scan, --stories, --json, --strict, and opt-in --llm advisory review. Read-only: never writes prompt or story files.",
    "dependencies": [
      "prompt_lint_python.prompt"
    ],
    "priority": 238,
    "filename": "commands/prompt_python.prompt",
    "filepath": "pdd/commands/prompt.py",
    "tags": [
      "command",
      "python",
      "lint"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "prompt_lint",
            "description": "Lint prompts and user stories; exposed as pdd checkup lint"
          }
        ]
      }
    },
    "position": {
      "x": 13800,
      "y": 2000
    }
  },
  {
    "reason": "Defines frontend command configs, global options, and constants for the PDD UI.",
    "description": "Frontend constants module containing command configurations, global options, and helper functions.",
    "dependencies": [],
    "priority": 150,
    "filename": "frontend/constants_typescript.prompt",
    "filepath": "pdd/frontend/constants.ts",
    "tags": [
      "module",
      "typescript",
      "frontend"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "getCommandByBackendName",
            "signature": "(backendName: string)",
            "returns": "CommandConfig | undefined"
          },
          {
            "name": "getPromptTemplate",
            "signature": "function getPromptTemplate(name: string, language: string, description?: string): string",
            "returns": "string"
          }
        ]
      }
    },
    "position": {
      "x": 14000,
      "y": 2000
    }
  },
  {
    "reason": "Standalone CI script for detecting and auto-healing prompt/example drift with budget-capped execution.",
    "description": "CI script that detects prompt/example drift across PDD modules using sync_determine_operation, optionally reclassifies auto-deps/generate drift against a git diff base, skips existing review-only example rewrites in PR auto-heal, heals drifted modules via pdd update/sync/example subprocesses, tracks LLM costs against a budget cap, and commits/pushes checkpointed changes automatically.",
    "dependencies": [
      "sync_determine_operation_python.prompt",
      "operation_log_python.prompt",
      "agentic_sync_runner_python.prompt",
      "user_story_tests_python.prompt",
      "agentic_common_python.prompt",
      "auto_include_python.prompt",
      "agentic_langtest_python.prompt",
      "architecture_sync_python.prompt",
      "metadata_sync_python.prompt"
    ],
    "priority": 80,
    "filename": "ci_drift_heal_python.prompt",
    "filepath": "pdd/ci_drift_heal.py",
    "tags": [
      "backend",
      "ci",
      "drift-detection"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "main",
            "signature": "(modules: Optional[List[str]], budget_cap: Optional[float], skip_ci: bool, diff_base: Optional[str])",
            "returns": "int"
          },
          {
            "name": "detect_drift",
            "signature": "(modules: Optional[List[str]], diff_base: Optional[str])",
            "returns": "Tuple[List[DriftInfo], List[DriftInfo]]"
          },
          {
            "name": "heal_module",
            "signature": "(drift: DriftInfo, env: Dict[str, str])",
            "returns": "Optional[bool]"
          },
          {
            "name": "commit_and_push",
            "signature": "(healed_modules: Sequence[DriftInfo], skip_ci: bool, checkpoint: bool, finalized_modules: Optional[List[Tuple[str, str]]] = None)",
            "returns": "bool"
          }
        ]
      }
    },
    "position": {
      "x": 14200,
      "y": 2000
    }
  },
  {
    "reason": "Orchestrates the agentic split workflow for diagnosing and splitting large PDD dev units.",
    "description": "State machine for agentic split: intent, survey, diagnose, investigate, propose options, setup worktree, phase-extract, extract children with per-child 6v validation/repair gates, final cross-cutting verify, regen gate, arch sync, post-arch checkup, assess, global repair, and refine_check. Resume state tracks per-child status, repair attempts, terminal child failure detail, stops immediately on unrecovered extraction or terminal per-child verify failure, clears stale child failure detail when a child recovers, and migrates legacy extracted children as extracted (not verified) so the new 6v gate still runs.",
    "dependencies": [
      "agentic_common_worktree_python.prompt",
      "split_validation_python.prompt",
      "architecture_sync_python.prompt"
    ],
    "priority": 224,
    "filename": "agentic_split_orchestrator_python.prompt",
    "filepath": "pdd/agentic_split_orchestrator.py",
    "tags": [
      "agentic",
      "module",
      "python",
      "split"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_split_orchestrator",
            "signature": "(target_file, *, cwd, verbose, quiet, timeout_adder, use_github_state, diagnose_only, propose_only, delete_dead, force_split, no_verify, skip_regen_gate, experimental_language, intent, no_phase_extraction, max_cost)",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          }
        ]
      }
    },
    "position": {
      "x": 21680,
      "y": 0
    }
  },
  {
    "reason": "CLI entry point for the agentic split workflow: validates the target, then either delegates to the orchestrator or, in strangler mode, uses the first proposed plan only to determine N and runs N independent full orchestrator passes.",
    "description": "Validates target file exists, detects language, finds git root, then either invokes the orchestrator directly (default) or runs --propose-only once to count children and then N independent --force-split passes via _run_strangler_split when strangler=True (the original plan is not threaded through; see issue #1402).",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt",
      "agentic_common_python.prompt",
      "get_language_python.prompt",
      "agentic_common_worktree_python.prompt"
    ],
    "priority": 225,
    "filename": "agentic_split_python.prompt",
    "filepath": "pdd/agentic_split.py",
    "tags": [
      "agentic",
      "module",
      "python",
      "split"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_agentic_split",
            "signature": "(target_file, *, verbose, quiet, timeout_adder, use_github_state, diagnose_only, propose_only, delete_dead, force_split, no_verify, skip_regen_gate, experimental_language, intent, no_phase_extraction, strangler, max_cost)",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          },
          {
            "name": "_run_strangler_split",
            "signature": "(*, target_path, git_root, verbose, quiet, timeout_adder, use_github_state, force_split, no_verify, skip_regen_gate, experimental_language, intent, no_phase_extraction, max_cost)",
            "returns": "Tuple[bool, str, float, str, List[str]]"
          }
        ]
      }
    },
    "position": {
      "x": 21960,
      "y": 0
    }
  },
  {
    "reason": "LLM prompt for agentic split step 1: neighborhood survey and target file scan.",
    "description": "Step 1 of agentic split. Surveys project architecture, conventions, sibling modules, and scans target file for symbols, churn, and health signals.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 226,
    "filename": "agentic_split_step1_survey_LLM.prompt",
    "filepath": "prompts/agentic_split_step1_survey_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 22240,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 2: classify architectural problem or declare LEAVE_ALONE.",
    "description": "Step 2 of agentic split. Classifies architectural problems (hotspot, dead_code, tangle, etc.) and decides whether splitting is justified.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 227,
    "filename": "agentic_split_step2_diagnose_LLM.prompt",
    "filepath": "prompts/agentic_split_step2_diagnose_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 22520,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 3: extend with test-seam data and cross-module analysis.",
    "description": "Step 3 of agentic split. Classifies symbols into tiers, extracts test-seam data, applies STAYS PUT rules, and enumerates test-file references.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 228,
    "filename": "agentic_split_step3_investigate_LLM.prompt",
    "filepath": "prompts/agentic_split_step3_investigate_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 22800,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 4: propose 2-4 split options with scoring and auto-pick.",
    "description": "Step 4 of agentic split. Produces 2-4 distinct split options with complete SplitPlans, metric projections, and qualitative assessments.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 229,
    "filename": "agentic_split_step4_propose_options_LLM.prompt",
    "filepath": "prompts/agentic_split_step4_propose_options_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 23080,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 6: extract one child as a full PDD dev unit.",
    "description": "Step 6 of agentic split. Extracts one child per invocation with full dev unit (prompt+code+example+tests) and updates parent metadata.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 230,
    "filename": "agentic_split_step6_extract_LLM.prompt",
    "filepath": "prompts/agentic_split_step6_extract_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 23360,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 7: qualitative assessment of split improvement.",
    "description": "Step 7 assessment of agentic split. Evaluates cohesion, boundary clarity, and change decorrelation to determine if the split is an improvement.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 231,
    "filename": "agentic_split_step7_assess_LLM.prompt",
    "filepath": "prompts/agentic_split_step7_assess_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 23640,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 8: repair verification failures with accumulator.",
    "description": "Step 8 of agentic split. Fixes final cross-cutting verification failures after all children pass 6v, with max 5 global iterations; per-child 6v repair is capped at 2 attempts per child.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 232,
    "filename": "agentic_split_step8_repair_LLM.prompt",
    "filepath": "prompts/agentic_split_step8_repair_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 23920,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 0: intent classification from target file + optional user flag.",
    "description": "Step 0 of agentic split. Infers user intent (REDUCE_MONOLITH, ENABLE_PARALLEL_WORK, EXTRACT_REUSABLE_LAYER, REDUCE_TEST_TIME) from target and CLI hints; step 4 re-weights rubric by intent.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 226,
    "filename": "agentic_split_step0_intent_LLM.prompt",
    "filepath": "prompts/agentic_split_step0_intent_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 21960,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 6a: per-child phase extraction analysis before step 6.",
    "description": "Step 6a of agentic split. Detects whether the largest symbol inside each child warrants phase helper extraction; produces phase_plan consumed by step 6.",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 226,
    "filename": "agentic_split_step6a_phase_extract_LLM.prompt",
    "filepath": "prompts/agentic_split_step6a_phase_extract_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 23080,
      "y": 220
    }
  },
  {
    "reason": "LLM prompt for agentic split step 9: refine-check decision after repair.",
    "description": "Step 9 of agentic split. Evaluates whether the largest child is still too monolithic and warrants a focused refinement pass (step 6a + step 6 rerun on that child).",
    "dependencies": [
      "agentic_split_orchestrator_python.prompt"
    ],
    "priority": 226,
    "filename": "agentic_split_step9_refine_check_LLM.prompt",
    "filepath": "prompts/agentic_split_step9_refine_check_LLM.prompt",
    "tags": [
      "agentic",
      "config",
      "llm",
      "template",
      "split"
    ],
    "interface": {
      "type": "config",
      "config": {
        "keys": []
      }
    },
    "position": {
      "x": 24200,
      "y": 220
    }
  },
  {
    "reason": "Post-extraction validation for the agentic split orchestrator (git sanity, completeness, metadata, test seams, parent wiring).",
    "description": "Runs checks (a-g) on a worktree after extraction: byte_equivalence, children_completeness, prompt_metadata, explicit new_example/example_file and new_test/test_file existence with conventional fallbacks, test_ownership, test_seam_resolution, and parent_wiring. Works with full or single-child plan views for the split orchestrator's per-child 6v gate. Also exposes deterministic resolvers for test/lint commands (delegating to get_test_command and get_lint_commands; returns None / empty list when no deterministic command exists).",
    "dependencies": [
      "get_test_command_python.prompt",
      "get_lint_commands_python.prompt"
    ],
    "priority": 225,
    "filename": "split_validation_python.prompt",
    "filepath": "pdd/split_validation.py",
    "tags": [
      "agentic",
      "module",
      "python",
      "split",
      "validation"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "validate_extraction",
            "signature": "(plan, worktree: Path) -> ValidationResult",
            "returns": "ValidationResult"
          },
          {
            "name": "get_test_command",
            "signature": "(file: Path) -> Optional[TestCommand]",
            "returns": "Optional[TestCommand]"
          },
          {
            "name": "get_lint_commands",
            "signature": "(file: Path) -> list[LintCommand]",
            "returns": "list[LintCommand]"
          }
        ]
      }
    },
    "position": {
      "x": 21680,
      "y": 220
    }
  },
  {
    "reason": "Shared worktree setup, git-scope guards, and agentic output parsing helpers used by multiple orchestrators.",
    "description": "Provides setup_worktree, get_git_root, resolve_main_ref, get_modified_and_untracked, check_target_file_unchanged, revert_out_of_scope_changes_with_dirs, extract_block_marker. Used by bug, split, and change orchestrators.",
    "dependencies": [
      "git_porcelain_python.prompt"
    ],
    "priority": 224,
    "filename": "agentic_common_worktree_python.prompt",
    "filepath": "pdd/agentic_common_worktree.py",
    "tags": [
      "agentic",
      "module",
      "python",
      "shared",
      "worktree"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "setup_worktree",
            "signature": "(cwd: Path, issue_number: int, quiet: bool, *, resume_existing: bool = False, branch_prefix: str = 'fix', worktree_prefix: str = 'fix')",
            "returns": "Tuple[Optional[Path], Optional[str]]"
          },
          {
            "name": "get_git_root",
            "signature": "(cwd: Path)",
            "returns": "Optional[Path]"
          },
          {
            "name": "resolve_main_ref",
            "signature": "(git_root: Path)",
            "returns": "str"
          }
        ]
      }
    },
    "position": {
      "x": 21400,
      "y": 220
    }
  },
  {
    "reason": "Resolves language-specific lint commands (ruff, eslint, etc.) used by split validation and repair.",
    "description": "Returns a list of LintCommand objects for a given file; empty list signals agentic fallback.",
    "dependencies": [],
    "priority": 223,
    "filename": "get_lint_commands_python.prompt",
    "filepath": "pdd/get_lint_commands.py",
    "tags": [
      "module",
      "python",
      "lint"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_lint_commands",
            "signature": "(file_path: str) -> list[LintCommand]",
            "returns": "list[LintCommand]"
          }
        ]
      }
    },
    "position": {
      "x": 21120,
      "y": 220
    }
  },
  {
    "reason": "Central error handling and reporting for the CLI.",
    "description": "Provides shared Rich console error output and core-dump error collection for CLI command failures.",
    "dependencies": [],
    "priority": 5,
    "filename": "core/errors_python.prompt",
    "filepath": "pdd/core/errors.py",
    "tags": [
      "core",
      "cli",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "get_core_dump_errors",
            "signature": "() -> List[Dict[str, Any]]",
            "returns": "List[Dict[str, Any]]"
          },
          {
            "name": "clear_core_dump_errors",
            "signature": "() -> None",
            "returns": "None"
          },
          {
            "name": "handle_error",
            "signature": "(exception: Exception, command_name: str, quiet: bool) -> None",
            "returns": "None"
          }
        ]
      }
    }
  },
  {
    "reason": "Defines the main PDDCLI group, global estimate options, result rendering, and duplicate guard.",
    "description": "Core CLI logic: PDDCLI click.Group, global options, entry function, and result callback that records only successful runs to the duplicate guard store.",
    "dependencies": [
      "core/dump_python.prompt",
      "core/errors_python.prompt",
      "core/utils_python.prompt",
      "core/duplicate_cli_guard_python.prompt",
      "agentic_common_python.prompt",
      "auto_update_python.prompt",
      "track_cost_python.prompt",
      "cli_branding_python.prompt"
    ],
    "priority": 5,
    "filename": "core/cli_python.prompt",
    "filepath": "pdd/core/cli.py",
    "tags": [
      "core",
      "cli",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "cli",
            "signature": "(ctx, force, strength, temperature, verbose, quiet, color, output_cost, estimate, estimate_json, review_examples, local, time, context_override, list_contexts, core_dump, keep_core_dumps, compress_examples, compress_test_context, context_compression, compression_fallback)",
            "returns": "None"
          },
          {
            "name": "process_commands",
            "signature": "(ctx, results, **kwargs)",
            "returns": "None"
          },
          {
            "name": "_derive_success_from_normalized_results",
            "signature": "(normalized_results: List[Any])",
            "returns": "bool"
          },
          {
            "name": "_restore_captured_streams",
            "signature": "(ctx)",
            "returns": "None"
          },
          {
            "name": "_strip_ansi_codes",
            "signature": "(text: str)",
            "returns": "str"
          }
        ]
      }
    }
  },
  {
    "reason": "Prevents accidental duplicate CLI invocations by persisting only successful runs within a configurable time window.",
    "description": "CLI duplicate-run guard: maintains .pdd/last_run.json store, fingerprints git state, blocks duplicate guarded subcommands, and records only successful runs so failures are retriable.",
    "dependencies": [
      "architecture_registry_python.prompt",
      "agentic_langtest_python.prompt",
      "agentic_test_orchestrator_python.prompt",
      "agentic_common_python.prompt"
    ],
    "priority": 5,
    "filename": "core/duplicate_cli_guard_python.prompt",
    "filepath": "pdd/core/duplicate_cli_guard.py",
    "tags": [
      "core",
      "cli",
      "guard",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "check_duplicate_before_subcommand",
            "signature": "(ctx)",
            "returns": "None"
          },
          {
            "name": "record_after_guarded_command",
            "signature": "(ctx, success: bool = True)",
            "returns": "None"
          },
          {
            "name": "save_last_run",
            "signature": "(project_root: Path, argv_tail: List[str], subcommand: str) -> None",
            "returns": "None"
          }
        ]
      }
    }
  },
  {
    "reason": "Regenerate pdd/data/llm_model.csv from LiteLLM metadata with agent-reviewed DeepSWE-first rank scores, raw Arena ELO metadata, pricing overrides, and provider-scoped filtering.",
    "description": "Deterministic CLI that rebuilds llm_model.csv from LiteLLM metadata plus pdd/data/deepswe_manifest.json (primary model_rank_score source) and pdd/data/arena_elo_manifest.json (raw ELO / rank fallback). The manifests are refreshed agentically with reviewed aliases and provenance; Python validates/applies them, falls back to static scores, deduplicates, and Pareto-filters per provider.",
    "dependencies": [],
    "priority": 5,
    "filename": "generate_model_catalog_python.prompt",
    "filepath": "pdd/generate_model_catalog.py",
    "tags": [
      "module",
      "python",
      "catalog"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "build_rows",
            "signature": "(refresh_elo: bool = False, existing_catalog: Optional[Path] = None, score_manifest: Optional[Path] = None, deepswe_manifest: Optional[Path] = None) -> List[dict]",
            "returns": "List[dict]"
          },
          {
            "name": "main",
            "signature": "() -> None",
            "returns": "None"
          }
        ]
      }
    }
  },
  {
    "reason": "Bounds keyring operations that can hang in headless CI/SSH or locked desktop keychains.",
    "description": "Runs keyring calls in daemon worker threads with platform-specific timeouts so authentication workflows can fail gracefully instead of blocking process exit.",
    "dependencies": [],
    "priority": 233,
    "filename": "_keyring_timeout_python.prompt",
    "filepath": "pdd/_keyring_timeout.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "_keyring_op_with_timeout",
            "signature": "(op: Callable[..., Any], *args: Any, timeout: Optional[float] = None) -> Tuple[bool, Any, Optional[BaseException]]",
            "returns": "Tuple[bool, Any, Optional[BaseException]]",
            "sideEffects": [
              "Starts a daemon worker thread for the keyring operation"
            ]
          }
        ]
      }
    }
  },
  {
    "reason": "LLM prompt that identifies which PDD modules need syncing from GitHub issue content and validates dependency graphs.",
    "description": "LLM prompt that identifies which PDD modules need syncing from GitHub issue content and validates dependency graphs.",
    "dependencies": [],
    "priority": 235,
    "filename": "agentic_sync_identify_modules_LLM.prompt",
    "filepath": "prompts/agentic_sync_identify_modules_LLM.prompt",
    "tags": [
      "llm"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "identify_modules",
            "signature": "(issue_content, architecture_json)",
            "returns": "MODULES_TO_SYNC list and DEPS_VALID boolean"
          }
        ]
      }
    }
  },
  {
    "reason": "Centralized cloud config: routes endpoint URLs (incl. submitExample) and JWT auth honoring PDD_CLOUD_URL.",
    "description": "CloudConfig provides static methods for cloud URL resolution (get_base_url, get_endpoint_url with CLOUD_ENDPOINTS map including submitExample), environment inference (ensure_default_env), JWT auth (get_jwt_token), and cloud-environment detection (is_running_in_cloud, is_cloud_enabled). Module-level get_cloud_timeout() reads PDD_CLOUD_TIMEOUT, and get_cloud_request_timeout() returns the requests timeout tuple used by cloud POST calls. Used by sync/fix auto-submit so PDD_CLOUD_URL is honored across staging/prod/local (#859).",
    "dependencies": [
      "auth_service_python.prompt"
    ],
    "priority": 5,
    "filename": "core/cloud_python.prompt",
    "filepath": "pdd/core/cloud.py",
    "tags": [
      "cloud",
      "config",
      "python",
      "core"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "CloudConfig.get_base_url",
            "signature": "() -> str",
            "returns": "str"
          },
          {
            "name": "CloudConfig.get_endpoint_url",
            "signature": "(endpoint_name: str) -> str",
            "returns": "str"
          },
          {
            "name": "CloudConfig.get_jwt_token",
            "signature": "(verbose: bool = False, app_name: str = 'PDD Code Generator') -> Optional[str]",
            "returns": "Optional[str]"
          },
          {
            "name": "CloudConfig.ensure_default_env",
            "signature": "() -> None",
            "returns": "None"
          },
          {
            "name": "CloudConfig.is_running_in_cloud",
            "signature": "() -> bool",
            "returns": "bool"
          },
          {
            "name": "CloudConfig.is_cloud_enabled",
            "signature": "() -> bool",
            "returns": "bool"
          },
          {
            "name": "get_cloud_timeout",
            "signature": "() -> int",
            "returns": "int"
          },
          {
            "name": "get_cloud_request_timeout",
            "signature": "() -> tuple[int, int]",
            "returns": "tuple[int, int]"
          }
        ]
      }
    },
    "position": {
      "x": 1200,
      "y": 600
    }
  },
  {
    "reason": "Job queue manager for async PDD command execution via subprocesses.",
    "description": "Async job queue manager that executes PDD commands as subprocesses with concurrency control (semaphore), real-time output streaming, and robust cancellation (SIGTERM/SIGKILL). Surfaces architecture-conformance failures (markers 'Architecture conformance error for ' or '=== architecture conformance failure ===') in job.error with the structured block, 'Reproduce locally:' line, and '--- env ---' fingerprint instead of the generic 'Sync operation failed' so the GitHub App can show missing symbols and a repro command.",
    "dependencies": [],
    "priority": 220,
    "filename": "server/jobs_python.prompt",
    "filepath": "pdd/server/jobs.py",
    "tags": [
      "module",
      "python",
      "server"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "Job",
            "signature": "dataclass Job(id: str, command: str, args: Dict[str, Any], options: Dict[str, Any], status: JobStatus, result: Optional[Any], error: Optional[str], cost: float, created_at: datetime, started_at: Optional[datetime], completed_at: Optional[datetime], live_stdout: str, live_stderr: str)",
            "returns": "Job"
          },
          {
            "name": "JobCallbacks",
            "signature": "()",
            "returns": "JobCallbacks"
          },
          {
            "name": "JobManager",
            "signature": "(max_concurrent: int = 1, executor=None, project_root=None)",
            "returns": "JobManager"
          },
          {
            "name": "JobManager.submit",
            "signature": "async (command: str, args: Dict[str, Any] = None, options: Dict[str, Any] = None) -> Job",
            "returns": "Job"
          },
          {
            "name": "JobManager.cancel",
            "signature": "async (job_id: str) -> bool",
            "returns": "bool"
          },
          {
            "name": "JobManager.cleanup_old_jobs",
            "signature": "(max_age_seconds: float = 3600) -> int",
            "returns": "int"
          },
          {
            "name": "JobManager.shutdown",
            "signature": "async () -> None",
            "returns": "None"
          }
        ]
      }
    }
  },
  {
    "reason": "Shared orchestrator that finalizes prompt metadata (tags, architecture, run report, fingerprint) in a fixed order so update/auto-heal/preflight workflows leave no half-synced state.",
    "description": "Shared orchestrator that finalizes prompt metadata stages (prompt, tags, architecture, run_report, fingerprint) in a fixed order so update/auto-heal/preflight workflows leave no half-synced state.",
    "dependencies": [
      "architecture_sync_python.prompt",
      "sync_determine_operation_python.prompt",
      "operation_log_python.prompt"
    ],
    "priority": 5,
    "filename": "metadata_sync_python.prompt",
    "filepath": "pdd/metadata_sync.py",
    "tags": [
      "sync",
      "metadata",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_metadata_sync",
            "signature": "(prompt_path: Path, code_path: Optional[Path] = None, *, dry_run: bool = False, repo_root: Optional[Path] = None, architecture_path: Optional[Path] = None) -> MetadataSyncResult",
            "returns": "MetadataSyncResult with per-stage status"
          }
        ]
      }
    },
    "position": {
      "x": 24600,
      "y": 800
    }
  },
  {
    "reason": "Shared, lossless parser for `git status --porcelain=v1 -z` that preserves renames and exotic path bytes.",
    "description": "Centralized helper that parses the machine-readable `git status --porcelain=v1 -z` output: NUL-separated records, rename-aware old+new path pairs, and `os.fsdecode`-decoded paths so non-UTF-8 filenames round-trip. Replaces ad-hoc text-mode `line[3:]` / `split(' -> ')` parsers in scope guards, change detectors, and prompt-changed endpoints. See issue #1080.",
    "dependencies": [],
    "priority": 4,
    "filename": "git_porcelain_python.prompt",
    "filepath": "pdd/git_porcelain.py",
    "tags": [
      "git",
      "module",
      "python",
      "shared",
      "porcelain"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "parse_porcelain_z",
            "signature": "(stdout: Union[bytes, str]) -> List[PorcelainEntry]",
            "returns": "List[PorcelainEntry]"
          },
          {
            "name": "run_porcelain_z",
            "signature": "(cwd: Union[str, Path], *, include_untracked: bool = True, timeout: Optional[float] = 30) -> Optional[List[PorcelainEntry]]",
            "returns": "Optional[List[PorcelainEntry]]"
          },
          {
            "name": "iter_changed_paths",
            "signature": "(entries: Iterable[PorcelainEntry]) -> Iterator[str]",
            "returns": "Iterator[str]"
          }
        ]
      }
    }
  },
  {
    "reason": "Provides unified LLM invocation with provider retries, context checks, and dry-run cost estimates.",
    "description": "Provides unified LLM invocation across all PDD operations with provider abstraction, retries, and context window validation.",
    "dependencies": [
      "path_resolution_python.prompt",
      "server/token_counter_python.prompt"
    ],
    "priority": 236,
    "filename": "llm_invoke_python.prompt",
    "filepath": "pdd/llm_invoke.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "llm_invoke",
            "signature": "(prompt, input_json, strength, temperature, verbose, output_pydantic, output_schema, time, use_batch_mode, messages, language, use_cloud, grounding_overrides=None, source_prompt=None, estimate_only=None, shots=1, verifier=None, task_class=None)",
            "returns": "Dict[str, Any]"
          },
          {
            "name": "setup_file_logging",
            "signature": "(log_file_path=None)",
            "returns": "None"
          },
          {
            "name": "set_verbose_logging",
            "signature": "(verbose=False)",
            "returns": "None"
          },
          {
            "name": "set_quiet_logging",
            "signature": "()",
            "returns": "None"
          }
        ]
      }
    }
  },
  {
    "reason": "Bridges a ChatGPT/Codex subscription (codex login token in ~/.codex/auth.json) into litellm's chatgpt/ provider so llm_invoke can use it as a flat-rate fallback when ANTHROPIC_API_KEY is missing or rate-limited (issue #1269).",
    "description": "ChatGPT/Codex subscription support for llm_invoke: bridges ~/.codex/auth.json into litellm's chatgpt/ provider auth dir, applies the litellm empty-output workaround (#25429/PR#27562), detects subscription auth for the credential gate, and builds the schema-in-prompt instruction for structured output.",
    "dependencies": [],
    "priority": 236,
    "filename": "codex_subscription_python.prompt",
    "filepath": "pdd/codex_subscription.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "is_chatgpt_subscription_model",
            "signature": "(model_name)",
            "returns": "bool"
          },
          {
            "name": "bridge_codex_auth_for_litellm",
            "signature": "()",
            "returns": "bool"
          },
          {
            "name": "has_codex_subscription_auth",
            "signature": "()",
            "returns": "bool"
          },
          {
            "name": "apply_litellm_chatgpt_output_patch",
            "signature": "()",
            "returns": "bool"
          },
          {
            "name": "build_chatgpt_schema_instruction",
            "signature": "(schema)",
            "returns": "str"
          }
        ]
      }
    }
  },
  {
    "reason": "Parses prompt contract sections into a deterministic IR.",
    "description": "Extracts contract_rules, coverage, waivers, vocabulary, capabilities, and related metadata from .prompt files and stories for downstream deterministic checks.",
    "dependencies": [],
    "priority": 239,
    "filename": "contract_ir_python.prompt",
    "filepath": "pdd/contract_ir.py",
    "tags": [
      "module",
      "python",
      "contracts"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "parse_prompt_contracts",
            "signature": "(path: Path) -> PromptContractIR",
            "returns": "PromptContractIR"
          },
          {
            "name": "extract_sections",
            "signature": "(text: str) -> dict",
            "returns": "dict"
          },
          {
            "name": "extract_rules",
            "signature": "(text: str) -> list[Rule]",
            "returns": "list[Rule]"
          },
          {
            "name": "extract_waivers",
            "signature": "(text: str) -> list[Waiver]",
            "returns": "list[Waiver]"
          },
          {
            "name": "iter_covers_refs",
            "signature": "(story_text: str) -> \"Iterator[str]\"",
            "returns": "Iterator[str]"
          }
        ]
      }
    },
    "position": {
      "x": 0,
      "y": 0
    }
  },
  {
    "reason": "Deterministic contract authoring checks for prompt and story artifacts.",
    "description": "Implements structural lint for contract sections (modal verbs, IDs, coverage refs, waivers) and validates MUST-NOT coverage using leading coverage entries only.",
    "dependencies": [
      "contract_ir_python.prompt"
    ],
    "priority": 240,
    "filename": "contract_check_python.prompt",
    "filepath": "pdd/contract_check.py",
    "tags": [
      "module",
      "python",
      "contracts"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "check_prompt",
            "signature": "(path, *, strict: bool = False) -> ContractResult",
            "returns": "ContractResult"
          },
          {
            "name": "check_directory",
            "signature": "(directory, *, strict: bool = False) -> list[ContractResult]",
            "returns": "list[ContractResult]"
          },
          {
            "name": "check_stories",
            "signature": "(stories_dir, prompts_dir, *, strict: bool = False) -> list[ContractResult]",
            "returns": "list[ContractResult]"
          }
        ]
      }
    },
    "position": {
      "x": 0,
      "y": 0
    }
  },
  {
    "reason": "Exposes deterministic contract checks via Click CLI.",
    "description": "Implements `pdd contracts check` for running contract_check over files/directories with optional JSON output.",
    "dependencies": [
      "contract_check_python.prompt"
    ],
    "priority": 241,
    "filename": "commands/contracts_python.prompt",
    "filepath": "pdd/commands/contracts.py",
    "tags": [
      "module",
      "python",
      "commands",
      "contracts"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": []
      }
    },
    "position": {
      "x": 0,
      "y": 0
    }
  },
  {
    "reason": "Selects bounded source-code targets for the Claude Code simplify candidate workflow while excluding generated and ignored artifacts.",
    "description": "Discovers source files eligible for pdd checkup simplify from a path, staged changes, or a git baseline diff.",
    "dependencies": [],
    "priority": 4,
    "filename": "checkup_file_selection_python.prompt",
    "filepath": "pdd/checkup_file_selection.py",
    "tags": [
      "checkup",
      "simplify",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "discover_simplify_targets",
            "signature": "(*, path: Optional[Path] = None, since: Optional[str] = None, staged: bool = False, max_files: int = 20, cwd: Optional[Path] = None) -> tuple[Path, List[Path]]",
            "returns": "tuple[Path, List[Path]]"
          }
        ]
      }
    }
  },
  {
    "reason": "Invokes Claude Code bundled /simplify for the default simplify engine.",
    "description": "Finds the Claude CLI, builds slash messages, and runs /simplify with JSON output parsing.",
    "dependencies": [],
    "priority": 5,
    "filename": "checkup_simplify_claude_python.prompt",
    "filepath": "pdd/checkup_simplify_claude.py",
    "tags": [
      "checkup",
      "simplify",
      "claude",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "check_claude_code_simplify_available",
            "signature": "(*, quiet: bool) -> Tuple[Optional[str], Optional[Tuple[int, int, int]], Optional[str]]",
            "returns": "Tuple[Optional[str], Optional[Tuple[int, int, int]], Optional[str]]"
          },
          {
            "name": "build_simplify_slash_message",
            "signature": "(rel_files: Sequence[str], *, focus: str) -> str",
            "returns": "str"
          },
          {
            "name": "run_claude_simplify_command",
            "signature": "(slash_message: str, repo_root: Path, *, cli_path: str, verbose: bool, quiet: bool, timeout: float = DEFAULT_TIMEOUT_SECONDS) -> Tuple[bool, str, float, str]",
            "returns": "Tuple[bool, str, float, str]"
          }
        ]
      }
    }
  },
  {
    "reason": "Selects and runs simplify engines (claude, codex, gemini, opencode, auto) for checkup simplify.",
    "description": "Resolves engine names, checks provider availability, and dispatches to Claude Code or agentic workflow execution.",
    "dependencies": [
      "checkup_simplify_claude_python.prompt"
    ],
    "priority": 5,
    "filename": "checkup_simplify_engines_python.prompt",
    "filepath": "pdd/checkup_simplify_engines.py",
    "tags": [
      "checkup",
      "simplify",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "normalize_simplify_engine",
            "signature": "(engine: Optional[str], *, default: str = \"claude\") -> str",
            "returns": "str"
          },
          {
            "name": "resolve_simplify_engine",
            "signature": "(engine: str) -> str",
            "returns": "str"
          },
          {
            "name": "check_simplify_engine_available",
            "signature": "(engine: str, *, quiet: bool) -> Tuple[str, str, Optional[str]]",
            "returns": "Tuple[str, str, Optional[str]]"
          },
          {
            "name": "run_simplify_engine_command",
            "signature": "(engine: str, rel_files: Sequence[str], repo_root: Path, *, focus: str, verbose: bool, quiet: bool, timeout: float = DEFAULT_TIMEOUT_SECONDS) -> Tuple[bool, str, float, str]",
            "returns": "Tuple[bool, str, float, str]"
          }
        ]
      }
    }
  },
  {
    "reason": "Orchestrates multi-engine simplify candidates in isolated worktrees and copies back a conservatively selected verified result.",
    "description": "Provides multi-attempt candidate simplification for changed code while enforcing file scope, backups, evidence, and optional verification.",
    "dependencies": [
      "checkup_file_selection_python.prompt",
      "checkup_simplify_engines_python.prompt",
      "checkup_simplify_claude_python.prompt",
      "git_porcelain_python.prompt"
    ],
    "priority": 5,
    "filename": "checkup_simplify_python.prompt",
    "filepath": "pdd/checkup_simplify.py",
    "tags": [
      "checkup",
      "simplify",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_checkup_simplify",
            "signature": "(*, path: Optional[Path], apply: bool, since: Optional[str], staged: bool, max_files: Optional[int], attempts: Optional[int], engine: Optional[str], evidence: bool, verify: bool, no_format: bool, quiet: bool, verbose: bool) -> SimplifyRunResult",
            "returns": "SimplifyRunResult"
          }
        ]
      }
    }
  },
  {
    "reason": "Loads YAML and built-in policy for evidence gate require/allow/limit rules.",
    "description": "Resolves `pdd checkup gate --policy` overrides merged with default gate policy.",
    "dependencies": [],
    "priority": 242,
    "filename": "gate_policy_python.prompt",
    "filepath": "pdd/gate_policy.py",
    "tags": [
      "checkup",
      "gate",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "default_policy",
            "signature": "() -> GatePolicy",
            "returns": "GatePolicy"
          },
          {
            "name": "load_policy",
            "signature": "(path: Optional[Path]) -> GatePolicy",
            "returns": "GatePolicy"
          }
        ]
      }
    }
  },
  {
    "reason": "Reads dev-unit evidence manifests and exposes freshness helpers for policy enforcement.",
    "description": "Loads `.pdd/evidence/devunits/*.latest.json` and compares on-disk prompt/output hashes to manifest records.",
    "dependencies": [],
    "priority": 242,
    "filename": "evidence_store_python.prompt",
    "filepath": "pdd/evidence_store.py",
    "tags": [
      "checkup",
      "gate",
      "evidence",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "list_latest_manifests",
            "signature": "(project_root: Path) -> list[Path]",
            "returns": "list[Path]"
          },
          {
            "name": "output_freshness",
            "signature": "(manifest: ManifestView, project_root: Path) -> list[OutputFreshness]",
            "returns": "list[OutputFreshness]"
          },
          {
            "name": "prompt_freshness",
            "signature": "(manifest: ManifestView) -> bool",
            "returns": "bool"
          }
        ]
      }
    }
  },
  {
    "reason": "Checkup-scoped CLI for evidence policy gate (no top-level pdd gate).",
    "description": "Dispatches `pdd checkup gate` to `run_gate_policy` with JSON and YAML policy options.",
    "dependencies": [
      "gate_python.prompt"
    ],
    "priority": 242,
    "filename": "commands/gate_python.prompt",
    "filepath": "pdd/commands/gate.py",
    "tags": [
      "checkup",
      "gate",
      "commands",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": []
      }
    }
  },
  {
    "reason": "Evaluates evidence manifest policy for CI merge gates on dev units.",
    "description": "Runs deterministic checks on validation status, stale outputs, contract coverage, and cost limits.",
    "dependencies": [
      "evidence_store_python.prompt",
      "gate_policy_python.prompt",
      "contract_check_python.prompt",
      "coverage_contracts_python.prompt"
    ],
    "priority": 242,
    "filename": "gate_python.prompt",
    "filepath": "pdd/gate_main.py",
    "tags": [
      "checkup",
      "gate",
      "evidence",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_gate_policy",
            "signature": "(project_root: Path, *, policy_path: Optional[Path] = None, target: Optional[str] = None, stories_dir: Optional[Path] = None, tests_dir: Optional[Path] = None) -> GateResult",
            "returns": "GateResult"
          },
          {
            "name": "evaluate_manifest",
            "signature": "(manifest: ManifestView, project_root: Path, policy: GatePolicy, *, stories_dir: Optional[Path] = None, tests_dir: Optional[Path] = None) -> list[GateFailure]",
            "returns": "list[GateFailure]"
          }
        ]
      }
    }
  },
  {
    "reason": "Loads optional .pdd/grounding_policy.yaml and checks per-run grounding evidence against required-review and required-pin rules for critical modules.",
    "description": "Policy library for the grounding evidence recorded by pdd generate. Library-only; CI integration (pdd gate) will be wired separately.",
    "dependencies": [],
    "priority": 243,
    "filename": "grounding_policy_python.prompt",
    "filepath": "pdd/grounding_policy.py",
    "tags": [
      "module",
      "python",
      "grounding",
      "policy"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "load_policy",
            "signature": "(path: Optional[str] = None) -> GroundingPolicy",
            "returns": "GroundingPolicy"
          },
          {
            "name": "check",
            "signature": "(policy: GroundingPolicy, module: str, grounding: Dict[str, Any]) -> List[PolicyViolation]",
            "returns": "List[PolicyViolation]"
          }
        ]
      }
    },
    "position": {
      "x": 0,
      "y": 0
    }
  },
  {
    "reason": "Writes optional per-run JSON evidence manifests, including generation.grounding provenance.",
    "description": "Hashes prompts, includes, outputs, and validation metadata into .pdd/evidence without replacing core dumps. Records generation.grounding provenance when provided.",
    "dependencies": [
      "context_snapshot_python.prompt"
    ],
    "priority": 244,
    "filename": "pdd/evidence_manifest_python.prompt",
    "filepath": "pdd/evidence_manifest.py",
    "tags": [
      "module",
      "python",
      "evidence"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "write_evidence_manifest",
            "signature": "(*, command: str, prompt_file: Optional[str | Path] = None, output_files: Iterable[str | Path] = (), model: str = \"\", cost_usd: float = 0.0, temperature: Optional[float] = None, validation: Optional[Mapping[str, str]] = None, logs: Optional[Mapping[str, Optional[str]]] = None, basename: Optional[str] = None, project_root: Optional[str | Path] = None, context_snapshot: Optional[Mapping[str, Any]] = None, grounding: Optional[Mapping[str, Any]] = None, reviewed: bool = False, compression: Optional[Mapping[str, Any]] = None, agentic_fallback: Optional[Mapping[str, Any]] = None, compress: bool = False) -> Path",
            "returns": "Path"
          },
          {
            "name": "validation_from_sync",
            "signature": "(sync_result, *, skip_tests, skip_verify, dry_run=False) -> dict",
            "returns": "dict"
          },
          {
            "name": "resolve_generate_output_paths",
            "signature": "(prompt_file: str | Path, *, output: Optional[str] = None, context_override: Optional[str] = None, force: bool = True, quiet: bool = True) -> list[str]",
            "returns": "list[str]"
          },
          {
            "name": "resolve_test_output_paths",
            "signature": "(prompt_file: str | Path, code_file: str | Path, *, output: Optional[str] = None, language: Optional[str] = None, context_override: Optional[str] = None, force: bool = True, quiet: bool = True) -> list[str]",
            "returns": "list[str]"
          }
        ]
      }
    },
    "position": {
      "x": 0,
      "y": 0
    }
  },
  {
    "reason": "AST-based pytest context slicer for precise extraction of test context and dependencies.",
    "description": "Precisely slices pytest files to include only relevant test context and its transitive closure of dependencies (fixtures, helpers, imports).",
    "dependencies": [],
    "priority": 100,
    "filename": "pytest_slicer_python.prompt",
    "filepath": "pdd/pytest_slicer.py",
    "tags": [
      "module",
      "python",
      "testing"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "PytestSlicer.__init__",
            "signature": "(main_source: str, file_path: str | None = None)",
            "returns": "None"
          },
          {
            "name": "PytestSlicer.slice",
            "signature": "(test_names: List[str])",
            "returns": "Tuple[str, SlicedManifest]"
          }
        ]
      }
    }
  },
  {
    "reason": "AST-based API/dependency contract slicing for compressed generation (#875).",
    "description": "Preserves imported symbols, patch targets, and transitive local helpers/constants when compressing module context.",
    "dependencies": [],
    "priority": 99,
    "filename": "api_contract_slicer_python.prompt",
    "filepath": "pdd/api_contract_slicer.py",
    "tags": [
      "module",
      "python",
      "context"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "ApiContractSlicer.slice",
            "signature": "(seeds: List[str])",
            "returns": "Tuple[str, ContractManifest]"
          }
        ]
      }
    }
  },
  {
    "reason": "Provides precise extraction of file content based on criteria like lines, AST, and regex for selective includes.",
    "description": "Deterministic content extraction from files based on line ranges, Python AST structures, Markdown sections, regex patterns, and JSON/YAML path traversal.",
    "dependencies": [
      "pytest_slicer_python.prompt",
      "api_contract_slicer_python.prompt"
    ],
    "priority": 101,
    "filename": "content_selector_python.prompt",
    "filepath": "pdd/content_selector.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "ContentSelector.select",
            "signature": "(content: str, selectors: list[str] | str, file_path: str | None = None, mode: str = \"full\")",
            "returns": "str"
          }
        ]
      }
    }
  },
  {
    "reason": "Ranks candidate test files by a four-signal composite score and packs them under a configurable token budget, emitting a TestPackingManifest for every selection and omission decision.",
    "dependencies": [
      "pytest_slicer_python.prompt"
    ],
    "priority": 102,
    "filename": "test_context_packer_python.prompt",
    "filepath": "pdd/test_context_packer.py",
    "tags": [
      "module",
      "python",
      "testing",
      "context"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "TestContextPacker.pack",
            "signature": "(module_path: str, failing_tests: list[str] | None = None, budget_tokens: int | None = None, candidate_files: list[str] | None = None, slice_failing_tests: bool = False) -> TestPackingResult",
            "returns": "TestPackingResult"
          }
        ]
      }
    }
  },
  {
    "reason": "Preprocesses prompt files by resolving include/shell/web tags, handling selective extraction, and escaping curly braces.",
    "description": "Preprocesses prompt files by resolving include/shell/web tags, handling selective extraction, and escaping curly braces.",
    "dependencies": [
      "path_resolution_python.prompt",
      "content_selector_python.prompt",
      "include_query_extractor_python.prompt",
      "context_snapshot_python.prompt"
    ],
    "priority": 243,
    "filename": "preprocess_python.prompt",
    "filepath": "pdd/preprocess.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "preprocess",
            "signature": "(prompt, recursive=False, double_curly_brackets=True, exclude_keys=None, compress=False, examples_dir=\"examples/\", tests_dir=\"tests/\", _seen=None, _failed=None, _user_intent_paths=None, snapshot_recorder=None, exclude=None)",
            "returns": "str"
          },
          {
            "name": "get_file_path",
            "signature": "(file_name)",
            "returns": "str"
          }
        ]
      }
    }
  },
  {
    "reason": "Defines miscellaneous CLI commands, including preprocess with snapshot forwarding.",
    "description": "Defines miscellaneous CLI commands, including preprocess with snapshot forwarding.",
    "dependencies": [
      "preprocess_main_python.prompt"
    ],
    "priority": 244,
    "filename": "commands/misc_python.prompt",
    "filepath": "pdd/commands/misc.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "preprocess",
            "description": "Preprocess prompt files with optional XML tagging, PDD tag injection, and context snapshots"
          }
        ]
      }
    }
  },
  {
    "reason": "Defines frontend TypeScript type contracts for commands, options, architecture, and config.",
    "description": "Defines frontend TypeScript type contracts for commands, options, architecture, and config.",
    "dependencies": [],
    "priority": 246,
    "filename": "frontend/types_typescript.prompt",
    "filepath": "pdd/frontend/types.ts",
    "tags": [
      "module"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": []
      }
    }
  },
  {
    "reason": "Owns durable snapshot and replay primitives for expanded prompt context.",
    "description": "Owns durable snapshot and replay primitives for expanded prompt context.",
    "dependencies": [],
    "priority": 247,
    "filename": "context_snapshot_python.prompt",
    "filepath": "pdd/context_snapshot.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "start_snapshot_run",
            "signature": "(prompt_path: str | Path, *, evidence_root: str | Path | None = None, command: str = \"\")",
            "returns": "ContextSnapshotRecorder"
          },
          {
            "name": "replay_snapshot",
            "signature": "(run_artifact: str | Path)",
            "returns": "Dict[str, Any]"
          }
        ]
      }
    }
  },
  {
    "reason": "Implements the top-level replay command for auditing expanded prompt context snapshots.",
    "description": "Implements the top-level replay command for auditing expanded prompt context snapshots.",
    "dependencies": [
      "context_snapshot_python.prompt"
    ],
    "priority": 248,
    "filename": "commands/replay_python.prompt",
    "filepath": "pdd/commands/replay.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "replay",
            "description": "Reconstruct and audit expanded prompt context from a snapshot run artifact"
          }
        ]
      }
    }
  },
  {
    "reason": "Reports per-source token attribution for a hydrated prompt so users can audit context-window cost before generation, displayed by default as a Claude-Code /context-style usage box.",
    "description": "Implements the `pdd context` CLI command that preprocesses a prompt file the same way generation does and reports token attribution broken down by source (prompt body, each include, tests, examples, shell output, web output, grounding), rendered by default as a Claude-Code /context-style usage box (--table for the raw table, --json for machine-readable output).",
    "dependencies": [
      "commands/replay_python.prompt",
      "context_audit_python.prompt"
    ],
    "priority": 249,
    "filename": "commands/context_python.prompt",
    "filepath": "pdd/commands/context.py",
    "tags": [
      "cli",
      "python"
    ],
    "interface": {
      "type": "cli",
      "cli": {
        "commands": [
          {
            "name": "context",
            "description": "Show context-window usage by source for a preprocessed prompt"
          }
        ]
      }
    }
  },
  {
    "reason": "Shared, deterministic, no-LLM context-audit core consumed by both the pdd context CLI and the pdd connect context-audit endpoint, so the two surfaces never drift.",
    "description": "Implements the pdd/context_audit.py core: preprocesses a prompt the same way generation's first pass does and returns a structured ContextAudit of per-source token attribution (prompt body, each include, grounding) with stable per-row status (resolved/unresolved/deferred/unavailable/body), surfacing unresolved includes and deferred dynamic tags. Presentation-free and applies no budget policy; reuses pdd.server.token_counter and pdd.preprocess and makes no LLM call.",
    "dependencies": [],
    "priority": 249,
    "filename": "context_audit_python.prompt",
    "filepath": "pdd/context_audit.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "audit_prompt_file",
            "signature": "(prompt_path: str, model: str)",
            "returns": "ContextAudit"
          },
          {
            "name": "audit_prompt_text",
            "signature": "(text: str, model: str, *, base_dir: Optional[str] = None)",
            "returns": "ContextAudit"
          },
          {
            "name": "percent",
            "signature": "(part: int, whole: Optional[int])",
            "returns": "Optional[float]"
          },
          {
            "name": "row_percent",
            "signature": "(row: AuditRow, total_tokens: int)",
            "returns": "float"
          },
          {
            "name": "threshold_exceeded",
            "signature": "(percent_used: Optional[float], threshold: int)",
            "returns": "bool"
          }
        ]
      }
    }
  },
  {
    "reason": "Builds deterministic phase-aware compressed context packages for sync LLM phases.",
    "dependencies": [
      "context_snapshot_python.prompt",
      "contract_ir_python.prompt",
      "operation_log_python.prompt",
      "test_context_packer_python.prompt"
    ],
    "priority": 249,
    "filename": "compressed_sync_context_python.prompt",
    "filepath": "pdd/compressed_sync_context.py",
    "tags": [
      "module",
      "sync",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "build_compressed_sync_context",
            "signature": "(phase, prompt_path, code_path=None, example_path=None, test_paths=None, run_report_path=None, contract_summary=None, repair_directive=None, budget=None, context_compression=None) -> CompressedSyncContext",
            "returns": "CompressedSyncContext"
          },
          {
            "name": "render_for_prompt",
            "signature": "(context: CompressedSyncContext) -> str",
            "returns": "str"
          }
        ]
      }
    }
  },
  {
    "reason": "Shared blocking quality gate that drift-syncs and build/smoke-verifies a PR for pre-checkup or fix-final validation.",
    "description": "A blocking quality gate shared by PR-producing orchestrators. The change/feature orchestrator runs it immediately before handing off to `pdd checkup --pr`; the fix/bug e2e orchestrator runs it as the final deterministic verification before returning success. Runs two ordered phases against the PR worktree and returns a single hard pass/fail the caller uses to block checkup handoff for change callers or success return for fix callers. Phase 1 (drift-sync) brings code, prompt, architecture.json and .pdd/meta into sync in the worktree via run_metadata_sync / sync_all_prompts_to_architecture / detect_drift+heal_module, then re-detects to confirm. .pdd/meta fingerprints written here via run_metadata_sync cover prompt+code only (example/test hashes are null), so canonical fingerprint finalization belongs to the post-merge sync (#1317): BOTH orchestrator paths defer it — the fix path filters .pdd/** from its commit, the change path restores tracked .pdd/meta after the gate — so the PR tree may still show fingerprint drift until the post-merge sync completes it. Phase 2 (build/smoke) runs deterministic per-file checks (py_compile/ruff/tsc) plus a project smoke layer (import changed modules, route/router-object probe, caller-compatibility sweep over both from-import and alias-import call sites, git-diff-targeted unit tests including directly-changed test files) and hard-blocks on any red result. Closes the vacuous-pass hole in run_ci_validation_loop by running its own checks rather than only polling GitHub required checks; preserves the fork-PR-RCE constraint by delegating to checkup_gates._script_is_acceptable, which runs only validated, safe package.json scripts (never build scripts) and skips the npm-family path entirely when the PR touches package.json / a package-manager config; the gate adds no command execution of its own beyond that allowlist. For checkup-bound callers it prepares the PR for cross-model review; for `pdd fix`, `pdd checkup --pr --issue --final-gate` remains a separate explicit command. Docs-only diffs short-circuit; strict mode reuses PDD_STRICT_DOC_SYNC (issue #1293).",
    "dependencies": [
      "checkup_gates_python.prompt",
      "ci_drift_heal_python.prompt",
      "metadata_sync_python.prompt",
      "architecture_sync_python.prompt",
      "operation_log_python.prompt"
    ],
    "priority": 218,
    "filename": "pre_checkup_gate_python.prompt",
    "filepath": "pdd/pre_checkup_gate.py",
    "tags": [
      "checkup",
      "ci",
      "gates",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_pre_checkup_gate",
            "signature": "(worktree: Path, changed_files: Sequence[str], *, base_ref: Optional[str] = None, repo_owner: str = '', repo_name: str = '', issue_number: Optional[int] = None, strict: Optional[bool] = None, quiet: bool = False, timeout_per_check: float = 300.0) -> Tuple[bool, str, float]",
            "returns": "Tuple[bool, str, float]"
          }
        ]
      }
    }
  },
  {
    "reason": "Non-interactive prompt source-set repair orchestrator using the internal change() contract.",
    "description": "Implements run_prompt_repair_loop(): consumes pdd.prompt_source_set_report.v1 findings and supporting context, synthesizes bounded change instructions, applies the complete prompt through pdd.change.change(), enforces token growth, rebuilds the full source-set report after each round, and writes an audit artifact to .pdd/evidence/prompt_repair/.",
    "dependencies": [
      "change_python.prompt",
      "checkup_prompt_main_python.prompt",
      "prompt_lint_python.prompt"
    ],
    "priority": 250,
    "filename": "prompt_repair_python.prompt",
    "filepath": "pdd/prompt_repair.py",
    "tags": [
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "functions": [
          {
            "name": "run_prompt_repair_loop",
            "signature": "(path: Path, config: PromptRepairConfig, *, context: Optional[Dict[str, str]] = None, cwd: Optional[Path] = None) -> RepairResult",
            "returns": "RepairResult"
          }
        ]
      }
    }
  },
  {
    "reason": "Defines the engine-agnostic interactive prompt-repair session protocol and fake backend.",
    "description": "Defines typed repair options, approved patches, a structural interactive repair session protocol, and an in-memory fake backend for checkup prompt repair.",
    "dependencies": [],
    "priority": 240,
    "filename": "checkup_interactive_session_python.prompt",
    "filepath": "pdd/checkup_interactive_session.py",
    "tags": [
      "module",
      "python",
      "checkup"
    ],
    "interface": {
      "type": "module",
      "module": {
        "dataclasses": [
          "ApprovedPatch",
          "RepairOption"
        ],
        "functions": [
          {
            "name": "InteractiveRepairSession",
            "returns": "Protocol"
          },
          {
            "name": "FakeInteractiveSession",
            "signature": "(options_by_finding=None, answers=None)",
            "returns": "FakeInteractiveSession"
          }
        ]
      }
    }
  },
  {
    "reason": "Deterministic validator and applicator for approved interactive checkup prompt patches.",
    "description": "Validates ApprovedPatch objects, enforces safe prompt/story target allowlists including pdd/prompts/**/*.prompt, supports dry-run apply logs, creates backups for real writes, runs deterministic postflight checkup, and reports rejected approved patches as non-pass/non-zero outcomes.",
    "dependencies": [
      "checkup_interactive_session_python.prompt",
      "checkup_prompt_main_python.prompt"
    ],
    "priority": 242,
    "filename": "checkup_prompt_apply_python.prompt",
    "filepath": "pdd/checkup_prompt_apply.py",
    "tags": [
      "checkup",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "classes": [
          {
            "name": "ApplyFindingRecord",
            "kind": "dataclass"
          },
          {
            "name": "ApplyRunResult",
            "kind": "dataclass"
          }
        ],
        "functions": [
          {
            "name": "apply_approved_patches",
            "signature": "(patches: Sequence[ApprovedPatch], *, repo_root: Path, original_target: str, dry_run: bool = False, interactive: bool = True, strict: bool = False, choices_by_finding: Mapping[str, int] | None = None) -> ApplyRunResult",
            "returns": "ApplyRunResult"
          }
        ]
      }
    }
  },
  {
    "reason": "Interactive prompt checkup orchestration for explicit pdd checkup --interactive repair sessions.",
    "description": "Builds source-set reports, filters findings for explicit or clarification-only interactive repair, presents Option A/Option B/keep current/custom/rationale affordances, records typed repair choices, and delegates approved patch application with truthful rejected-patch output.",
    "dependencies": [
      "checkup_interactive_session_python.prompt",
      "checkup_prompt_apply_python.prompt",
      "checkup_prompt_main_python.prompt"
    ],
    "priority": 243,
    "filename": "checkup_interactive_main_python.prompt",
    "filepath": "pdd/checkup_interactive_main.py",
    "tags": [
      "checkup",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "classes": [
          {
            "name": "ClickInteractiveSession",
            "kind": "class"
          }
        ],
        "functions": [
          {
            "name": "filter_interactive_findings",
            "signature": "(report: PromptSourceSetReport, *, explicit_interactive: bool) -> list[SourceSetFinding]",
            "returns": "list[SourceSetFinding]"
          },
          {
            "name": "build_repair_options_for_finding",
            "signature": "(finding: SourceSetFinding) -> list[RepairOption]",
            "returns": "list[RepairOption]"
          },
          {
            "name": "run_interactive_checkup",
            "signature": "(target: str, *, apply: bool = False, dry_run: bool = False, project_root: Optional[Path] = None, strict: bool = False, quiet: bool = False, explicit_interactive: bool = True, session_factory: Optional[Callable[[], InteractiveRepairSession]] = None) -> Optional[tuple[str, float, str]]",
            "returns": "Optional[tuple[str, float, str]]"
          }
        ]
      }
    }
  },
  {
    "reason": "Agentic checkup session runner for explicitly requested prompt review, interactive, and auto modes.",
    "description": "Runs CheckupAgent only after explicit CLI intent, coordinates planner, terminal/session events, risk-gated grouped repair choices, Option A/Option B/keep current/custom/rationale UX, patch application, verification, artifacts, accounting, and blocking decisions.",
    "dependencies": [
      "checkup_interactive_main_python.prompt",
      "checkup_planner_python.prompt",
      "checkup_report_python.prompt",
      "checkup_tools_python.prompt",
      "checkup_prompt_main_python.prompt"
    ],
    "priority": 244,
    "filename": "checkup_agent_python.prompt",
    "filepath": "pdd/checkup_agent.py",
    "tags": [
      "checkup",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "classes": [
          {
            "name": "AgentEvent",
            "kind": "dataclass"
          },
          {
            "name": "CheckupSession",
            "kind": "class"
          },
          {
            "name": "TerminalCheckupSession",
            "kind": "class"
          },
          {
            "name": "RecordingCheckupSession",
            "kind": "class"
          },
          {
            "name": "CheckupAgent",
            "kind": "class"
          }
        ],
        "constants": [
          {
            "name": "MODE_REVIEW",
            "value": "review"
          },
          {
            "name": "MODE_INTERACTIVE",
            "value": "interactive"
          },
          {
            "name": "MODE_AUTO",
            "value": "auto"
          }
        ]
      }
    }
  },
  {
    "reason": "Planning strategy abstraction for explicit agentic prompt checkup sessions.",
    "description": "Defines deterministic and LLM-backed planners plus a planner factory for explicit agentic checkup sessions; deterministic planning is stable and CI-safe.",
    "dependencies": [
      "checkup_tools_python.prompt"
    ],
    "priority": 241,
    "filename": "checkup_planner_python.prompt",
    "filepath": "pdd/checkup_planner.py",
    "tags": [
      "checkup",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "classes": [
          {
            "name": "Plan",
            "kind": "dataclass"
          },
          {
            "name": "Planner",
            "kind": "protocol"
          },
          {
            "name": "DeterministicPlanner",
            "kind": "class"
          }
        ],
        "functions": [
          {
            "name": "make_planner",
            "signature": "(name: str) -> DeterministicPlanner | LLMPlanner",
            "returns": "DeterministicPlanner | LLMPlanner"
          }
        ]
      }
    }
  },
  {
    "reason": "Grouping, risk classification, accounting, and artifact helpers for agentic prompt checkup sessions.",
    "description": "Groups source-set findings, classifies deterministic repair risk, renders concise summaries, writes checkup artifacts, and maintains truthful accounting for queued, applied, skipped, saved-for-review, failed, and remaining repairs.",
    "dependencies": [
      "checkup_prompt_main_python.prompt"
    ],
    "priority": 241,
    "filename": "checkup_report_python.prompt",
    "filepath": "pdd/checkup_report.py",
    "tags": [
      "checkup",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "classes": [
          {
            "name": "FindingGroup",
            "kind": "dataclass"
          },
          {
            "name": "CheckupAccounting",
            "kind": "dataclass"
          }
        ],
        "functions": [
          {
            "name": "repair_risk_for",
            "signature": "(finding: SourceSetFinding | None) -> str",
            "returns": "str"
          },
          {
            "name": "group_findings",
            "signature": "(findings: Sequence[SourceSetFinding]) -> list[FindingGroup]",
            "returns": "list[FindingGroup]"
          },
          {
            "name": "decision_for",
            "signature": "(status: str, *, strict: bool, blocking: bool) -> tuple[str, bool]",
            "returns": "tuple[str, bool]"
          },
          {
            "name": "write_artifacts",
            "returns": "dict[str, Path]"
          }
        ]
      }
    }
  },
  {
    "reason": "Adapter layer from unified prompt source-set reports to named agentic checkup tools.",
    "description": "Provides read-only tool adapters for lint, contract, coverage, gate, snapshot, and drift results already present in PromptSourceSetReport, including status, skip reason, and tool-scoped findings.",
    "dependencies": [
      "checkup_prompt_main_python.prompt"
    ],
    "priority": 240,
    "filename": "checkup_tools_python.prompt",
    "filepath": "pdd/checkup_tools.py",
    "tags": [
      "checkup",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "classes": [
          {
            "name": "ToolResult",
            "kind": "dataclass"
          },
          {
            "name": "CheckupTool",
            "kind": "protocol"
          }
        ],
        "constants": [
          {
            "name": "ALL_TOOLS",
            "kind": "dict"
          }
        ]
      }
    }
  },
  {
    "reason": "Owns the normalized pdd.prompt_source_set_report.v1 report and the deterministic per-engine clarification heuristics that route findings to interactive vs. automated repair.",
    "description": "Single owner of the pdd.prompt_source_set_report.v1 report consumed by `pdd checkup` prompt targets and downstream interactive/non-interactive prompt repair. Orchestrates the deterministic lint, contract, coverage, gate, snapshot, and drift engines, aggregates their results into one PromptSourceSetReport of SourceSetFinding objects, owns as_dict() serialization, and applies code/status-driven heuristics that set requires_clarification and a compact clarification_reason without calling an LLM or mutating source.",
    "dependencies": [
      "prompt_lint_python.prompt",
      "contract_check_python.prompt",
      "coverage_contracts_python.prompt",
      "gate_python.prompt",
      "construct_paths_python.prompt",
      "user_story_tests_python.prompt",
      "evidence_store_python.prompt"
    ],
    "priority": 245,
    "filename": "checkup_prompt_main_python.prompt",
    "filepath": "pdd/checkup_prompt_main.py",
    "tags": [
      "checkup",
      "module",
      "python"
    ],
    "interface": {
      "type": "module",
      "module": {
        "constants": [
          {
            "name": "REPORT_SCHEMA",
            "value": "pdd.prompt_source_set_report.v1"
          }
        ],
        "classes": [
          {
            "name": "SourceSetFinding",
            "kind": "dataclass"
          },
          {
            "name": "PromptSourceSetReport",
            "kind": "dataclass"
          }
        ],
        "functions": [
          {
            "name": "build_prompt_source_set_report",
            "signature": "(prompt_path: Path, *, target: str, project_root: Optional[Path] = None, strict: bool = False, stories_dir: Optional[Path] = None, tests_dir: Optional[Path] = None) -> PromptSourceSetReport",
            "returns": "PromptSourceSetReport"
          },
          {
            "name": "run_checkup_prompt",
            "signature": "(target: str, *, explain: bool = False, as_json: bool = False, quiet: bool = False, strict: bool = False, project_root: Optional[Path] = None) -> tuple[bool, str, float, str, int]",
            "returns": "tuple[bool, str, float, str, int]"
          },
          {
            "name": "run_checkup_prompt_paths",
            "signature": "(prompt_paths: Sequence[Path], *, target: str = '', project_root: Optional[Path] = None, strict: bool = False) -> tuple[list[PromptSourceSetReport], int]",
            "returns": "tuple[list[PromptSourceSetReport], int]"
          },
          {
            "name": "render_prompt_source_set_report",
            "signature": "(report: PromptSourceSetReport, *, quiet: bool = False) -> None",
            "returns": "None"
          },
          {
            "name": "render_automatic_gate_summary",
            "signature": "(reports: Sequence[PromptSourceSetReport], *, mode: str, quiet: bool = False) -> None",
            "returns": "None"
          }
        ]
      }
    }
  }
]
