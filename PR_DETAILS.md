## Summary
Adds failing tests that detect the bug reported in #1248.

## Test Files
- Unit test: `tests/test_issue_1248_reproduction.py`

## Prompt Files
- Prompt file fixed in Step 7: `pdd/prompts/agentic_arch_step7_generate_LLM.prompt`, `pdd/prompts/ci_drift_heal_python.prompt`

## What This PR Contains
- Failing unit test that reproduces the reported bug
- Prompt file fixes from Step 7
- Tests are verified to fail on current code and will pass once the bug is fixed

## Root Cause
The metadata files in .pdd/meta/ are currently tracked in git due to a legacy workaround (Issue #1006) implemented as a block of negation rules in .gitignore. These rules were added to ensure that state-tracking fingerprints and run reports are available in CI environments to avoid slow pdd sync runs that hit timeouts. However, these files are runtime artifacts that should ideally be ignored. The "bug" is the continued presence of this workaround, which is now considered technical debt. My analysis reveals that removing these entries from .gitignore without updating dependent code and prompt templates will break CI auto-heal staging and lead to the pattern being re-introduced by agents.

## Next Steps
1. [ ] Implement the fix at the identified location
2. [ ] Verify the unit test passes
3. [ ] Verify the E2E test passes
4. [ ] Run full test suite
5. [ ] Mark PR as ready for review

Fixes #1248

---
*Generated by PDD agentic bug workflow*
