## Summary
Adds failing tests that detect the bug reported in #1248.

## Test Files
- Unit test: `tests/test_issue_1248_reproduction.py`
- E2E test: `(none)`

## Prompt Files
- Prompt file fixed in Step 7: `pdd/prompts/agentic_arch_step7_generate_LLM.prompt`, `pdd/prompts/ci_drift_heal_python.prompt`

## What This PR Contains
- Failing unit test that reproduces the reported bug
- Prompt file fixes from Step 7
- Tests are verified to fail on current code and will pass once the bug is fixed

## Root Cause
Metadata files in .pdd/meta/ are currently tracked due to a legacy workaround for Issue #1006. Negation rules in .gitignore, mandatory staging verification in pdd/ci_drift_heal.py, and prompt templates all enforce this behavior, which is now considered technical debt. Removing these without updating the logic breaks CI workflows.

## Next Steps
1. [ ] Implement the fix at the identified location (.gitignore, pdd/ci_drift_heal.py)
2. [ ] Verify the unit test passes
3. [ ] Verify the E2E test passes (if applicable)
4. [ ] Run full test suite
5. [ ] Mark PR as ready for review

Fixes #1248

---
*Generated by PDD agentic bug workflow*
