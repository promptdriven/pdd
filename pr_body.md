## Summary
Adds failing tests that detect the bug reported in #882.

## Test Files
- Unit test: `tests/test_issue_882_reproduction.py`

## What This PR Contains
- Failing unit test that reproduces the reported bug
- Tests are verified to fail on current code and will pass once the bug is fixed

## Root Cause
The `run_agentic_task` function returns a 4-element tuple, but `pdd/update_main.py` fails to unpack this tuple and instead treats it as a string when searching for the `<updated-prd>` tags.

## Next Steps
1. [ ] Implement the fix at the identified location
2. [ ] Verify the unit test passes
3. [ ] Run full test suite
4. [ ] Mark PR as ready for review

Fixes #882

---
*Generated by PDD agentic bug workflow*
