## Summary
Adds failing tests that detect the bug reported in #1238.

## Test Files
- Unit test: `tests/test_sync_orchestration.py`, `tests/test_sync_determine_operation.py`

## Prompt Files
- Prompt file fixed in Step 7: `pdd/prompts/sync_orchestration_python.prompt`

## What This PR Contains
- Failing unit test that reproduces the reported bug
- Prompt file fixes from Step 7
- Tests are verified to fail on current code and will pass once the bug is fixed

## Root Cause
The root cause analysis identifies two bugs. Bug (a) is located in `pdd/sync_orchestration.py` where `operation_history.append(operation)` at line 2202 adds no-op iterations before execution, causing the 5-consecutive-fix guard at line 2252 to incorrectly trip on 0-cost iterations. Bug (b) is caused by `sync_determine_operation` reading stale cached test-result state from prior runs, which repeatedly recommends a fix on fresh invocations. Together, these bugs cause the limit to be exhausted rapidly without actual LLM requests.

## Next Steps
1. [ ] Implement the fix at the identified location
2. [ ] Verify the unit test passes
3. [ ] Verify the E2E test passes
4. [ ] Run full test suite
5. [ ] Mark PR as ready for review

Fixes #1238

---
*Generated by PDD agentic bug workflow*
