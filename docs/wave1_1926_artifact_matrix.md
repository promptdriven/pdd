# Wave 1 #1926 artifact matrix

This record classifies the artifacts reconciled for the transactional
finalizer repair. `REQUIRED_UPDATED` means this candidate refreshed the
artifact from its authoritative writer; `REQUIRED_ALREADY_CURRENT` means the
current source or mapping was verified without an unrelated rewrite.

| Unit | Classification | Current evidence |
| --- | --- | --- |
| `commands/modify` | `REQUIRED_UPDATED` | The prompt states that a transactional metadata/fingerprint failure exits non-zero. `.pdd/meta/commands_modify_python.json` was regenerated by `save_fingerprint` with current prompt, code, example, `tests/commands/test_modify.py`, `test_files`, and include hashes; its paired run record reports 46 passing tests. The exact `architecture.json` path remains `pdd/commands/modify.py`. |
| `metadata_sync` | `REQUIRED_UPDATED` | The prompt now declares `fingerprint_transaction_python.prompt`; the architecture dependency is the same. `.pdd/meta/metadata_sync_python.json` was regenerated with current prompt/code/example/test/include hashes and `.pdd/meta/metadata_sync_python_run.json` was published through `AtomicStateUpdate` after the focused 47-test run, including real-owner injections for both pending-buffer mutations. Its exact architecture path is `pdd/metadata_sync.py`. |
| `fingerprint_transaction` | `REQUIRED_UPDATED` | The prompt and code now require canonical role authority even for absent root defaults. `tests/test_transactional_finalizer.py` supplies root-fallback, custom/nested configuration, test-family, concurrency, recovery, and failure-injection coverage. The canonical metadata record was regenerated with `tests/test_fingerprint_transaction.py`; the additional finalizer regression file is test-only evidence, not an alternate PDD role mapping. The focused direct test passed (1); a per-unit run JSON is `NOT_APPLICABLE` because the repository ignores that record. Its exact architecture path is `pdd/fingerprint_transaction.py`. |
| `sync_orchestration` | `REQUIRED_UPDATED` | Its prompt/code change remains represented by regenerated prompt/code/test/example/include hashes in `.pdd/meta/sync_orchestration_python.json`. The focused crash/skip set passed (33 selected, 184 deselected) and exercised its paired-state path; a per-unit run JSON is `NOT_APPLICABLE` because the repository ignores that record. Its exact architecture path is `pdd/sync_orchestration.py`. |
| `pin_example_hack` | `REQUIRED_UPDATED` | The missing exact `architecture.json` entry now maps `pin_example_hack_python.prompt` to `pdd/pin_example_hack.py`, with the real `_execute_tests_and_create_run_report` interface and dependencies. Its regenerated metadata covers prompt/code/example/test/include hashes; its paired run record reports 2 passing tests. |
| `json_atomic` | `REQUIRED_ALREADY_CURRENT` / `HUMAN_OWNED` | `docs/pdd_hand_owned_modules.md` records the explicit durability-primitive exemption. It is not regeneration-safe under the prompting guide's declared-interface/behavioral/negative-test mold, so it must remain hand-owned until a separately reviewable generated interface and complete verification mold exist. Direct caller and transactional failure coverage includes `tests/test_transactional_finalizer.py` and `tests/test_auto_deps_architecture.py`; changes require write, fsync, replace, and cleanup failure coverage. |

All prompt-owned rows use the repository root `.pddrc`; the canonical resolver
also has focused positive coverage for a custom output directory and a nested
`.pddrc` root. Exact architecture paths and dependency declarations, rather
than matching filenames or artifact existence, are the authority for their
role mappings.
