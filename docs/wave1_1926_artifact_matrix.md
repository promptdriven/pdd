# Wave 1 #1926 artifact matrix

This record classifies the artifacts reconciled for the transactional
finalizer repair. `REQUIRED_UPDATED` means this candidate refreshed the
artifact from its authoritative writer; `REQUIRED_ALREADY_CURRENT` means the
current source or mapping was verified without an unrelated rewrite.

## Local D1 Phase B snapshot

This local-only D1 snapshot is compared to the frozen schema-2 base
`e7735e0f35a0915707142bfd4c767df59f8c3b9e`.  Each changed prompt has its
target byte digest recorded in the corresponding complete human-attestation
profile.  No requirement-rotation row is added: the protected Phase A trust
root remains deliberately absent, so this snapshot is not self-authorized.

`verification-profiles.json` D1 SHA-256:
`6e558bdd16e45c009173a85db65eaebbd2fd1ae12aecbf09325522fc785d3a37`.

| Prompt | Base SHA-256 | D1 target SHA-256 |
| --- | --- | --- |
| `pdd/prompts/auto_deps_main_python.prompt` | `a40eca375371f505c237157552228b1c4c9dc7b30f7849abc164f5b9a94bedf9` | `07fcec9880392acfdea0ea83c8f74d42978b4842d142aac6c14e280662522965` |
| `pdd/prompts/commands/maintenance_python.prompt` | `5f50939cf7cd9dd3cf1269d20cef4ba1e3bbe8c242bd33ccfc4fdeef51d0cda6` | `013f4e0fed81b8de5eabc36660078225f7dce8a0b2c6f829d04aa3c54298ff19` |
| `pdd/prompts/commands/modify_python.prompt` | `8d103ee748926ef9683ba4e0ab70ecfd32b91e9437eaf435767bb42d0beda647` | `aea4d957c28d71605de6694ea78ac4136f7b2d960cb72172b03fed5e496dd2fb` |
| `pdd/prompts/fingerprint_transaction_python.prompt` | `ABSENT` | `45cbca4446a3d0a1cbccf66422dac13df17ab9e197ea4a1fe1042b654c3201f9` |
| `pdd/prompts/metadata_sync_python.prompt` | `c9dee496de479efe665cc44767871a5530bb09cdbcc797d8603fd6e3191d5561` | `538f6324f078c0c9f8f39685946fbbb5a0532b89eab3bebea7414f4aa6584f56` |
| `pdd/prompts/operation_log_python.prompt` | `e9314dff3064004dafe75821e7aec984cbbec76e4745f6c49e73c50105ecb9c8` | `e2d48bf6f38f7aebfae2f57cf5b11028b34ed958295c944d996be7e23a0ab042` |
| `pdd/prompts/pin_example_hack_python.prompt` | `7efec65cda40e8357cd08c974abc4b1aebb450d2fe75401e5cc5629e0696feb9` | `4d3f9831124e7ff623e1f0d19a30600a7d938e874ce058eca19be229f12c812f` |
| `pdd/prompts/sync_main_python.prompt` | `f112de7cfda339c31245f82a1670cf75ff1a1afddac0d8bbcd176f1929bfa7ba` | `5449cdae2b079f471dc29fd6cfc1457aa247f2fc09df7a722f565394e63fb35b` |
| `pdd/prompts/sync_orchestration_python.prompt` | `ca4ad5eff6774715d7a65c73e17a12f79da66cb409c69fe90bf41ae097181266` | `8cfee3a49468843dc68e4fff1edf6035e4bff2a84b7bca613509d66dc6eda417` |
| `pdd/prompts/update_main_python.prompt` | `b1042be0dd594c9bbb197a4afe7e5c5a12aaad1574feadf7cb5b26f9bf85bccd` | `a27cbc96741c8b0847aa389e55b19b878e8bbcf29fb6f926d4df463d7866684f` |

| Unit | Classification | Current evidence |
| --- | --- | --- |
| `commands/modify` | `REQUIRED_UPDATED` | The prompt states that a transactional metadata/fingerprint failure exits non-zero. `.pdd/meta/commands_modify_python.json` was regenerated by `save_fingerprint` with current prompt, code, example, `tests/commands/test_modify.py`, `test_files`, and include hashes; its paired run record reports 46 passing tests. The exact `architecture.json` path remains `pdd/commands/modify.py`. |
| `metadata_sync` | `REQUIRED_UPDATED` | The prompt now declares `fingerprint_transaction_python.prompt`; the architecture dependency is the same. `.pdd/meta/metadata_sync_python.json` was regenerated with current prompt/code/example/test/include hashes and `.pdd/meta/metadata_sync_python_run.json` was published through `AtomicStateUpdate` after the focused 47-test run, including real-owner injections for both pending-buffer mutations. Its exact architecture path is `pdd/metadata_sync.py`. |
| `fingerprint_transaction` | `REQUIRED_UPDATED` | The prompt and code require canonical role authority, portable private lock ownership, and nested-abort propagation. `tests/test_transactional_finalizer.py` supplies root-fallback, custom/nested configuration, caught-exception abort, Windows-style lock fallback, concurrency, recovery, failure injection, and the deterministic post-command freshness property for every #1926 writer (`auto-deps`, generate, example, update, fix, metadata sync, sync, pin, and CI heal). The canonical metadata maps both `tests/test_fingerprint_transaction.py` and this substantive transactional suite. Its exact architecture path is `pdd/fingerprint_transaction.py`. |
| `update_main` | `REQUIRED_UPDATED` | Redirected true updates now lock the canonical source unit before workflow reads or mutation while retaining redirected-output fingerprint suppression. `tests/test_update_main.py` includes a two-thread explicit-output regression proving two calls cannot enter the workflow concurrently. Its exact architecture path is `pdd/update_main.py`. |
| `auto_deps`, `operation_log`, `sync_main`, `sync_orchestration`, `commands/maintenance`, `commands/modify` | `REQUIRED_UPDATED` | Each prompt and matching `architecture.json` entry now declares its direct transactional-finalizer dependency, so generated PDD topology records the actual mutation/failure boundary. |
| `ci_drift_heal` | `REQUIRED_UPDATED` | The tracked run evidence is refreshed against the current CI-heal test mapping; its fingerprint remains prompt/code/test-current. |
| `sync_determine_operation` | `REQUIRED_UPDATED` | Its tracked run evidence is refreshed; reader recovery remains deliberately lazy because the finalizer consumes its `Fingerprint` and path APIs, avoiding an eager import cycle. |
| `sync_orchestration` | `REQUIRED_UPDATED` | Its prompt/code change remains represented by regenerated prompt/code/test/example/include hashes in `.pdd/meta/sync_orchestration_python.json`. The focused crash/skip set passed (33 selected, 184 deselected) and exercised its paired-state path; a per-unit run JSON is `NOT_APPLICABLE` because the repository ignores that record. Its exact architecture path is `pdd/sync_orchestration.py`. |
| `pin_example_hack` | `REQUIRED_UPDATED` | The missing exact `architecture.json` entry now maps `pin_example_hack_python.prompt` to `pdd/pin_example_hack.py`, with the real `_execute_tests_and_create_run_report` interface and dependencies. Its regenerated metadata covers prompt/code/example/test/include hashes; its paired run record reports 2 passing tests. |
| `json_atomic` | `REQUIRED_ALREADY_CURRENT` / `HUMAN_OWNED` | `docs/pdd_hand_owned_modules.md` records the explicit durability-primitive exemption. It is not regeneration-safe under the prompting guide's declared-interface/behavioral/negative-test mold, so it must remain hand-owned until a separately reviewable generated interface and complete verification mold exist. Direct caller and transactional failure coverage includes `tests/test_transactional_finalizer.py` and `tests/test_auto_deps_architecture.py`; changes require write, fsync, replace, and cleanup failure coverage. |

All prompt-owned rows use the repository root `.pddrc`; the canonical resolver
also has focused positive coverage for a custom output directory and a nested
`.pddrc` root. Exact architecture paths and dependency declarations, rather
than matching filenames or artifact existence, are the authority for their
role mappings.
