"""Dependency-sliced ``pdd`` package stub (scenario ``pdd-find-section``).

The upstream ``pdd/__init__.py`` re-exports the full CLI surface and is not
part of the slice's dependency closure; this stub replaces it so the sliced
core imports cleanly. Recorded as a slicing artifact in ``core_files.txt``
and ``seed_novelty.md``.
"""
