# Gemini Customization (PDD CLI)

This file helps customize Gemini's behavior for the `pdd` directory.

## PDD CLI Development

- This is a Python CLI tool.
- Use `pip` and `requirements.txt` for package management.
- The main entry point is `pdd/cli.py`.
- Use `argparse` for command-line argument parsing.
