## Verdict
pass
## Summary
Frame sampled at 96.9% (frame 464/480), corresponding to animation phase 10 (Frame 450-480: caption hold). All critical elements are present and correctly rendered:

1. **Mold diagram (left):** Visible at reduced scale (~0.5×), positioned on the left side. Shows all three regions — amber 'Tests' walls, blue 'Prompt' nozzle, green 'Grounding' cavity. Labels are visible. Code output box appears below the mold with '{ code output }' text. 'The Mold' label visible beneath.

2. **Summary table (right):** Background panel with rounded corners present. Header row shows 'COMPONENT | ENCODES | OWNER' in small caps/uppercase muted text. Three data rows correctly rendered:
   - Row 1: Blue dot + 'Prompt' in blue, 'WHAT (intent)', 'Developer'
   - Row 2: Green dot + 'Grounding' in green, 'HOW (style)', 'Automatic'
   - Row 3: Amber dot + 'Tests' in amber, 'CORRECTNESS' in bold, 'Accumulated'
   Horizontal dividers visible between rows.

3. **Conflict resolution line:** 'When these conflict, tests win. Always.' visible below the table. 'tests win' and 'Always.' rendered in amber/gold color as specified. Subtle underline visible.

4. **Code output (bottom left):** Code snippet visible in monospace font, dimmed to low opacity (~0.2) as specified for this phase. The code shows a function definition consistent with the spec's intent.

5. **Caption (bottom center):** 'The code is output. The mold is what matters.' visible in amber/gold color, horizontally centered. Present as expected for frame 450-480.

6. **Background:** Dark navy-black background consistent with #0A0F1A. Subtle grid lines faintly visible.

All layout, color, typography, and animation-phase expectations are met. The table rows use correct color coding, the Tests row appears slightly emphasized, the conflict line highlights the right words in amber, the code is dimmed, and the final caption is displayed. Minor layout positioning differences (table slightly higher, mold slightly left of exact spec coordinates) are well within the 3% tolerance and preserve the intended composition.
