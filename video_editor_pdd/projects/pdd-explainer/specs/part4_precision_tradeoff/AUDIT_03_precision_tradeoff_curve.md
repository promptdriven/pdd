## Verdict
fail
## Summary
The inverse/hyperbola curve is drawn in the wrong direction. The spec requires a curve starting HIGH on the left (few tests = high prompt precision) and dropping LOW toward the right (many tests = low prompt precision). The rendered frame shows the curve going from LOW-LEFT to HIGH-RIGHT, which communicates the opposite relationship: more tests = more precision needed. The Y-axis labels (High at top, Low at bottom) confirm this is inverted. The left zone with 'Detailed prompt required' should be at the HIGH end of the curve (upper-left region) but visually sits at the LOW end. The right zone with 'Intent is enough' should be at the LOW end (lower-right region) but sits at the HIGH end. The curve function in the spec (expected vertical anchor + 580 × (1 / (1 + 0.08 × x_test_count))) describes a decreasing hyperbola, but the render shows an increasing logarithmic-like curve.
