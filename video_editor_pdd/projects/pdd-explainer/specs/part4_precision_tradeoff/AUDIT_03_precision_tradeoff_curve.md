## Verdict
fail
## Summary
The inverse curve is drawn in the wrong direction. The spec requires a decreasing hyperbola starting high on the left (few tests = high prompt precision) and dropping toward the lower-right (many tests = low prompt precision). The rendered frame shows an increasing logarithmic curve starting low-left and rising to upper-right, which communicates the opposite relationship. At frame 419/450 (hold phase), the completed chart should show the full inverse curve with both annotation zones positioned correctly relative to the curve's height — left zone in the HIGH precision region (upper-left), right zone in the LOW precision region (lower-right). Instead, the left zone sits near the bottom of the curve and the right zone sits near the top. The slider correctly sits at the right end of the curve for this hold phase, but the curve itself is fundamentally inverted. The title, axes, tick marks, labels, zone icons (dense prompt, sparse prompt with wall icons), and zone labels are all present and correctly styled.
