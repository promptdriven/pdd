## Summary
Adds failing tests that detect the bug reported in #921.

## Test Files
- Unit test: `tests/test_auto_heal_workflow.py`

## What This PR Contains
- Failing unit test that reproduces the reported bug
- Tests are verified to fail on current code and will pass once the bug is fixed

## Root Cause
The `auto-heal` workflow uses the GitHub REST API (`repos/{owner}/{repo}/collaborators/{username}`) to authorize the PR author against the `pdd_cloud` repository. However, GitHub Apps are not traditional users and cannot be "collaborators" on a repository. When the autonomous bot (`prompt-driven-github[bot]`) creates a PR, the API predictably returns a 404 Not Found, causing the workflow to reject the authorization and permanently block the PR from merging.

## Next Steps
1. [ ] Implement the fix at the identified location
2. [ ] Verify the unit test passes
3. [ ] Verify the E2E test passes
4. [ ] Run full test suite
5. [ ] Mark PR as ready for review

Fixes #921

---
*Generated by PDD agentic bug workflow*