## Summary
Adds failing tests that detect the bug reported in #880.

## Test Files
- Unit test: `tests/test_agentic_update.py`
- Unit test: `tests/test_issue_880_reproduction.py`

## What This PR Contains
- Failing unit test that reproduces the reported bug
- Tests are verified to fail on current code and will pass once the bug is fixed

## Root Cause
The `_allowed` set in `pdd/agentic_update.py` only permits the `prompt_path` and `code_path`, erroneously excluding explicit `<include>` docs and new `context/` files. This contradicts `agentic_update_LLM.prompt`. Fixing this requires modifying the guard and updating tracking variables (`before_paths`, `after_paths`) to observe the permitted scope expansion.

## Next Steps
1. [ ] Implement the fix at the identified location
2. [ ] Verify the unit test passes
3. [ ] Verify the E2E test passes
4. [ ] Run full test suite
5. [ ] Mark PR as ready for review

Fixes #880

---
*Generated by PDD agentic bug workflow*